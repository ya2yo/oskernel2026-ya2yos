= 内存管理

== 地址空间与物理页

`mm` 模块负责内核直接映射、每进程用户页表和物理页生命周期。RISC-V 使用 Sv39 风格的页表模型；LoongArch 使用其架构专用页表实现，但对上层暴露相同的 `VirtAddr`、`PhysAddr`、`VirtPageNum`、`PhysPageNum` 和 `MemorySet` 概念。

初始化先建立内核堆和 CMA 物理页分配器，再激活内核地址空间。为处理大内存启动期的映射约束，物理页分配器支持早期区间和页表完整建立后的 late 区间初始化。页表/TLB 操作保持在架构实现中。

== `MemorySet`、VMA 与 ELF

`MemorySet` 管理一个进程的页表和映射区域。`MapArea` 记录虚拟页范围、映射类型、权限及与文件映射相关的元数据；`mmap_ops.rs`、`area_ops.rs` 和 `fork_clone.rs` 将映射、拆分、保护属性与 fork 复制职责拆开。

ELF 加载器读取可加载段，按权限创建 VMA，并设置用户栈、堆及辅助向量。`PT_INTERP` 路径支持动态链接器所需的装载信息。`execve` 构建新的地址空间后再替换进程资源，以避免半初始化状态对外可见。

== 按需分页、文件映射与 COW

匿名映射和可扩展堆可以延迟到首次访问才分配物理页。页故障处理按照 VMA 属性判断当前访问是否合法，并可能执行以下动作：

1. 为合法的匿名页分配并映射清零页；
2. 为文件映射读取或建立相应页面；
3. 对 fork 后只读共享的私有页执行写时复制；
4. 对完整文件 EOF 以外的映射访问报告 `SIGBUS`，而不是伪造零页；
5. 对不属于任何有效 VMA 的访问生成相应异常信号。

fork 不直接复制全部用户页面。私有可写页以 COW 方式暂时降为只读，第一次写入才分配独立页；共享映射依据 `MAP_SHARED` 语义保留共享关系。`munmap`、`mprotect` 和 `mremap` 必须同步调整 VMA 边界和页表权限。

== 用户内存访问

系统调用绝不将用户虚拟地址当作内核可直接解引用的指针。`copy_from_user`、`copy_to_user` 及其值类型变体执行范围检查、逐页翻译和必要的懒缺页处理。它们是读取路径字符串、复制 ioctl 结构体、写回 stat 信息以及构造信号帧的基础。

这里的关键不变量是：用户范围不得溢出或越过用户地址上限；每一页都必须具有所需权限；访问过程中不持有进程元数据、fd 表或网络/文件系统的长锁。这样既避免错误地址破坏内核，也避免跨子系统的锁顺序反转。
