= 当前边界与演进方向

== 已知边界

Ya2yOS 的目标是竞赛环境下的 Linux 用户态兼容，而非完整 Linux 内核替代品。接口覆盖范围会随测例推进；某些系统调用、socket option、文件系统特性或设备路径仍可能是部分实现、stub 或仅在特定架构可用。调用方应以实际 syscall 返回值和目标测试结果判断可用性。

当前默认启动配置主要以单 hart 运行。代码中保留了多 hart 启动屏障与 per-processor 结构，但并不等同于所有子系统已完成面向 SMP 的压力验证。网络服务含轮询驱动部分；性能、可扩展性和实时性不应由功能正确性推断。

== 后续工作

- 继续用目标 Linux 测例完善边缘 errno、信号、文件锁、挂载和 socket 语义，并将每个非平凡修复沉淀为 problem 文档。
- 扩大两架构的自动化回归，覆盖启动、用户程序、内存故障、文件系统、网络真实 TX/RX 和长时稳定性。
- 审视锁顺序、可中断阻塞点和用户内存访问边界，逐步缩小全局锁覆盖范围。
- 改善多 hart 运行时验证与设备中断/轮询协同，避免将单核假设带入通用路径。
- 让设计文档随源码重构同步更新：修改模块边界、核心不变量或启动/验证流程时，应同时更新本 Typst 工程。

== 阅读路线

首次阅读建议先看“系统概览”和“启动、架构与异常入口”，再按问题域进入内存、任务/信号、VFS 或网络/驱动。定位具体实现时，文中列出的目录可直接对应 `os/src/`；定位已解决问题和测试证据时，请转到 `Docs/决赛文档/problem/` 的索引。
