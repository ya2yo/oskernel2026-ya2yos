# BuildStorm 待实施优化方案

> 范围：本文件记录本轮优化目标、已实施的 P1 以及后续验收边界；历史依据见
> `Docs/决赛文档/problem/` 与开发日志。
>
> P1 已完成：缩短 `Ext4Inode::write_at()` 中由纯 Rust 配额记账占用的
> `EXT4_OP_LOCK` 时间。所有 lwext4 path/file C API 继续串行化；不调整 sparse 容量、
> dentry 或全局锁类型。

当前剩余工作是按 P0 配置完成两次约 30 分钟 RISC-V 样本、定向 sparse 语义回归及完整
BuildStorm 验收；`tmp_16.ans` 已提供十分钟观测，本轮另用 180 秒 qcow2 overlay 做启动烟测，均不能替代完整验收。

## 1. 已确认的目标

当前 `log.ans` 是未完成的 RISC-V BuildStorm 运行：`BUILDSTORM_TOOLCHAIN` 与
`BUILDSTORM_MINIBUILD` 为 ok，最后 perf 快照是 `t=1771.859s`，没有
`BUILDSTORM_COMPILE`、测试组 END 或 `shutdown!`，也没有 `panic`、`TFAIL`、`TBROK`、`ERROR`。

| 与随后 perf 快照对齐的时间 | Cargo 进度 |
| --- | ---: |
| `t=75.861s` | `0/446` |
| `t=712.012s` | `47/446` |
| `t=968.778s` | `48/446` |
| `t=1601.712s` | `54/446` |
| `t=1771.859s` | `54/446` |

在 `t=1107.360s` 到 `t=1771.859s` 的 `664.499s` 窗口内，`ext4_sparse_buffer` 固定为
`88 batches / 34,922,007 B`，`batch_max_bytes=524288`，所有 `global_budget_*` 和分配失败计数为零。
该窗口内 `EXT4_OP_LOCK` 的分类 `hold_us` 合计约 `642.17s`，说明全局 gate 接近饱和。

写路径是本轮唯一可安全缩短的已定位区段：该窗口新增 `560` 次 `ext4_write_lock`，累计
`wait_us=421.569s`、`hold_us=104.765s`；其已有阶段统计为 open `22.233s`、quota `22.181s`、
data `59.875s`。其中 quota 仅执行 `MNT_TABLE` 容量预留、`quota_reserved` 更新与错误回滚，不调用
lwext4；data 阶段的 `file_write_at()` 必须保留在 `EXT4_OP_LOCK` 内。

## 2. 不变约束

- 保持 `MAX_SPARSE_WRITE_BUFFER_SIZE = 512 KiB`、`MAX_SPARSE_WRITE_BUFFER_RUNS = 32`、
  `MAX_TOTAL_SPARSE_WRITE_BUFFER_BYTES = 8 MiB`；本轮不修改这些常量或 sparse 发布边界。
- `EXT4_OP_LOCK` 继续保护所有 lwext4 block cache 及 path/file C API。禁止改为 rwlock，禁止多 hart
  并发进入 lwext4。
- 保持锁序 `write_state -> io_state -> EXT4_OP_LOCK`；本轮新增的约束是 `MNT_TABLE` 不能与
  `EXT4_OP_LOCK` 同时持有。
- 保持 quota reserve 的粒度、最终小 chunk 的 `ENOSPC` 回退、`quota_reserved` 更新、写失败回滚、
  `defer_close_flush()`、`st_blocks`/mtime/ctime 失效和 sparse overlay 语义。

## 3. P0：建立可比较基线

在改动前以同一源码快照、镜像、QEMU 内存/hart 数和相同冷/热缓存状态运行两次 RISC-V 样本。每次保存：

```bash
git rev-parse HEAD
git status --short
/usr/bin/time -f 'elapsed_s=%e user_s=%U sys_s=%S' \
  timeout 1900s make run TARGET_ARCH=riscv64 > /tmp/buildstorm-quota-before.log 2>&1
```

从日志同时提取 Cargo 进度和 perf 快照：

```bash
perl -pe 's/\e\[[0-9;]*[[:alpha:]]//g' /tmp/buildstorm-quota-before.log \
  | rg -a 'Building.*[0-9]+/446|^\[perf\] t=[0-9]+ms syscalls|ext4_write_(lock|duration)'
```

记录 `47/446`、`48/446`、`54/446` 附近的 guest `t=...ms`，以及 `ext4_write_lock`、
`ext4_namespace_lock`、`ext4_fstat_lock` 的 samples/wait/hold。未完成样本只能比较相同 Cargo 阶段的
增量，不能以终点或外层 timeout 宣称端到端加速。

## 4. P1：将 write quota 移出 `EXT4_OP_LOCK`（已实施）

这是本轮唯一的代码改动，涉及 `os/src/fs/ext4_lw/inode.rs`、`os/src/utils/perf/fs.rs` 和
`os/src/utils/perf/report.rs`。

### 4.1 先拆分 write 锁统计

在 `os/src/utils/perf/fs.rs` 中把现有 `Ext4LockClass::Write` 拆为 `WriteOpen` 与 `WriteData`：

1. 为两类各增加一个 `Ext4LockStats`，并保留现有 `EXT4_WRITE_LOCK_STATS` 作为两段的总计。
2. 增加 `Ext4OpLock::lock_for_write_open()`、`lock_for_write_data()`；两者都记入 write 总计，分别记入
   open/data 子计。
3. 在 `os/src/utils/perf/report.rs` 输出 `ext4_write_open_lock` 与 `ext4_write_data_lock`。

这样可以区分“为得到 descriptor/size 的必要 gate 时间”和“实际 `file_write_at()` 的必要 gate 时间”，而不是
在改动后把减少的 quota 时间误读成 data 加速。

### 4.2 重排 `Ext4Inode::write_at()`

在 `os/src/fs/ext4_lw/inode.rs` 的 `write_at()` 中保持 `write_state`、`io_state` 覆盖整个操作，但按以下顺序
重构：

```text
write_state -> io_state
  -> WriteOpen gate: live path、ensure_open(O_RDWR)、known_size/file_size、delayed pin
  -> 释放 EXT4_OP_LOCK
  -> MNT_TABLE.reserve_write() 与 quota_reserved 更新
  -> WriteData gate: sparse 判定、file_write_at()、失败时 defer_close_flush()
  -> 释放 EXT4_OP_LOCK
  -> 写失败时 MNT_TABLE.rollback_reservation()；成功时更新 size/cache 状态
```

具体要求如下：

1. `WriteOpen` 段内复制稳定的 `path`，并以已有 `known_size()` 为准；仅在它为空时调用
   `file.file_size()`。`write_state` 已与 rename/truncate/write 互斥，因而 quota 阶段使用该 path 和 size
   不会与同 inode 的路径或大小变更并发。
2. quota 阶段原有的 `target` 对齐、`previous_reserved.max(current_size)`、首次 `ENOSPC` 后按 `end`
   再试、以及所有 `quota_reserved` 写入必须逐字保留；仅改变其不再位于 global gate 内。
3. `WriteData` 段保留 `disable_write_back_cache()`、`file_write_at()` 与失败时的
   `defer_close_flush()`。从 `file.last_write_path()` 取得 fstat 失效原因后再释放该 gate；不得让任何
   `Ext4File` 方法跨到 quota 或 rollback 段。
4. 失败回滚在 `WriteData` guard 释放后执行。成功与失败都不得持有 `MNT_TABLE` 时获取
   `EXT4_OP_LOCK`，也不得持有 `EXT4_OP_LOCK` 时获取 `MNT_TABLE`。

### 4.3 同一补丁解除既有反向嵌套

上述顺序要求同时修改 `inode.rs` 中两处既有嵌套，避免 quota 外移后引入
`MNT_TABLE -> EXT4_OP_LOCK` 与旧 `EXT4_OP_LOCK -> MNT_TABLE` 的循环等待：

1. `Ext4Inode::unlink()`：在 namespace gate 内只完成 `file_remove()`、epoch 与 stat-cache 更新；成功后
   退出该 scope，再调用 `MNT_TABLE.remove_file(path)`。
2. `impl Drop for Ext4Inode`：在 close gate 内完成 delayed `file_remove()` 和 `file_close()`，将待删除路径
   保存到局部值；guard 释放后才调用 `MNT_TABLE.remove_file()`。

这两处的 quota 删除必须只在 lwext4 删除成功时发生，错误返回、延迟删除和 inode identity/directory epoch
的现有语义保持不变。

## 5. P2：验证 P1

先执行构建与静态检查：

```bash
cargo fmt --manifest-path os/Cargo.toml -- --check
make perf TARGET_ARCH=riscv64
make perf TARGET_ARCH=loongarch64
make TARGET_ARCH=riscv64
make TARGET_ARCH=loongarch64
git diff --check
```

随后以 P0 的完全相同配置运行两次约 30 分钟 RISC-V BuildStorm，并完成 sparse 相关回归：

- `mmap16`、`lseek11` 的 `SEEK_DATA/SEEK_HOLE`；
- `fstat/statx` 的 `st_blocks`、mtime/ctime；
- write-read、rename、truncate、unlink-open-write-close、fsync/sync。

P1 仅在下列条件同时满足时保留：

1. 无 `panic`、`ERROR`、`TFAIL`、`TBROK`，且 quota `ENOSPC` 与写失败回滚语义不变。
2. 同一 Cargo 检查点的 `ext4_write_lock` hold/wait 下降；新增 `ext4_write_open_lock` 与
   `ext4_write_data_lock` 证明 quota 不再占用 global gate，且 data hold 没有回升。
3. `ext4_write_duration quota` 的工作量与基线同量级，表明收益来自缩短锁域而不是跳过 quota。
4. 两次样本均不比 P0 更慢；若任一项失败，回退 P1，不继续叠加其他缓存或锁改动。

## 6. P3：完整 BuildStorm 验收

P1 通过后，使用同一 source snapshot 与配置运行到脚本自身结束，而不是以外层短 timeout 判断结果：

```text
BUILDSTORM_COMPILE mode=multi ok=true elapsed_s=<...>
#### OS COMP TEST GROUP END buildstorm ####
shutdown!
```

报告 guest `elapsed_s`、宿主 wall-clock、两次完整运行的结果和 RISC-V 配置。LoongArch64 至少完成构建；
若 P1 改动了共享文件系统语义，再运行对应 guest 回归。只有完整样本才可报告 BuildStorm 端到端性能。

## 9. 参考

- [BuildStorm EXT4 稀疏写缓冲、顺序预读与锁统计收敛](./problem/buildstorm-ext4-sparse-write-readahead.md)
- [BuildStorm 读路径、FsIndex 与 fstat 锁争用](./problem/buildstorm-read-path-lock-contention.md)
- [开发日志](./开发日志.md)

## 8. 下一轮候选：恢复正向 dentry 命中

`tmp_16.ans` 最后快照 `t=573.267s`、Cargo `Building 30/446` 附近仍显示
`dentry_positive_hit=0`，但 `fsidx_hit=49,829`、`fsidx_miss=20,498`、`cached_parent_find=6,012`；同期
`ext4_find_lock` 为 `12,447` 次、累计 `wait_us=455,517,853`、`hold_us=71,616,077`，`path/open/stat` syscall
累计分别为 `315.051s/542.356s/613.668s`。这支持“正向 dentry 未命中导致重复 find/path 元数据操作”的可证伪假设，
但累计锁时间仍是多 hart 总量，不能直接等同 wall-clock。

下一轮先做 P4.0 取证：沿 `DENTRY_CACHE` lookup、插入、失效和 `FsIndex` 发布路径补充命中/拒绝原因的低开销
聚合计数，并在相同入口重复两次样本；只有确认 key/lifetime/epoch 语义完整后，才恢复正向 dentry 插入或复用。
必须保持 rename、unlink、inode identity epoch、负 dentry、`O_CREAT` 和 `O_NOFOLLOW` 语义，不与 P1 的锁域改动叠加。
