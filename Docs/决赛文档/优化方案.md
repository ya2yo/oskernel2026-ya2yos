# BuildStorm 优化方案

> 本文件只保留当前可执行计划、约束和验收边界。历史取证细节见
> [BuildStorm EXT4 稀疏写缓冲、顺序预读与锁统计收敛](./problem/buildstorm-ext4-sparse-write-readahead.md)、
> [BuildStorm 读路径、FsIndex 与 fstat 锁争用](./problem/buildstorm-read-path-lock-contention.md)
> 和 [开发日志](./开发日志.md)。

## 1. 当前结论

- 最新样本 tmp_08.ans 已装载目录 stat 局部 epoch 与 local_epoch_miss/global_epoch_miss 标签；日志无
  panic/TFAIL/TBROK/ERROR/SIGSEGV/rustc error，但止于 t=558.003s、Cargo Building 28/446，没有
  BUILDSTORM_COMPILE、测试组 END 或 shutdown!，因此不是完整 BuildStorm 验收样本。
- tmp_08 长时间没有“编译成功”输出，是 Cargo 仍在 28/446 附近编译或等待重 crate，进度条未刷新；后续 perf
  快照仍在打印，不能解释为成功后卡死或新崩溃。
- 目录局部 epoch 有方向性收益：相邻窗口中 ext4_fstat_lock 从 3174 / 227.383s wait / 56.014s hold
  降到 2632 / 166.707s wait / 44.491s hold，actual_ext4_fstat 从 3168 降到 2630，
  directory_epoch_cached 从 2119 增至 2682。Cargo 阶段不同，不能报告严格 A/B 或端到端加速。
- 页缓存不是下一轮优先项：tmp_08 末尾 resident_pages=57468/98304、capacity_bypass_pages=0，
  mmap_prefetch=0，四类 inode_read_source 继续与 ext4 reads 守恒。
- 当前主要问题是无输出窗口内仍有多类累计等待增长：ext4_read_data_lock、ext4_find_lock、
  ext4_fstat_lock、ext4_write_lock、ext4_namespace_lock 和 pipe read_wait。下一轮先做 interval delta
  和子阶段归因，再决定是否移动锁边界。

## 2. 已固定优化

| 阶段 | 状态 | 保留结论 |
| --- | --- | --- |
| P1 写路径 | 已实施 | write quota 移出 EXT4_OP_LOCK；write open/data 锁拆分；unlink/drop 中 MNT_TABLE 与 EXT4_OP_LOCK 不再反向嵌套。 |
| P4/P5 dentry/stat | 已实施 | 正 dentry 被 FsIndex path 命中绕过，不恢复正向 dentry fast path；负 dentry 保留；目录 stat cache 受 epoch 保护。 |
| P9 文件页缓存 | 已实施 | regular-file read 准入固定为 32 MiB，全局页缓存固定为 96K 页；容量满时直读但不发布，不能返回 ENOMEM 或旧数据。 |
| P10/P11 读来源 | 已实施 | inode_read_source 覆盖 mmap fill、page-cached cold run、direct bypass、other；未对齐 ELF 段改为最大 64 KiB 分块读。 |
| P12/P13 mmap | 已实施 | mmap 来源拆分 demand/prefetch；EOF 检查改为页偏移与 inode.size() 判断，避免普通 fault 的重复页缓存查询。 |
| P15 目录 epoch | 已实施 | 目录自身 metadata 只推进本 inode directory_stat_epoch；rename/link/unlink/delayed remove 等保守边界继续推进全局 epoch。 |
| BuildStorm 入口 | 已调整 | RUST_MIN_STACK=33554432 避免 tmp_07 的 rustc 栈不足崩溃；initproc 当前为直接运行 buildstorm::compile::run() 的定向入口。 |

## 3. 不变约束

- EXT4_OP_LOCK 继续串行化所有 lwext4 block cache 与 path/file C API；不改 rwlock，不允许多 hart 并发进入 lwext4。
- 保持锁序简单：write_state -> io_state -> EXT4_OP_LOCK；不得在持有 MNT_TABLE 时获取 EXT4_OP_LOCK，也不得反向嵌套。
- 不扩大 P9 参数：保持 32 MiB 文件准入、96K 页全局容量、四页预读上限，除非新样本证明容量触顶或预读 miss 明确主导。
- 不牺牲文件系统语义：quota reserve/rollback、ENOSPC 小 chunk 回退、sparse overlay、st_blocks、mtime/ctime、
  rename/truncate/unlink/write 的 cache 失效必须保持。
- 未出现完整结束标记前，所有十分钟样本只可用于方向性取证，不能报告端到端加速。

## 4. 下一轮 P16

先补取证字段，不立即移动 lwext4 gate：

1. Interval delta：在保留累计值的同时输出上一次 perf report 以来的 delta，覆盖 ext4_*_lock、
   syscall_duration read/write/open/stat/path、pipe_duration read_wait、scheduler wakeup 和 Cargo 进度标记附近的 t=...。
2. Namespace/create 细分：将 create 类持锁时间拆成 exist_check、dir_mk_or_file_open、file_close、
   metadata_apply、VFS/cache 失效等子阶段。tmp_08 末尾 ext4_namespace_lock hold=114.849s 已值得单独归因。
3. Pipe wait 归因：记录 read wait 时是否存在 writer、writer 是否 runnable/睡眠、是否发生 poll 唤醒后重检。
   只有确认 writer 可运行但未及时调度，才进入 scheduler 或 pipe 唤醒优化。
4. 静默窗口比较：单独比较 Cargo 28/446 后的 interval delta。若 namespace/create 子阶段占主导，再评估复用
   negative dentry/FsIndex 结果跳过重复 check_inode_exist，或把纯 VFS/cache 收尾移出 gate；若 read/find 占主导，
   回到路径解析与冷页读取。

## 5. 验收边界

构建与静态检查：

    cargo fmt --manifest-path os/Cargo.toml --all -- --check
    git diff --check
    make perf TARGET_ARCH=riscv64
    make perf TARGET_ARCH=loongarch64
    make TARGET_ARCH=riscv64
    make TARGET_ARCH=loongarch64

运行期验收必须看测试自身结束，而不是外层 timeout：

    BUILDSTORM_COMPILE mode=multi ok=true elapsed_s=<...>
    #### OS COMP TEST GROUP END buildstorm ####
    shutdown!

如果下一轮改动触及写入、rename、truncate、unlink、mmap 或 stat 语义，还需补定向回归：

- mmap16、lseek11 的 SEEK_DATA/SEEK_HOLE；
- fstat/statx 的 st_blocks、mtime/ctime；
- write-read、rename、truncate、unlink-open-write-close、fsync/sync；
- 文件映射部分尾页、完整越界页和并发 truncate 的 SIGBUS。

## 6. 常用提取

    rg -a -n "BUILDSTORM|shutdown!|panic|TFAIL|TBROK|ERROR|SIGSEGV|Building.*[0-9]+/446" tmp_08.ans
    rg -a -n "^\[perf\] (t=|ext4_.*_lock|ext4_.*duration|pipe_duration|syscall_duration|file_cache|inode_read_source)" tmp_08.ans
    strings tmp_08.ans | tail -160

## 7. 参考

- [BuildStorm EXT4 稀疏写缓冲、顺序预读与锁统计收敛](./problem/buildstorm-ext4-sparse-write-readahead.md)
- [BuildStorm 读路径、FsIndex 与 fstat 锁争用](./problem/buildstorm-read-path-lock-contention.md)
- [开发日志](./开发日志.md)
