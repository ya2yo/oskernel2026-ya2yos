## 7.2

重构 PCB 与地址空间锁边界：将自带同步的 `MemorySet`、`SigTable`、`FdTable`、`FSInfo` 移出旧 `ProcessInner`，`MemorySet` 内部持有 `RwLock<MemorySetInner>`，`personality` 并入 `ProcessMeta`。

继续重构 PCB 访问接口：移除 `Process::inner_lock()` 和 `ProcessResources` 兼容层，调用点直接通过 `Process` 访问地址空间、信号表、fd 表和 fs 上下文；`make` 通过，QEMU 运行验证本轮未完成。

新增 `ResourceSlot<T>` 资源槽抽象，将 `Process.memory_set` 与 `Process.sig_table` 改为私有可替换槽位，明确区分资源内部同步和进程资源指针替换；`make` 通过，LoongArch64 `make run` 基础测试跑到 `shutdown!`。

继续收敛 PCB 地址空间接口：删除 `get_locked_memory_set_read/write()` 兼容方法，调用点统一使用 `memory_set_arc()`；`make` 通过，LoongArch64 `make run` 基础测试跑到 `shutdown!`。

收敛 pipe 文件描述符分类：`pipe2()` 分配的匿名 pipe 统一存入 `FileClass::Pipe`，`vmsplice` 改为通过 pipe 类型入口校验和写入；默认 LoongArch64 `make` 通过。

实现 `splice(2)` 兼容搬运路径：支持 regular file 与 pipe、pipe 与 regular file、pipe 与 pipe 之间通过内核缓冲区传输，补齐 offset 写回、pipe offset `ESPIPE`、`O_APPEND` 输出 `EINVAL`、`SPLICE_F_NONBLOCK` 基础语义；LoongArch64/RISC-V 构建通过，LoongArch64 `splice01` musl/glibc 均 `TPASS`。详见 problem/。

优化 pipe 内部缓冲结构：将 `PipeRingBuffer` 从字节环改为 `PipeBuf` 片段队列，`splice(pipe, pipe)` 走片段引用移动，`tee()` 走片段引用复制；默认 LoongArch64 `make` 通过。当前 `initproc` 中的 `splice05` 仍暴露 socket splice `ENOTCONN` 语义缺口，需后续单独处理。

接入文件页面缓存到 `MAP_SHARED` mmap 与 `splice(file, pipe)`：共享文件缺页改由 `FILE_PAGE_CACHE` 提供 `FrameTracker`，`PipeBuf` 支持挂载 `FilePage`，普通文件到 pipe 的 `splice` 走缓存页引用 fast path；默认 LoongArch64 `make` 通过，当前 `splice09` 入口因内核版本要求 `TCONF/skipped` 并跑到 `shutdown!`。

修复 pipe LTP 语义：无读端写入 pipe 时先投递 `SIGPIPE` 再返回 `EPIPE`，并实现 `ioctl(FIONREAD)` 返回 pipe 当前可读字节数；默认 LoongArch64 `make` 通过，当前 `pipe02`/`pipe08`/`pipe12` 核心断言均 `TPASS`。详见 problem/。

重构 pipe 源码布局：将原 `os/src/fs/files/pipe.rs` 按功能拆分到 `os/src/fs/files/pipe/`，分别承载片段缓冲、共享 ring buffer、FIFO 表、splice/tee、阻塞等待和 `File` trait 实现；对外 `Pipe`、`make_pipe()`、`open_fifo()` 路径保持不变。默认 LoongArch64 `make` 通过。

## 7.3

实现 `signalfd4(2)` 基础语义：创建/更新 signal fd、读取 pending `siginfo_t`，支持 `SFD_CLOEXEC`、`SFD_NONBLOCK` 与 poll；构建验证受当前环境缺少 `x86_64-linux-musl-cc` 及 nightly `printf-compat` 不兼容阻塞。

将 `FsIndex` 从路径强引用表改为 VFS inode cache：使用 `(st_dev, st_ino)` 归并 inode 身份，路径索引只保存 cache key，inode cache 使用 `Weak` 避免缓存永久持有对象；`Ext4Inode` 增加路径别名以兼容 hard link / rename 后的 path-based lwext4 操作。RISC-V 与 LoongArch64 构建通过，RISC-V QEMU 交互验证 hard link、unlink 原路径、rename 后读取均正常。

修复 RISC-V Alpine 镜像启动期仍按竞赛测试镜像布局写 `/glibc/lib/libgcc_s.so.1` 和覆盖 `/bin/sh -> /musl/busybox` 的问题；动态解释器加载增加精确路径 `open_direct()` 回退，并将解释器加载失败作为 exec 错误传播。`make TARGET_ARCH=riscv64` 通过，25 秒 QEMU 日志未再出现原 ENOENT panic、`exec /bin/sh failed` 或 `FetchInstructionPageFault 0xfffffffffffffffe`。详见 [problem/riscv-alpine-initfiles-dynamic-link.md](./problem/riscv-alpine-initfiles-dynamic-link.md)。

将 `initproc` 调整为默认启动 `/bin/sh -i`，失败时回退 `/musl/busybox sh -i`，并补充 `TERM/HOME/SHELL/USER` 默认环境变量。`make TARGET_ARCH=riscv64` 通过，25 秒 QEMU 日志出现 `/ #` 提示符，可直接交互输入命令。详见 [problem/riscv-alpine-initfiles-dynamic-link.md](./problem/riscv-alpine-initfiles-dynamic-link.md)。

补充控制台 stdin 基础回显，shell 中输入的命令字符、回车和退格现在会显示在 QEMU 串口输出中。`make TARGET_ARCH=riscv64` 通过，延迟输入 `echo SHELL_ECHO_OK` 后日志显示命令本身和执行结果。详见 [problem/riscv-alpine-initfiles-dynamic-link.md](./problem/riscv-alpine-initfiles-dynamic-link.md)。

修复 `vi` 的 `:` 模式输入双回显：stdio 现在支持最小 `TCGETS/TCSETS*` termios，stdin 回显受 `ECHO` 位控制，`ICANON` 关闭时按原始字符返回。`make TARGET_ARCH=riscv64` 通过，QEMU 中 `stty -echo` 后输入命令不再被内核回显，恢复 `stty echo` 后回显正常。详见 [problem/riscv-alpine-initfiles-dynamic-link.md](./problem/riscv-alpine-initfiles-dynamic-link.md)。

修复 `iperf-musl` 后连续运行 `iperf-glibc` 的 5001 端口冲突与 glibc 栈保护失败：socket fd close/exit 主动 shutdown 释放 listener，`TCGETS` 改回 Linux old termios 36 字节布局，并在标准 `run_testsuit()` 收尾时清理被 init 收养的 daemon 子进程。`make` 通过，`/tmp/iperf-generic-cleanup.log` 中 musl/glibc 两轮 iperf 六项均 success，未再出现 `socket already listening on port 5001`。详见 [problem/iperf-port-reuse-termios-stack-smash.md](./problem/iperf-port-reuse-termios-stack-smash.md)。

修复内核资源锁边界重构后 `netperf` 回归：`FdTable::close()` 不再因父进程关闭 accepted fd 而提前 shutdown 子进程继承的控制连接，`FdTable::clear()` 仍在进程退出时主动释放 listening socket，避免 `netperf-glibc` 复用 12865 端口失败。`make` 通过，`timeout 300s make run > log.ans 2>&1` 中 musl/glibc 两轮 netperf 十项均 success。详见 [problem/netperf-glibc-port-reuse.md](./problem/netperf-glibc-port-reuse.md)。

修复 `cyclictest` STRESS 模式下 `hackbench` 大量 `CLIENT: ready write (error: Broken pipe)`：`sys_socketpair()` 不再连续两次 `alloc_fd()` 复用同一空闲 fd，AF_UNIX socket 补齐阻塞 `recv/accept` wake 语义。`make` 通过，`timeout 300s make run > log.ans 2>&1` 中 musl/glibc cyclictest 四项均 success，未再出现 ready 阶段 Broken pipe。详见 [problem/cyclictest-socketpair-fd-allocation.md](./problem/cyclictest-socketpair-fd-allocation.md)。

修复 libctest `clocale_mbfuncs` 全量入口报 `Interrupted system call`：`sigtimedwait(SIGCHLD)` 成功消费信号后现在会清理内部 `interrupt_waker/interrupted` 状态，避免随后 `wait4()` 误返回 `EINTR`。`make` 通过，`make run` 中 clocale 输出 `Pass!`。详见 [problem/libctest-sigtimedwait-eintr.md](./problem/libctest-sigtimedwait-eintr.md)。

修复 LTP `abort01` 中 `tst_test.c:1654` 的 `waitpid(...)=EINTR` `TBROK`：`waitpid/waitid` 不再由通用 `interruptible()` 抢先返回 `EINTR`，而是按 pending signal 与 `SA_RESTART` 语义返回内部 `ERESTART` 并交由 signal frame 重启。`make` 通过，最新 `log.ans` 中 `abort01` 为 `passed 2 failed 0 broken 0`。详见 [problem/waitpid-sa-restart-eintr.md](./problem/waitpid-sa-restart-eintr.md)。

修复 LTP `access02`：`clone_process()` 按 `CLONE_VM` 处理共享地址空间子任务，避免 vfork/clone 路径在 `MemorySet::get_mut()` panic；`OpenFlags::read_write()` 改为按 `O_ACCMODE` 解码，避免 `O_RDONLY|O_CLOEXEC/O_LARGEFILE` 被误判成读写打开。`make` 通过，最新 `log.ans` 中 `access02` 16 项核心断言均 `TPASS`。详见 [problem/access02-ltp-execve.md](./problem/access02-ltp-execve.md)。

修复 LTP `access04`：`faccessat()` 补齐单个路径分量 `NAME_MAX=255` 检查，并按绝对路径所属只读挂载点返回 `EROFS`。`make` 通过，最新 `log.ans` 中 `access04` 12 项核心断言均 `TPASS`。详见 [problem/access04-faccessat-name-rofs.md](./problem/access04-faccessat-name-rofs.md)。

修复 LTP `acct01`：`acct(2)` 改用 effective uid 做特权判断，补齐 `NAME_MAX`、尾随 `/`、只读挂载和错误优先级处理。`make` 通过，最新 `log.ans` 中 `acct01` 9 项核心断言均 `TPASS`。详见 [problem/acct01-sys-acct-errno.md](./problem/acct01-sys-acct-errno.md)。

修复 LTP `acct02`：process accounting 记录中的普通 `exit(128)` 不再被特殊归零，而是和 `wait4` 一致编码为 `exit_code << 8`。`make` 通过，`make run` 单跑 `acct02` 显示 `passed 1 failed 0 broken 0`。详见 [problem/acct02-exitcode-128.md](./problem/acct02-exitcode-128.md)。

修复 LTP `alarm05`：`getitimer/setitimer(old_value)` 现在返回按 `last_time` 折算后的剩余 `it_value`，周期 timer 到期后推进到下一轮 interval。`make` 通过，最新 `log.ans` 中 `alarm05` 3 项核心断言均 `TPASS`。详见 [problem/alarm05-itimer-remaining.md](./problem/alarm05-itimer-remaining.md)。

修复 LTP `bind01`：TCP/UDP `bind()` 增加本机地址归属检查，非本地地址返回 `EADDRNOTAVAIL`；AF_UNIX pathname bind 先校验父目录，路径前缀非目录返回 `ENOTDIR`。`make` 通过，最新 `log.ans` 中 `bind01` 7 项核心断言均 `TPASS`。详见 [problem/bind01-bind-address-path.md](./problem/bind01-bind-address-path.md)。

修复 LTP `bind04`：AF_UNIX pathname bind 创建 VFS socket 节点以支持 cleanup `unlink()`，补齐 AF_UNIX `SOCK_SEQPACKET`，允许 IPv4/IPv6 sockaddr 使用更大的 `sockaddr_storage` 长度，并为 SCTP one-to-one stream 与 IPv6 loopback 通信提供最小兼容。`make` 和单跑 `bind04` 的 `make run` 通过，最新 `log.ans` 中 16 项通信场景均 `TPASS`。详见 [problem/bind04-unix-seqpacket-sockaddr.md](./problem/bind04-unix-seqpacket-sockaddr.md)。

修复 LTP `splice07`：`splice` 对非 pipe 端增加文件类型准入检查，避免目录输入到 pipe 错误成功，以及空 pipe 读端 splice 到 `/dev/zero` 输出时阻塞；`DummyFd` 补默认 `fstat()` 以避免未实现 fd 错误路径 panic。`make` 通过，单跑 `splice07` summary 为 `passed 566 failed 0 broken 0 skipped 25 warnings 0`。详见 [problem/splice07-file-type-validation.md](./problem/splice07-file-type-validation.md)。

修复 LTP `open14` 长时间无输出：补齐 `O_CREAT|O_EXCL` 已存在返回 `EEXIST` 语义，减少 `mkdirat`/`linkat` 的重复路径查找，并收窄 `rmdir`/`unlink` 慢路径。`make` 通过，`timeout 600s make run` 单跑 `open14` summary 为 `passed 3 failed 0 broken 0`。详见 [problem/open14-otmpfile-path-slow.md](./problem/open14-otmpfile-path-slow.md)。

继续优化 `open14` 深层路径性能：`open()` 和 `create_file()` 现在优先复用 `FsIndex` 中已缓存的父目录 inode，并避免 `Ext4Inode::types()` 为类型判断再次走路径查询；`make` 与单跑 `open14` 通过。详见 [problem/open14-otmpfile-path-slow.md](./problem/open14-otmpfile-path-slow.md)。

实现 VFS dentry cache：在 `os/src/fs/dcache.rs` 增加父 inode + child name 的 positive/negative 目录项缓存，`open()` 复用缓存父目录时可直接命中 child inode，并在 create/link/unlink/symlink/rename 成功后失效或回填目录项；`make` 与单跑 `open14` 通过。详见 [problem/open14-otmpfile-path-slow.md](./problem/open14-otmpfile-path-slow.md)。

继续降低 `open14` 深层目录性能开销：`FsIndex` 与 dentry positive cache 改为可显式失效的 strong inode cache，`Ext4Inode::live_path()` 只在元数据操作失败后走 alias 恢复，并合并 `create_file()` 父目录元数据读取；`open14` 单跑从约 223.35s 降到 65.94s，summary 仍为 `passed 3 failed 0 broken 0`。详见 [problem/open14-otmpfile-path-slow.md](./problem/open14-otmpfile-path-slow.md)。

修复 LTP `tst_virt` 启动探测阶段 `/proc/cpuinfo` 缺失导致的 `TBROK`：启动期补齐 RISC-V/LoongArch64 最小 `/proc/cpuinfo` 伪文件，避免 `fopen("/proc/cpuinfo")` 返回 `ENOENT`。`make` 通过，最新 `log.ans` summary 为 `passed 7 failed 0 broken 0`。详见 [problem/proc-cpuinfo-tst-virt.md](./problem/proc-cpuinfo-tst-virt.md)。

修复 RISC-V musl LTP `epoll_create02`：对预赛镜像 `/musl/lib/libc.so` 的 `epoll_create(size)` 包装补只读兼容 patch，保留 `epoll_create1(0)` 成功语义，同时让 `size <= 0` 返回 `EINVAL`。`make TARGET_ARCH=riscv64` 通过，临时单跑 `epoll_create02` 核心断言 2 项 `TPASS`。详见 [problem/epoll-create02-riscv-musl-libc.md](./problem/epoll-create02-riscv-musl-libc.md)。

修复 iperf server 启动阶段 `setsockopt(IPPROTO_IPV6, IPV6_V6ONLY)` 返回 `ENOPROTOOPT` 导致 5001 端口未监听的问题；新增 IPv6 socket option 最小兼容。`make` 通过，使用 `/tmp` qcow2 overlay 运行 RISC-V QEMU 后 musl/glibc 两组 iperf 六项均 `success`。详见 [problem/iperf-ipv6-v6only.md](./problem/iperf-ipv6-v6only.md)。

## 7.4

修复 LoongArch64 glibc LTP `mkdir09`：`PagePrivilegeIllegal` 先进入统一缺页处理以支持 `MAP_STACK` 懒分配，同时为 stack/brk 懒分配补 VMA 权限检查；`tmpfs` 挂载前清空挂载点内容，避免简化 mount 模型下 ext2 轮次目录泄漏。`make TARGET_ARCH=loongarch64` 通过，单跑 `mkdir09` summary 为 `passed 12 failed 0 broken 0`。详见 [problem/mkdir09-loongarch-map-stack-tmpfs.md](./problem/mkdir09-loongarch-map-stack-tmpfs.md)。

## 7.6

实现 `faccessat2(439)`：复用并整理 `faccessat` 权限检查，补齐 `AT_EACCESS`、`AT_SYMLINK_NOFOLLOW`、`AT_EMPTY_PATH` flags 校验与分发，同时修正绝对路径忽略坏 `dirfd` 的语义。`make` 通过，RISC-V 单跑 musl/glibc `faccessat201`、`faccessat202` 均为 `TPASS`。

实现 `fanotify_init(262)` 的基础 fd 创建与参数校验：新增 `FanotifyFd` 文件对象，支持 `FAN_CLOEXEC/FAN_NONBLOCK` 转 fd flags，并校验 init flags 与 event fd flags。LoongArch64 `make` 通过；当前 `fanotify01` 继续停在未实现的 `fanotify_mark(263)`。

实现 `fanotify_mark(263)` 并补齐 `fanotify01` 所需基础事件队列：支持 mark add/remove/flush、ignore mask、open/access/modify/close 事件投递和 legacy metadata 读取。LoongArch64 `make` 通过，单跑 musl/glibc `fanotify01` summary 均为 `passed 156 failed 0 broken 0`。详见 [problem/fanotify01-mark-events.md](./problem/fanotify01-mark-events.md)。

收敛 syscall 模块边界：将 inotify syscall 从 `fs/mod.rs` 拆出，将 process accounting 写记录与 `CloneFlags` 下沉到 task 层，去除 task 对 syscall 根模块的反向依赖；默认 LoongArch64 `make` 通过。

新增 `os/src/syscall/fs/path.rs` 聚合路径上下文相关 syscall：迁入 `getcwd/chdir/chroot/readlinkat/faccessat/faccessat2`，并移除 `sys` 下的 chroot 子模块；默认 LoongArch64 `make` 通过。

修复 LTP `fchmod02` 启动环境前置条件：启动期 `/etc/group` 补齐 `users` 与 `daemon` 组，避免 `SAFE_GETGRNAM_FALLBACK("users", "daemon")` 找不到组而 `TBROK`。`make` 通过，最新 `log.ans` 中 musl/glibc `fchmod02` 均 `TPASS`，Summary 为 `passed 1 failed 0 broken 0`。详见 [problem/fchmod02-group-database.md](./problem/fchmod02-group-database.md)。

修复 LTP `fchmod05`：`fchmod/fchmodat` 现在统一执行 chmod 权限检查、只读挂载 `EROFS` 检查，并在非 root 且有效 gid 不匹配目标 inode gid 时静默清除 `S_ISGID`。`make` 通过，最新 `log.ans` 中 musl/glibc `fchmod05` 均 `TPASS`，Summary 为 `passed 1 failed 0 broken 0`。详见 [problem/fchmod05-chmod-setgid.md](./problem/fchmod05-chmod-setgid.md)。

修复 LTP `fanotify02` 卡死：目录 mark 现在支持 `FAN_EVENT_ON_CHILD` 直接子项匹配，并允许 `FAN_MARK_REMOVE` 单独移除 `FAN_EVENT_ON_CHILD/FAN_ONDIR` 修饰位。`make` 通过，LoongArch64 单跑 musl/glibc `fanotify02` 均 8 项 `TPASS`。详见 [problem/fanotify02-event-on-child.md](./problem/fanotify02-event-on-child.md)。

修复 LTP `chown04`：`chown/fchownat` 补齐路径长度、父目录搜索权限和只读挂载 errno 优先级处理，避免统一落到非 root `EPERM`。`make` 通过，LoongArch64 单跑 musl/glibc `chown04` 均 8 项 `TPASS`。详见 [problem/chown04-chown-errno.md](./problem/chown04-chown-errno.md)。

修复 LTP `fcntl01`：`F_GETFL` 现在返回 fd 保存的真实访问模式和文件状态标志，`F_SETFL` 会更新 `O_APPEND/O_NONBLOCK` 等可修改状态位。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl01` 均 `passed 1 failed 0 broken 0`。详见 [problem/fcntl01-f-getfl-setfl.md](./problem/fcntl01-f-getfl-setfl.md)。

继续完善 `fcntl(2)` 基础兼容性：修正无效 fd 错误码、`F_DUPFD/F_DUPFD_CLOEXEC` 的 `FD_CLOEXEC` 语义和 fd limit 错误码，并为 pipe 增加 per-pipe capacity、`F_GETPIPE_SZ/F_SETPIPE_SZ` 与 `/proc/sys/fs/pipe-max-size`。默认 LoongArch64 `make` 通过；`make run` 在根文件系统 ext4 mount 阶段失败，未进入 LTP。详见 [problem/fcntl-dupfd-pipe-size.md](./problem/fcntl-dupfd-pipe-size.md)。

修复 LTP `fcntl11`：POSIX record lock 现在使用当前进程 pid 作为 owner，同进程重叠锁会按区间覆盖拆分/合并，`F_GETLK` 返回最靠前的其它进程冲突锁。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl11` 均 `passed 1 failed 0 broken 0`。详见 [problem/fcntl11-record-lock.md](./problem/fcntl11-record-lock.md)。

修复 LTP `fcntl13`：record lock 分支现在先从用户空间复制 `struct flock`，再解析普通文件对象，避免 stdout 等非普通 fd 的 `EINVAL` 遮蔽坏用户指针应返回的 `EFAULT`。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl13` 均 `passed 4 failed 0 broken 0`。详见 [problem/fcntl13-lock-efault-priority.md](./problem/fcntl13-lock-efault-priority.md)。

修复 LTP `fcntl14`：POSIX record lock 支持 `SEEK_CUR` 当前偏移、负 `l_len` 反向区间、非法 `l_whence` 返回 `EINVAL`，并补齐 `F_SETLKW` 阻塞等待、死锁检测和 close/exit 释放锁。RISC-V 与默认 LoongArch64 构建通过，维护者确认 `fcntl14` 已通过。详见 [problem/fcntl14-record-lock-seekcur-len.md](./problem/fcntl14-record-lock-seekcur-len.md)。

修复 LTP `fcntl23`：`F_SETLEASE/F_GETLEASE` 不再固定返回 `EAGAIN/F_UNLCK`，新增按路径和 pid 管理的最小文件租约表，并在 close/exit 时清理租约。`make` 通过，LoongArch64 单跑 musl/glibc `fcntl23` 均 `TPASS`。详见 [problem/fcntl23-file-lease.md](./problem/fcntl23-file-lease.md)。

修复 LTP `fcntl31`：补齐 `F_GETOWN/F_SETOWN/F_GETOWN_EX/F_SETOWN_EX/F_GETSIG/F_SETSIG` 的最小 async I/O owner 状态，并让 pipe 写入时向 TID/PID/PGRP owner 投递配置的信号。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl31` 均 5 项 `TPASS`。详见 [problem/fcntl31-fasync-owner-signal.md](./problem/fcntl31-fasync-owner-signal.md)。

重构 `file_lock` 模块：将 `struct flock` ABI、POSIX record lock、file lease 和 BSD `flock(2)` 拆分为高内聚子模块，对外保持 `file_lock::...` 接口不变；LoongArch64 与 RISC-V 构建均通过。

完善 session ID 语义：`ProcessMeta` 新增独立 `sid` 字段，`fork/clone` 继承调用者的 `pgid/sid`，`getsid/setsid/setpgid` 不再混用 `pgid` 表示会话；LoongArch64 与 RISC-V 构建均通过。

收敛 `fcntl(2)` syscall 代码位置：将 `sys_fcntl`、record lock 阻塞 helper 和 `struct f_owner_ex` 编解码从 `fd_ops.rs` 迁入 `fcntl.rs`，让 `fd_ops.rs` 回到通用 fd/open/close 操作；默认 LoongArch64 `make` 通过。

修复 LTP `fcntl33`：补齐 `/proc/sys/fs/lease-break-time`，为冲突 `open/truncate` 增加 file lease break `SIGIO` 通知，并修正写 lease 降级与 `truncate()` 相对路径语义。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl33` 均 7 项 `TPASS`，Summary 为 `passed 7 failed 0 broken 0`。详见 [problem/fcntl33-lease-break-sigio.md](./problem/fcntl33-lease-break-sigio.md)。

修复 LTP `fcntl34`：OFD lock 改用 open file description 级 owner，`F_OFD_SETLKW` 接入阻塞等待，并在最后一个 fd 关闭时释放对应 OFD 锁。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl34` 均 `TPASS`，Summary 为 `passed 1 failed 0 broken 0`。详见 [problem/fcntl34-ofd-lock-owner.md](./problem/fcntl34-ofd-lock-owner.md)。

修复 LTP `fcntl35`：`/proc/sys/fs/pipe-max-size` 写入现在会同步到 pipe 子系统，非 `CAP_SYS_RESOURCE` 任务新建 pipe 时初始容量受该上限约束，特权任务保持默认 `65536`。LoongArch64 `make` 通过，单跑 musl/glibc `fcntl35` 均为 `passed 2 failed 0 broken 0`。详见 [problem/fcntl35-pipe-max-size-init.md](./problem/fcntl35-pipe-max-size-init.md)。

## 7.7

修复 sys_setresuid 语义的bug，setresuid_allowed函数体内判断各个uid各自管各自的逻辑。

修复 LTP `kill05`：`kill(2)` 对用户发信号增加 real/effective/saved uid 权限检查，并修正 `pid == 0`、`pid < -1` 的进程组投递语义。`make` 通过，最新 `log.ans` 中 musl/glibc `kill05` 均 `TPASS`，Summary 为 `passed 1 failed 0 broken 0`。详见 [problem/kill05-kill-permission.md](./problem/kill05-kill-permission.md)。

重构 `os/src/signal` 模块：将 signal frame、pending/default action、信号投递/`kill(2)` 权限和 itimer/pselect 信号拆成独立子模块，`mod.rs` 收敛为门面导出；默认 LoongArch64 `make` 通过。

实现 `shmdt(197)` 基础语义：接入 syscall 分发，按页对齐和当前进程 shm attach 起始地址校验，成功时解除当前地址空间中的 SysV shm 映射并刷新 TLB。默认 LoongArch64 `make` 通过。

重构 `os/src/mm/memory_set`：将 `MemorySetInner` 类型定义、锁封装、基础 area 操作、页表/统计/回收 helper 从 `mod.rs` 拆入独立子模块，`mod.rs` 收敛为门面导出并补充模块职责文档；默认 LoongArch64 `make` 通过。

修复 LTP `kill10`：pending signal 现在保存用户态 `kill/tkill/tgkill` 的发送者 `siginfo_t`，`SA_SIGINFO` handler 可读取正确 `si_pid`，避免 signal flooding 测试误判来源 pid 后循环刷屏。LoongArch64 单跑 musl/glibc `kill10` 均 `TPASS`，LoongArch64 与 RISC-V 构建通过。详见 [problem/kill10-siginfo-sender.md](./problem/kill10-siginfo-sender.md)。

修复 LTP `kill12`：显式 `SIG_IGN` 的 pending signal 不再按默认终止动作处理，并移除投递阶段过早写入 `termination_signal` 的逻辑，避免被忽略信号污染 `waitpid()` status。LoongArch64 单跑 musl/glibc `kill12` 均 `TPASS`，LoongArch64 与 RISC-V 构建通过。详见 [problem/kill12-sigign-wait-status.md](./problem/kill12-sigign-wait-status.md)。

修复 LTP `link04`：`linkat` 补齐空路径、超长路径、路径分量长度和父目录权限错误码处理。`make` 通过，LoongArch64 单跑 musl/glibc `link04` 均为 `passed 14 failed 0 broken 0`。详见 [problem/link04-linkat-errno-permission.md](./problem/link04-linkat-errno-permission.md)。

修复 LTP `link08`：`linkat` 补齐跨挂载点 `EXDEV`、只读挂载 `EROFS`，并避免符号链接环被路径长度预检错误遮蔽为 `ENAMETOOLONG`。`make` 通过，LoongArch64 单跑 musl/glibc `link08` 均为 `passed 4 failed 0 broken 0`。详见 [problem/link08-linkat-mount-rofs-eloop.md](./problem/link08-linkat-mount-rofs-eloop.md)。

修复 LTP `linkat02` 卡死：为 hard link 增加有限 `EMLINK` 上限，并让 `unlinkat` 删除最终 symlink 本身，避免 cleanup 阶段跟随 symlink 环产生 warning。`make` 通过，LoongArch64 单跑 musl/glibc `linkat02` 均为 `passed 7 failed 0 broken 0`。详见 [problem/linkat02-hardlink-emlink-unlink-symlink.md](./problem/linkat02-hardlink-emlink-unlink-symlink.md)。

修复 LTP `mkdir02`：目录创建时若父目录带 `S_ISGID`，新目录现在继承该 mode 位并继续继承父目录 gid。`make` 通过，LoongArch64 `make run` 中 musl/glibc `mkdir02` 均 `TPASS`。详见 [problem/mkdir02-setgid-inherit.md](./problem/mkdir02-setgid-inherit.md)。

## 7.12

修复 LTP `mmap04`：`/proc/<pid>/maps` 在打开时按当前 VMA 刷新，使用 Linux 兼容的无前导零地址格式并正确显示共享映射 `s`；`MAP_FIXED` 拆分 VMA 时同步更新共享/私有标志。LoongArch64 单跑 musl/glibc 均为 `passed 14 failed 0 broken 0`，LoongArch64 与 RISC-V 构建通过。详见 [problem/proc-self-maps-mmap04.md](./problem/proc-self-maps-mmap04.md)。

补齐 `/proc/<pid>/pagemap` 与 `/proc/self/pagemap`：按当前页表刷新 Linux pagemap 二进制条目，并以稀疏文件保存未映射页的零值；initproc 创建和进程退出清理同步覆盖该文件。默认 LoongArch64 `make` 通过，最新 `log.ans` 中 musl/glibc `mmap12` 均为 `passed 1 failed 0 broken 0`。详见 [problem/proc-self-pagemap-mmap12.md](./problem/proc-self-pagemap-mmap12.md)。

修复 LTP `mmap13`：文件映射在完整 EOF 外页缺页时现在投递 `SIGBUS`，VMA 保留 mmap 时的文件长度；同时修正 ext4 截断扩展后的长度一致性，避免 LTP 框架 unlink 的共享页被误判。LoongArch64 musl/glibc 均为 `passed 1 failed 0 broken 0`。详见 [problem/mmap13-sigbus-eof.md](./problem/mmap13-sigbus-eof.md)。

## 7.13

修复 LoongArch PCI VirtIO-net 启动期内存破坏：早期每 hart 4 KiB 栈无法容纳网卡初始化的 Rust 调用链，覆盖 `.data` 中的 UART/ext4 cache；同时修正 PCI ECAM 非对齐非 volatile 访问及 VirtQueue DMA 未清零。LoongArch64 完整启动至用户态 `shutdown!`，RISC-V 构建和启动回归均未见 panic。详见 [problem/loongarch-pci-virtio-net-bootstrap-corruption.md](./problem/loongarch-pci-virtio-net-bootstrap-corruption.md)。

为 VirtIO-net 增加用户态分层回归：UDP loopback 作为协议栈基线，SLIRP DNS 单次往返和 160 次连续往返验证真实 `eth0` TX/RX 与描述符回收，非阻塞超时用例验证无响应路径不会卡死。LoongArch64 与 RISC-V QEMU 均为 `passed 4 failed 0`。详见 [problem/loongarch-pci-virtio-net-bootstrap-corruption.md](./problem/loongarch-pci-virtio-net-bootstrap-corruption.md)。

修复 LoongArch64 启动期 fork 停滞：`/proc/<pid>/pagemap` 不再在进程创建时对 ext4 截断 384 MiB，改为按需生成的动态只读文件。`glibc/basic_testcode.sh` 完整结束，glibc `mmap12` 仍为 `passed 1 failed 0 broken 0`；LoongArch64 与 RISC-V 构建通过。详见 [problem/proc-pagemap-fork-allocation.md](./problem/proc-pagemap-fork-allocation.md)。

修复 LoongArch64 `cyclictest` 的内核堆 OOM：AF_UNIX 接收队列按 64KiB 上限实施背压，龙芯 QEMU RAM 升至 2GiB 并按真实两段地址纳入同一 CMA。LoongArch64 musl/glibc cyclictest 八个阶段和两个 hackbench 清理均成功，无 panic。详见 [problem/loongarch-unix-queue-oom.md](./problem/loongarch-unix-queue-oom.md)。

RISC-V QEMU RAM 同步为连续 2GiB：CMA 在 bootstrap 1GiB 映射内初始化，完整 Sv39 直接映射启用后再纳入第二个 GiB；RISC-V 2GiB 启动完成 `remap_test`、initproc、busybox/Lua 两组和 iperf-musl 六项，无内存分配 panic。详见 [problem/loongarch-unix-queue-oom.md](./problem/loongarch-unix-queue-oom.md)。

修复 LTP `signal03` 卡死：pending 信号分发对显式 `SIG_IGN` 不再把默认 stop 信号例外处理，`SIGTSTP`/`SIGTTIN`/`SIGTTOU` 现会被正确忽略。LoongArch64 单跑 musl/glibc 均完成，Summary 分别为 `passed 31`、`passed 30`，均无 failed/broken。详见 [problem/signal03-sigign-stop.md](./problem/signal03-sigign-stop.md)。

## 7.14

将内核设计文档从 Markdown 迁移为以 `Docs/ya2yos/main.typ` 为入口的 Typst 工程，补齐十章内容、参考文献和参赛人员信息；同步调整章节编号、目录结构，并以 Typst 原生图替换原有 UML 源文件和图片。

新增 `make doc`，仅为内核 crate 生成 Rustdoc（不生成依赖文档）；增加 `scripts/extract_failed_tests.py`，可从测试日志中识别 libctest 与 LTP 的实际失败项，并忽略固定打印的 `FAIL LTP CASE ... : 0` 包装行。

修正 `sys_read` 对不可读 fd 的 Linux 错误码语义：由 `EACCES` 改为 `EBADF`。

修复 LTP `splice07` 的匿名挂载 fd 卡死：`fsopen`/`fspick` 与 `open_tree` 返回的上下文 fd 不再沿用 `File` 的默认可读写能力，`splice` 现在在空 pipe 读之前返回 `EBADF`。RISC-V `make` 与 LoongArch64 `make` 均通过；最新 `log.ans` 中 musl/glibc 均为 `passed 566 failed 0 broken 0`，并正常 `shutdown!`。详见 [problem/splice07-mount-context-fd-block.md](./problem/splice07-mount-context-fd-block.md)。

修复 LTP `pipe2_01`：syscall 分发不再丢弃 `pipe2(flags)` 参数，`O_CLOEXEC`、`O_NONBLOCK` 和 `O_DIRECT` 现在按 fd/端点语义保存。RISC-V musl/glibc 均为 `passed 7 failed 0 broken 0` 并正常关机。详见 [problem/pipe2-flags-propagation.md](./problem/pipe2-flags-propagation.md)。

修复 LTP `open02`：打开既有 inode 时，`O_NOATIME` 现在要求 caller 是文件 owner 或持有 `CAP_FOWNER`，否则返回 `EPERM`。LoongArch64 musl/glibc 均为 `passed 2 failed 0 broken 0` 并正常关机。详见 [problem/open02-noatime-permission.md](./problem/open02-noatime-permission.md)。

修复 LTP `open11` 目录 flags 语义：既有目录的写 access mode、非 `O_PATH` 的 `O_TRUNC` 或 `O_CREAT` 现在返回 `EISDIR`，并保持 `O_PATH` 不触发创建、截断或 `O_NOATIME` 检查。RISC-V 与 LoongArch64 构建通过；QEMU 运行分别受 `IllegalInstruction` 和沙箱 `/var/tmp` 只读限制，尚未取得有效 LTP 回归结果。详见 [problem/open11-directory-open-flags.md](./problem/open11-directory-open-flags.md)。

## 7.15

修复 LTP `utimes01`：补齐 NULL pathname 的 `EFAULT`、非 owner 时间戳更新的 `EACCES`/`EPERM` 以及只读挂载 `EROFS` 语义。RISC-V/LoongArch64 单测中 musl/glibc 均通过；详见 [problem/utimes01-permission-rofs.md](./problem/utimes01-permission-rofs.md)。

修复 LoongArch64 全量 glibc LTP `kill10` 的 signal frame EFAULT panic：完整预检用户信号帧范围，保证 handler 栈 16 字节对齐，失败时仅终止当前任务为 `SIGSEGV`，并移除 `TaskControlBlockInner` 锁跨用户内存访问。RISC-V/LoongArch64 构建通过，LoongArch64 单跑 musl/glibc `kill10` 均为 `passed 1 failed 0 broken 0`；全量 LTP 尚待恢复全量入口后复跑。详见 [problem/kill10-signal-frame-efault-panic.md](./problem/kill10-signal-frame-efault-panic.md)。

修复 LTP `openat201`：`openat2` 现在接受基础 resolve 标志，并补齐 `open_how` 扩展尾部、flags/mode、pathname 和 dirfd 的 ABI 校验；RISC-V musl/glibc 均为 `passed 16 failed 0 broken 0`。详见 [problem/openat2-open-how-resolve.md](./problem/openat2-open-how-resolve.md)。

修复 LTP `socket01` 的 socket type errno：非法 type 现在返回 `EINVAL`，未实现的 IPv4/IPv6 `SOCK_RAW` 返回 `EPROTONOSUPPORT`；最新 `log.ans` 中 musl/glibc 均为 `passed 9 failed 0 broken 0` 并正常关机。详见 [problem/socket01-socket-type-errno.md](./problem/socket01-socket-type-errno.md)。

修复 LTP `socketpair01`：AF_INET/AF_INET6 的 socketpair errno 现在区分 `EINVAL`、`EOPNOTSUPP` 与 `EPROTONOSUPPORT`；RISC-V user-copy 也会在缺页前校验用户 VMA，坏指针返回 `EFAULT`。RISC-V musl/glibc 均为 `passed 10 failed 0 broken 0`。详见 [problem/socketpair01-protocol-errno.md](./problem/socketpair01-protocol-errno.md)。

修复 signal 默认 disposition ABI：默认 action 不再复用 `SIG_IGN` 或内核函数地址，显式 `SIG_IGN`、默认动作和用户 handler 现由独立 disposition 表示。LoongArch64 与 RISC-V 的 musl/glibc `waitid07` 均为 `passed 5 failed 0 broken 0`。详见 [problem/signal-disposition-default-abi.md](./problem/signal-disposition-default-abi.md)。

修复 LTP `clone02`：为 `CLONE_FILES`/`CLONE_FS` 共享的 fd table 和 FS context 建立独立活跃进程所有者计数，避免退出子进程清空父进程资源，也避免 zombie `Arc` 阻止最后一个 pipe 端关闭。RISC-V 与 LoongArch64 的 musl/glibc 均为 `passed 2 failed 0 broken 0` 并正常关机。详见 [problem/clone02-shared-resource-exit.md](./problem/clone02-shared-resource-exit.md)。

修复 legacy `clone(2)` 的 `CLONE_THREAD` 低位退出信号兼容：RISC-V musl/glibc 和 LoongArch64 glibc `clone08` 均为 `passed 5 failed 0 broken 0`；LoongArch64 musl 剩余失败确认由测试镜像 libc 在 syscall 前拒绝线程 flags，未修改镜像。详见 [problem/clone08-legacy-clone-thread-signal.md](./problem/clone08-legacy-clone-thread-signal.md)。

修复 LTP `getcwd03`：`chdir` 保存解析后的目录路径，`readlinkat` 与内部 unlink 查找保留末级符号链接并绕过已跟随链接的缓存。LoongArch64 与 RISC-V 的 musl/glibc 均为 `passed 1 failed 0 broken 0`。详见 [problem/getcwd03-symlink-cwd-readlink-cache.md](./problem/getcwd03-symlink-cwd-readlink-cache.md)。

## 7.16

修复 LTP `vmsplice02`：对有效但非 pipe 的 fd 调用 `vmsplice` 现在返回 `EBADF`，不再错误返回 `EINVAL`。RISC-V 单测中 musl/glibc 均通过；详见 [problem/vmsplice02-non-pipe-fd.md](./problem/vmsplice02-non-pipe-fd.md)。

修复 LTP `waitpid04`：`waitpid/wait4` 现在严格拒绝未知 options 位，非法 `0xffffffff` 返回 `EINVAL` 而不是在无 child 时误返 `ECHILD`。RISC-V 单测中 musl/glibc 均通过；详见 [problem/waitpid04-invalid-options.md](./problem/waitpid04-invalid-options.md)。

修复 LTP `writev01` 的 `writev(2)` 错误码和空 iovec 语义：无效 fd 返回 `EBADF`，空 iovec 成功返回 0，零长度 NULL iovec 不再触发用户地址拷贝，关闭 pipe 读端可返回 `EPIPE`。双架构构建通过；详见 [problem/writev01-writev-errno.md](./problem/writev01-writev-errno.md)。

修复 LTP `unlink07`：`unlinkat` 在 pathname 归一化前拒绝空路径并检查总长度及分量长度，分别返回 `ENOENT` 与 `ENAMETOOLONG`。双架构构建通过，LoongArch64 musl/glibc 均为 `passed 6 failed 0 broken 0`；详见 [problem/unlink07-path-errno.md](./problem/unlink07-path-errno.md)。

修复 LTP `readv01`：合法 fd 上的零 iovec 现在成功返回 0；`readv` 同步收敛 fd、长度、目录和用户输出缓冲区校验。双架构构建通过，LoongArch64 musl/glibc 均为 `passed 10 failed 0 broken 0`；详见 [problem/readv01-iovec-semantics.md](./problem/readv01-iovec-semantics.md)。

修复 LTP `open14`：在普通源路径校验前识别 `/proc/self/fd/<fd>` magic-link，并让 lwext4 小文件缓存物化时保存内容和 mode、避免 `fstat` panic。RISC-V musl/glibc 均为 `passed 3 failed 0 broken 0`，无内核 error；详见 [problem/open14-procfd-linkat-cache.md](./problem/open14-procfd-linkat-cache.md)。

## 7.17

修复 LTP `fs_bind17` 至 `fs_bind21` 的 shared peer 与多级 slave 挂载传播，并消除 `fs_bind22` 同树 bind 的递归镜像内核 panic。RISC-V 回归中 17 至 21 均为 `failed 0`；同树 bind 的真实 mount-root 视图仍待 VFS 层实现。详见 [problem/fs-bind-peer-slave-propagation.md](./problem/fs-bind-peer-slave-propagation.md)。

修复 LTP `kill02`：pipe 阻塞读写与 readiness wait 现在会消费默认或显式忽略的 pending signal，避免默认忽略的 `SIGCHLD` 被错误返回为 `EINTR` 并提前关闭 LTP 输出读端。RISC-V 与 LoongArch64 的 musl/glibc `kill02` 均为 `passed 2 failed 0 broken 0`。详见 [problem/kill02-ignored-sigchld-pipe-eintr.md](./problem/kill02-ignored-sigchld-pipe-eintr.md)。

修复 LTP `linkat01`：相对路径的 `dirfd` 现在强制为目录并正确返回 `ENOTDIR`，`/proc` compatibility namespace 与普通路径间返回 `EXDEV`，未知 flags 返回 `EINVAL`。RISC-V 与 LoongArch64 的 musl/glibc 均为 `passed 22 failed 0 broken 0`。详见 [problem/linkat01-dirfd-procfs-flags.md](./problem/linkat01-dirfd-procfs-flags.md)。

重构 `os/src/syscall/fs/ctl/mod.rs`：拆为目录项、链接、命名空间、元数据、时间、ioctl 与共享 helper 八个子模块，门面保留原有 `fs::ctl::*` 接口。双架构构建通过，RISC-V `linkat01` musl/glibc 均为 `passed 22 failed 0 broken 0`。

修复 LTP `mmap08`：非匿名文件映射的无效 fd 现在在零长度参数校验前返回 `EBADF`，与 Linux 错误优先级一致。RISC-V musl/glibc 单跑均 `TPASS`；详见 [problem/mmap08-fd-errno-priority.md](./problem/mmap08-fd-errno-priority.md)。

修复 LTP `chdir01`：`chdir(2)` 现在按 effective uid/gid 验证已解析路径中各目录的 search 权限，非 root 无法进入 `0644` 目录。RISC-V musl/glibc 在 ext2、tmpfs 均为 `passed 32 failed 0 broken 0`；详见 [problem/chdir01-search-permission.md](./problem/chdir01-search-permission.md)。

修复 LTP `chdir04`：长度恰为 `MAX_PATH_LEN` 且未含 NUL 的 pathname 现在返回 `ENAMETOOLONG`，不再错误落入文件查找并返回 `ENOENT`。RISC-V musl/glibc 单跑均 `TPASS`；详见 [problem/chdir04-path-length-boundary.md](./problem/chdir04-path-length-boundary.md)。

修复 LTP `tcp4-multi-diffnic01`：启动期补齐 BusyBox `wc` applet，并为当前单节点、无独立网卡对的环境添加明确的空 alias-pair 兼容 wrapper。RISC-V musl/glibc 单跑均为 `passed 1 failed 0 broken 0`；详见 [problem/tcp4-multi-diffnic01-single-node-env.md](./problem/tcp4-multi-diffnic01-single-node-env.md)。

修复 LTP `fs_bind_rbind01`：启动期补齐 BusyBox `seq`、`diff` applet，并为路径化挂载表实现 bind 层、递归 shared propagation 和同事件卸载；RISC-V musl/glibc 均为 `passed 28 failed 0 broken 0`。详见 [problem/fs-bind-rbind-propagation.md](./problem/fs-bind-rbind-propagation.md)。

修复 LTP `fs_bind13`：挂载层现保存 unbindable 状态，bind clone unbindable source 返回 `EINVAL` 且不产生 peer 副本；RISC-V musl/glibc 均为 `passed 24 failed 0 broken 0`。详见 [problem/fs-bind13-unbindable-source.md](./problem/fs-bind13-unbindable-source.md)。

修复 LTP `fs_bind23`：`MS_MOVE` 现在重定位 source mount subtree、向目标 shared peer 复制同 event group，并同步路径化 VFS 目录视图。RISC-V musl/glibc 均为 `passed 20 failed 0 broken 0`。详见 [problem/fs-bind23-move-propagation.md](./problem/fs-bind23-move-propagation.md)。

修复 LTP `fs_bind24`：子目录 bind 现从覆盖 source 的顶层 mount 继承状态，shared-slave 转换保留上游 master，并在传播时保留 source 子目录偏移。RISC-V musl/glibc 均为 `passed 15 failed 0 broken 0`。详见 [problem/fs-bind24-subtree-shared-slave-propagation.md](./problem/fs-bind24-subtree-shared-slave-propagation.md)。

修复 LTP `fs_bind_move05`：private subtree 移入 shared parent 时，移动根现继承接收端 propagation state，后续事件通过同一 move event 的 peer root 保留子路径偏移。RISC-V musl/glibc 均为 `passed 27 failed 0 broken 0`。详见 [problem/fs-bind-move05-private-shared-propagation.md](./problem/fs-bind-move05-private-shared-propagation.md)。

## 7.18

完成 RISC-V QEMU 双 hart 首版 SMP bring-up：SBI 启动 AP、进程固定 hart 调度、共享任务/文件系统/异步定时状态加锁；RISC-V 双核和 LoongArch64 单核的 musl/glibc `iperf` 均完成并正常关机。详见 [problem/riscv-smp-bringup.md](./problem/riscv-smp-bringup.md)。

修复双 hart `netperf` 偶发卡死：统一 smoltcp 全局锁序，闭合 `block_on`/`AtomicWaker` 的跨核丢唤醒窗口，并按 `home_hart` 驱动 blocked timer。双架构构建通过，RISC-V musl/glibc netperf 连续两次共 10 项全成功并正常关机。详见 [problem/riscv-smp-netperf-wakeup-locking.md](./problem/riscv-smp-netperf-wakeup-locking.md)。

实现编译期可切换的 CFS/RR 调度器，默认 CFS 按 nice 加权 `vruntime` 从 per-Hart heap 选择任务，`make SCHEDULER=rr` 保留 FIFO RR。双策略、双架构构建通过；RISC-V 的 CFS/RR 及 LoongArch64 的 CFS 下，musl/glibc cyclictest 各 8 个阶段均成功，两轮 400-task hackbench 完成预期清理并正常关机。

## 7.19

修复 RISC-V 双 hart `basic_testcode.sh/test_yield` 卡死：父进程继续 fork 与首个子进程退出时不再以相反顺序获取 `ProcessMeta` 和 `TaskControlBlockInner`。双架构构建与运行回归通过，RISC-V release+CFS 多轮均完成 musl/glibc 各 15 条输出并正常 `shutdown!`。详见 [problem/basic-test-yield-fork-exit-deadlock.md](./problem/basic-test-yield-fork-exit-deadlock.md)。

修复 BusyBox musl/glibc 回归的 fork/exec/exit 并发卡死：futex 仅在 `Blocked -> Ready` 时入队，CFS/RR 丢弃陈旧就绪项，并在 exec/退出回收前切换有效页表。当前 RISC-V 日志两组均到达 `GROUP END` 并 `shutdown!`；其他 BusyBox 失败项仍待单独处理。详见 [problem/busybox-fork-exec-exit-smp-hang.md](./problem/busybox-fork-exec-exit-smp-hang.md)。

修复 RISC-V 双 hart CFS `UDP_RR` 首 burst 卡死：CFS 在陈旧 entry 出队时不再丢失并发网络唤醒，smoltcp 的微秒 deadline 也恢复为正确内核时间。双架构构建通过，独立 musl `UDP_RR` 多次完成并 `shutdown!`；详见 [problem/riscv-cfs-netperf-udp-rr-hang.md](./problem/riscv-cfs-netperf-udp-rr-hang.md)。

修复 RISC-V 双 hart libctest 批量运行的随机段错误：COW 写保护复制在解除旧 VMA 映射前固定源 `FrameTracker`，避免另一 hart 并发回收并复用源物理页。最终 `log.ans` 完整运行 217 项、输出 `GROUP END` 和 `shutdown!`，无段错误；`utime` 两个既有失败未纳入本次修复。详见 [problem/riscv-libctest-cow-source-frame-race.md](./problem/riscv-libctest-cow-source-frame-race.md)。

参考 Linux 7.0 `wp_page_copy()` 继续加固 RISC-V COW：新页现先分配和复制，成功后才替换 PTE/旧 frame；`copy_to_user` 也会正确触发 present COW 页分裂。同步修复 Brk shrink 遗留 PTE 与 lazy stack clone 的临时页表所有权；双架构构建通过，RISC-V libctest 217 项完整结束。详见 [problem/riscv-libctest-cow-source-frame-race.md](./problem/riscv-libctest-cow-source-frame-race.md)。

修复 libc-test `utime` 的 `futimens` ABI：`utimensat` 现仅将有效 fd 加 NULL pathname 识别为 fd 目标，保留 `utimes(NULL)` 的 `EFAULT`。RISC-V 与 LoongArch64 定向测例均输出 `Pass!` 并正常 `shutdown!`；详见 [problem/libctest-futimens-fd-null-pathname.md](./problem/libctest-futimens-fd-null-pathname.md)。

## 7.20

修复 iozone 连续 random-read/backward-read 卡死：FsIndex 不再遗留可被 inode 号复用命中的 canonical inode，lwext4 write-back FIFO 同步清理，并绕过短生命周期 task proc 文件缓存。RISC-V 与 LoongArch64 两项 iozone 测例均完整结束并 `shutdown!`；详见 [problem/iozone-inode-cache-reuse-hang.md](./problem/iozone-inode-cache-reuse-hang.md)。

修复 BusyBox `hwclock` 和目录 `mv/rmdir`：`renameat2` 不再以写方式打开目录源路径，`DevRtc` 实现 `RTC_RD_TIME`。RISC-V musl/glibc 最小回归三项均返回 0 并 `shutdown!`，LoongArch64 编译通过；详见 [problem/busybox-hwclock-rename.md](./problem/busybox-hwclock-rename.md)。

修复 lmbench musl 后 glibc 卡死：ext4 已存在目录创建失败时不再在持锁状态析构临时 inode 并重入同一自旋锁。RISC-V 24 项 musl 单项矩阵、真实 musl+glibc 组合均完整结束，LoongArch64 编译通过；详见 [problem/lmbench-ext4-eexist-drop-self-deadlock.md](./problem/lmbench-ext4-eexist-drop-self-deadlock.md)。

修复 RISC-V 双 hart LTP `kill10` 信号路径锁序风险：信号投递和 handler 现在均先快照 `SigTable` action，再进入 `TaskControlBlockInner` 更新 pending 状态，消除反向嵌套。RISC-V musl/glibc `kill10` 均为 `passed 1 failed 0 broken 0` 并正常 `shutdown!`；详见 [problem/kill10-signal-lock-order.md](./problem/kill10-signal-lock-order.md)。

修复 RISC-V release+CFS 的 LTP `kill10` `pause()` 卡死：无 fd `ppoll` 现在原子发布 Blocked 等待，ITIMER 能唤醒所有 blocked task，并补齐 raw ppoll signal-mask ABI。最新 `log.ans` 中 musl `kill08/kill10` 均通过并正常 `shutdown!`；详见 [problem/kill10-ppoll-itimer-blocked-wakeup.md](./problem/kill10-ppoll-itimer-blocked-wakeup.md)。

修复 RISC-V LTP `mmap001` 卡死：硬件 PTE 构造会将逻辑 `PROT_WRITE` 规范化为 `R|W`，避免生成 `R=0,W=1` 的保留编码；`mmap001` 现为 `passed 4 failed 0 broken 0` 并正常关机。详见 [problem/mmap001-riscv-write-only-pte.md](./problem/mmap001-riscv-write-only-pte.md)。

修复 BuildStorm Debian 根文件系统启动前置条件：目录初始化不再在已存在目录上误报 `EISDIR` 并漏建 DevFS，`/bin -> /usr/bin` 不再被 BusyBox wrapper 覆盖；补齐 `sigaltstack(2)`、`SA_ONSTACK` 信号帧和返回恢复。RISC-V、LoongArch64 release 构建通过，详见新增 problem/ 复盘。

修复 RISC-V 双 hart 启动栈越界和 BuildStorm 多线程 `execve`：每 hart bootstrap stack 提升至 128 KiB，exec 在替换共享地址空间前终止并等待 sibling 退出。完整 BuildStorm QEMU 回归待续跑，详见 [problem/riscv-buildstorm-bootstrap-stack-overflow.md](./problem/riscv-buildstorm-bootstrap-stack-overflow.md) 和 [problem/buildstorm-multithreaded-execve-dethread.md](./problem/buildstorm-multithreaded-execve-dethread.md)。

实现 Linux asm-generic `fadvise64(223)`：补齐 syscall 分发以及 fd、FIFO、长度和 advice 的 Linux errno 校验，合法 hint 当前作为无状态建议成功返回。RISC-V 与 LoongArch64 release 构建通过；按维护者要求未运行 QEMU/LTP，相关黑名单保持不变。

修复 BuildStorm 动态库打开路径的 `..` 规范化缺失：`get_abs_path()` 现对绝对路径与相对路径一致折叠 `.`/`..`，使 Rust 工具链 DSO 能匹配既有镜像路径兼容表。RISC-V 与 LoongArch64 release 构建通过，未运行 QEMU 回归。

## 7.21

扩展 LoongArch 评测配置至 8GiB/8 核：内核现按 QEMU 分段 RAM 纳管 CMA，并通过 mailbox/IPI 启动 7 个 AP；`getcpu`、affinity 和 `/proc/cpuinfo` 同步拓扑。LoongArch64 release 构建及真实 QEMU 基础回归均完成并正常关机；详见 [problem/loongarch-8g-8hart-bootstrap.md](./problem/loongarch-8g-8hart-bootstrap.md)。

修复 final-2026 Debian 动态库错误回退、ext4 `getdents64.d_type` ABI 和通用 `FIONBIO` 语义；RISC-V `log.ans` 已出现 `BUILDSTORM_TOOLCHAIN ok`，详见新增 problem/ 复盘。

RISC-V、LoongArch64 release 构建通过；本机仅 `2G / 2 CPU`，guest 内 minibuild 编译在 300 秒窗口仍持续满载，未将完整 BuildStorm 编译或性能项标记为通过。
实现 Linux asm-generic `rseq(293)`：补齐 syscall 分发、经典 32-byte ABI 的线程级注册状态和用户态入口，并加入基础回归探针。RISC-V `log.ans` 已输出 `rseq regression: PASS`；未运行 LTP/Linux rseq selftest 或完整 critical-section 语义验收，详见 [problem/rseq-syscall.md](./problem/rseq-syscall.md)。

接入 RISC-V `riscv_hwprobe(258)` 的最小桩：仅登记和分发，固定返回 `ENOSYS`，不会向用户态伪造硬件能力数据。RISC-V 与 LoongArch64 release 构建通过；未运行 QEMU 行为回归。

修复评测机 `8G / 8 CPU` RISC-V 启动：本地 allocator 现可表示 8GiB CMA 的高阶块，汇编 bootstrap stack 数同步为 8。双架构 release 构建通过，默认配置 QEMU 已启动全部 secondary hart 并进入 `BUILDSTORM_TOOLCHAIN ok`；详见 [problem/riscv-8g-8hart-bootstrap.md](./problem/riscv-8g-8hart-bootstrap.md)。

修复评测机 RISC-V QEMU 自动分配 VirtIO 网卡后启动日志报无设备：内核将网卡 MMIO 基址和映射改为 `virtio-mmio-bus.7` 的 `0x10008000`，无需改动评测机 QEMU 参数。RISC-V 启动已正常初始化网络；详见 [problem/riscv-virtio-net-mmio-autobus.md](./problem/riscv-virtio-net-mmio-autobus.md)。

记录 BuildStorm `BUILDSTORM_TOOLCHAIN ok` 后 minibuild 超时：尝试减少 lwext4 大文件的重复小文件缓存准入并复用只读 private DSO 页；RISC-V release 构建通过，但 10 分钟 QEMU 运行未到达 `BUILDSTORM_MINIBUILD ok`。复核还发现两项 mmap/cache 语义风险，源码暂不提交，详见 [problem/buildstorm-minibuild-post-toolchain-stall.md](./problem/buildstorm-minibuild-post-toolchain-stall.md)。

拆分 BuildStorm 诊断脚本为工具链、MINIBUILD prepare/build、`tg-xtask` 预构建和正式编译五项，并避免向评分日志输出正式 `BUILDSTORM_*` 标记。RISC-V、LoongArch64 release 构建通过；RISC-V 独立运行确认 prepare 完成后进入原始静默 `cargo build`，尚未得到 MINIBUILD 完成或根因结论；详见同一 problem 复盘。

修复 BuildStorm MINIBUILD Rustc 的 mmap 虚拟地址预算不足：两架构累计 mmap 上限从 512 MiB 提升到 2 GiB，已消除日志中的首个 `ENOMEM`。强制新建项目后进一步暴露 Cargo worker 普通 fork 的动态栈 `EFAULT`，完整 MINIBUILD 仍待修复；详见 [problem/buildstorm-minibuild-post-toolchain-stall.md](./problem/buildstorm-minibuild-post-toolchain-stall.md)。

修复 BuildStorm `clone3/vfork` 共享地址空间交接：vfork 父 task 现立即让出 CPU，exec 在新地址空间与 trap context 安装后才唤醒父 task，非线程 `CLONE_VM` 子进程继承父 hart，且两架构统一 clone3 stack ABI。RISC-V、LoongArch64 release 构建通过；完整 MINIBUILD 的动态栈 fork `EFAULT` 仍待单独修复。详见 [problem/buildstorm-vfork-clone3-lifecycle.md](./problem/buildstorm-vfork-clone3-lifecycle.md)。

修复 lwext4 大文件 mmap 触发的小文件缓存准入重复探测：超限文件只探测一次，`initialize cache!` 仅记录真实缓存成功；RISC-V MINIBUILD 完成并正常关机，详见 [problem/buildstorm-minibuild-post-toolchain-stall.md](./problem/buildstorm-minibuild-post-toolchain-stall.md)。

修复 BuildStorm MINIBUILD fresh `cargo build` 的普通 fork：动态 `MAP_STACK` VMA 现按 mmap 语义继承，固定初始 stack 仍由 child 重建，避免 `CLONE_CHILD_SETTID` 写入 `EFAULT`。RISC-V debug 日志已确认原失败 clone 成功创建并运行子进程，双架构 release 构建通过；fresh RISC-V 600 秒回归尚未到达 MINIBUILD 完成标记，详见 [problem/buildstorm-minibuild-post-toolchain-stall.md](./problem/buildstorm-minibuild-post-toolchain-stall.md)。

## 7.22

修复 BuildStorm MINIBUILD fresh fork 的 TrapContext 页错误覆盖，并删除已无调用的 eager `clone_area()` 接口；final Debian 原生 libc 的 linker script 现优先使用镜像 native `ld-linux`，仅在原路径缺失时使用 legacy fallback。RISC-V fresh `prepare -> build` 已输出 `BUILDSTORM_DEBUG_MINIBUILD ok` 与 `shutdown!`；RISC-V、LoongArch64 release 构建均通过，详见 [problem/buildstorm-minibuild-fresh-fork-loader.md](./problem/buildstorm-minibuild-fresh-fork-loader.md)。
修复 CAgent 决赛脚本启动：决赛运行器改用 `/bin/bash`，VFS 可正确解析 Debian `/bin -> /usr/bin` 中间符号链接。RISC-V 日志已到达 CAgent `GROUP END` 和 `shutdown!`，7 项 pass、3 项 reject；详见 [problem/cagent.md](./problem/cagent.md)。

实现 Linux `fchdir(50)`：从 fd 直接定位目录 inode，校验 `EBADF`、`ENOTDIR` 和目录 search 权限后更新共享 cwd，并补齐用户态封装。RISC-V `log.ans` 中 musl/glibc 的 `fchdir01` 至 `fchdir03` 均为 `TPASS`，已解除对应 LTP blacklist。

实现 Linux `prctl(PR_SET_CHILD_SUBREAPER)`：进程级 subreaper 标记现会在父进程退出时接管孤儿后代，并补齐 `PR_GET_CHILD_SUBREAPER`。RISC-V musl/glibc `prctl03` 均为 `passed 6 failed 0 broken 0` 且正常 `shutdown!`；未验证 LoongArch64，详见 [problem/prctl-child-subreaper.md](./problem/prctl-child-subreaper.md)。

修复 LTP `prctl04` seccomp 语义：`PR_SET/GET_SECCOMP` 现保存线程级 strict/filter 状态，在 syscall 入口执行严格模式和已验证的经典 BPF 子集，并由 fork/clone 继承。RISC-V、LoongArch64 的 musl/glibc 均为 `passed 9 failed 0 broken 0` 且正常 `shutdown!`；详见 [problem/prctl-seccomp-prctl04.md](./problem/prctl-seccomp-prctl04.md)。

实现 Linux `seccomp(277)`：支持 strict/filter 两种安装操作，复用既有 seccomp 状态、权限检查和 classic BPF 校验，并补齐用户态封装。RISC-V、LoongArch64 release 构建通过；详见 [problem/seccomp-syscall-277.md](./problem/seccomp-syscall-277.md)。

修复 LoongArch 八核 basic 随机非法指令与段错误：COW 分裂现固定源 `FrameTracker`，先复制新页再替换 PTE，避免父子跨 hart 并发释放并复用仍在读取的源页。LoongArch64 release 构建通过，`basic-musl/basic-glibc` 连续五轮完整结束并正常关机；详见 [problem/loongarch-cow-source-frame-race.md](./problem/loongarch-cow-source-frame-race.md)。

修复 LTP read03 `TBROK: Mode does not indicate fifo file`：mknodat 创建的 FIFO 在 ext4 上 inode mode 类型位被误置为普通文件，fsidx 记录的正确 InodeType 未应用于 fstat 输出。在 `Ext4Inode::fstat()` 末尾用 `FsIndex::special_node_type` 修正 st_mode 类型高位。LoongArch64 musl/glibc read03 均 TPASS；详见 [problem/read03-fifo-mode-type-bits.md](./problem/read03-fifo-mode-type-bits.md)。

修复 LoongArch LTP `fs_bind01`：启动期兼容 BusyBox hush 的 LTP timeout 局部变量写法，并将 bind mount 的 `/proc/mounts` source 改为非路径表示，避免 BusyBox 单次误卸两层挂载。LoongArch64 musl 单跑为 `passed 29 failed 0 broken 0` 并正常关机；详见 [problem/fs-bind01-loongarch-hush-timeout-mount-stack.md](./problem/fs-bind01-loongarch-hush-timeout-mount-stack.md)。

## 7.22

修复 LTP `kill07`：用户态 `SIGKILL` 现会在进入 pending 集时记录进程终止原因，阻塞快速退出后 `waitpid()` 正确返回信号终止 status；内部 execve sibling 清理信号不会污染状态。LoongArch64 单跑为 `passed 1 failed 0 broken 0` 并正常 `shutdown!`，双架构 release 构建通过；详见 [problem/kill07-sigkill-fast-exit-wait-status.md](./problem/kill07-sigkill-fast-exit-wait-status.md)。

修复 LoongArch CAgent 的动态链接器 LSX panic：每个 hart 现启用 `EUEN.SXE`，用户 trap、信号和 clone 上下文完整保存 32 个 128-bit LSX 寄存器。LoongArch64 release 构建通过，`log.ans` 到达 CAgent `GROUP END` 和 `shutdown!`，无 panic；详见 [problem/loongarch-cagent-lsx-disabled-panic.md](./problem/loongarch-cagent-lsx-disabled-panic.md)。

修复 LoongArch 批量 LTP 在 `fsmount01` 前的 Rust heap OOM：全局 heap 耗尽后可安全接管 CMA 连续页，LTP 退出边界同步回收 idle VFS 缓存、shared mmap 和遗留 helper。双架构 release 构建通过；LoongArch `fsmount01` 单例无 heap panic、6 项 TPASS，但仍有 1 项既有 TBROK，完整批量回归受运行窗口限制待续。详见 [problem/loongarch-ltp-heap-cma-reclaim.md](./problem/loongarch-ltp-heap-cma-reclaim.md)。

## 7.22

修复 BuildStorm Rustc 编译 `unicode-ident` 的 `mremap(MREMAP_MAYMOVE)` 数据丢失：私有 VMA 现先复制已驻留页、全部成功后才卸载旧映射。RISC-V、LoongArch64 构建和 RISC-V debug 构建通过；RISC-V probe 的原文件/复制文件 Rustc 均返回 0，但 180 秒窗口未完成完整 Cargo 编译。详见 [problem/buildstorm-mremap-data-loss.md](./problem/buildstorm-mremap-data-loss.md)。

## 7.22

修复 BuildStorm Rustc artifact rename 后的 lwext4 write-back 时序：rename 前先完整回写并清除旧 pathname cache，成功后清理源/目标孤立状态，短写返回 `EIO` 阻止发布。RISC-V、LoongArch64 release 构建通过，RISC-V `rename_publish` 回归通过；完整 `cargo build -p tg-xtask` 仍未在本轮完成，详见 [problem/buildstorm-rustc-artifact-rename-writeback.md](./problem/buildstorm-rustc-artifact-rename-writeback.md)。

## 7.22

将 BuildStorm 分阶段诊断从 `initfiles.rs` 的启动期脚本注入迁至 `user/src/bin/buildstorm/`：各 case 在用户态写入固定 `/tmp/buildstorm-*.sh` 后由 Bash 单独执行，保留官方顺序与扩展诊断组合入口。RISC-V snapshot 已确认选定脚本从 `/tmp` 启动并进入 `cargo build -p tg-xtask`，但 120 秒窗口未完成；内核不再向根文件系统写入 BuildStorm 测试脚本，详见 [problem/buildstorm-minibuild-post-toolchain-stall.md](./problem/buildstorm-minibuild-post-toolchain-stall.md)。
修复 LoongArch CAgent loopback TCP 大请求：仅 IPv4 loopback 使用 4096 B MSS 并以 IPv4 分片传输，修正 smoltcp 首片构造对整个 fragmentation buffer 计算 TCP checksum 的错误，同时串行排空全局 fragmenter。新增 checksum 回归测试通过；最终 LoongArch64 CAgent 十项均 pass，RISC-V release 构建通过，详见 [problem/cagent-loopback-tcp-fragmentation.md](./problem/cagent-loopback-tcp-fragmentation.md)。

将定制 `smoltcp` 从 `os/vendor/smoltcp` 迁移到 `crates/smoltcp`，保持 `[patch.crates-io]` 本地补丁模式；`os/dotcargo/config` 的 `vendor` 目录仍只服务其余离线注册表依赖。smoltcp 定向离线单测与 RISC-V、LoongArch64 release 构建均通过。

修复 BuildStorm 全量入口误用分阶段 DEBUG 输出导致 judge 为 0 分：正式 runner 现构建期嵌入参考脚本并从 `/tmp` 一次执行。双架构构建通过；RISC-V 180 秒回归已恢复 `BUILDSTORM_TOOLCHAIN/MINIBUILD ok`，judge 为 20/180，预构建阶段超时，尚未验证 compile/time 项；详见 [problem/buildstorm-full-run-marker-contract.md](./problem/buildstorm-full-run-marker-contract.md)。

## 7.23

优化 BuildStorm 普通只读路径：跳过一次性读取不需要的 whole-file cache 初始化和 `fseek`，
跨页 `read()` 合并为一次 EXT4 操作，并移除每次读取前的冗余 `inode.size()` 探测；同时将
调度器 timer 维护扫描改为按 hart 快照、10 ms 节流。双架构 release 构建通过，独占 RISC-V
运行进入 `pre-build tg-xtask` 且无错误，但完整 446 crate 和严格 A/B 加速比例仍待完成，详见
[problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

## 7.23

修复 LoongArch BuildStorm 对稀疏 EXT4 文件的读取破坏：hole 现零填且不参与物理块聚合，C archive 会随源码变更重建；`uname -m` 同时返回标准 `loongarch64`。官方 Docker 重建后 Rust 工具链和 MINIBUILD 均恢复 `ok`，完整 compile 仍因外层 QEMU 终止待续跑；详见 [problem/loongarch-buildstorm-sparse-ext4-read-corruption.md](./problem/loongarch-buildstorm-sparse-ext4-read-corruption.md)。
## 7.22

修复 LoongArch64 LTP `fcntl14` 的 `rt_sigsuspend` 信号掩码恢复时机：临时解除屏蔽的 `SIGUSR1` 现可先建立 handler frame，并在 `rt_sigreturn` 后恢复调用前 mask。LoongArch64 musl/glibc `fcntl14` 均为 `passed 96 failed 0 broken 0`；详见 [problem/fcntl14-sigsuspend-mask-restore.md](./problem/fcntl14-sigsuspend-mask-restore.md)。

## 7.23

修复 LTP `lseek11` 的 ext4 `SEEK_DATA/SEEK_HOLE`、sparse write 和 `ftruncate` partial-tail 数据一致性，并收口 sparse cache 的首次打开、`O_TRUNC`、unlink 生命周期及 ftruncate 锁错误路径：LoongArch64 musl/glibc 均为 `passed 15 failed 0 broken 0` 并正常 `shutdown!`；临时 `ftruncate01` 两侧均通过后已移除入口。RISC-V、LoongArch64 release 构建通过，详见 [problem/lseek11-ext4-seek-data-hole-sparse-write.md](./problem/lseek11-ext4-seek-data-hole-sparse-write.md)。

## 7.23

修复 BuildStorm Rustc 长 `argv` 被 pathname 256 B 上限截断导致的 `E0765`：`execve` 现按显式上限保存原始参数字节，并在替换地址空间前完成可失败的新栈构造；RISC-V 无任务时改用 timer-backed `WFI`，LoongArch 保持保守轮询。`initproc` 的定向入口保留子测例返回码。双架构构建通过，定向 prebuild 已越过原错误位置但完整 446 单元尚未完成，详见 [problem/buildstorm-execve-argv-truncation.md](./problem/buildstorm-execve-argv-truncation.md)。

优化 BuildStorm 并行构建：恢复 `sched_getaffinity` 的八核可见拓扑，允许干净 `MAP_PRIVATE` 文件页跨进程复用并通过 COW 保持私有写语义，同时删除 mmap 缺页的重复清零/中间复制。RISC-V、LoongArch64 release 构建通过，RISC-V 180 秒启动已进入多 crate Cargo 预构建；完整 446 单元耗时对比尚未完成，详见 [problem/buildstorm-parallel-file-cache.md](./problem/buildstorm-parallel-file-cache.md)。
收束决赛 CAgent/BuildStorm 本地诊断包装：平台入口继续在 `/glibc` 下以 `run_final_testsuit` 直接运行两个正式 `testcode.sh`；CAgent 仅保留 10 个单项入口，BuildStorm 仅保留 toolchain、MINIBUILD、compile success、compile time 四项。两架构用户态构建通过；未运行 QEMU，因为单项依赖 final 镜像环境且完整编译最长可达 4 小时。

## 7.24

为 BuildStorm 长时间编译增加低开销 guest 内统计：系统调用按 read/write/open/close/stat/mm/process/futex 分类，EXT4 记录读取字节数及全局锁等待/持有 tick，文件页缓存记录命中/缺页，调度器记录 ready queue 选取和 idle 次数；汇总最多每 30 秒输出一次，不逐条污染评分日志。独占 RISC-V 90 秒样本无 panic/错误，在 63 秒时观察到约 55 万次 syscall、13.3 万次 futex 和 631 万次调度选取，EXT4 锁等待约 47 万 tick、持有约 2.63 亿 tick。该结果表明下一轮应优先检查调度/任务切换频率，不能继续把文件系统假定为唯一瓶颈；双架构 release 构建通过。详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

优化 timer 抢占路径：当前 hart 没有其他就绪任务时直接返回，避免无竞争场景经过 idle 上下文往返；CFS/RR 均提供按 hart 的就绪检查，阻塞和主动让出路径保持原语义。双架构 perf 构建通过，完整 BuildStorm A/B 尚未完成，详见 [problem/scheduler-uncontended-preemption.md](./problem/scheduler-uncontended-preemption.md)。

修复 `rt_sigsuspend` 忙让出：等待信号的任务现在进入 `Blocked`，由信号唤醒，并补齐阻塞前 pending 检查；同时收束无竞争 `sched_yield` 与 timer future `nanosleep` 路径。详见 [problem/sigsuspend-busy-yield-scheduler.md](./problem/sigsuspend-busy-yield-scheduler.md)。

## 7.24

优化 clone 的 procfs 路径：clone 时只登记 PID，首次访问 `/proc/<pid>` 或枚举 `/proc` 时再物化 EXT4 目录；RISC-V 样本中 procfs 阶段由 95703 us 降至 231 us，CAgent CPU 正常通过。详见 [problem/clone-procfs-lazy-materialization.md](./problem/clone-procfs-lazy-materialization.md)。
## 7.23

修复 LTP `mmap3` 40 线程临时文件导致的 write-back cache 抖动，并回收 `MAP_STACK` 动态线程栈；RISC-V QEMU 中 musl/glibc 均 `TPASS`，详见 [problem/mmap3-cache-churn-and-map-stack-leak.md](./problem/mmap3-cache-churn-and-map-stack-leak.md)。

## 7.24

修复 LTP `mmap16` 的 `mremap` 原址扩展、共享 mmap 空间耗尽时的 `SIGBUS` 和 loop/ext4 逻辑容量账本，并优化连续小写入缓存路径。RISC-V musl/glibc 均完成 10 轮、summary 为 `passed 10 failed 0 broken 0`；详见 [problem/mmap16-ext4-loop-enospc-writeback.md](./problem/mmap16-ext4-loop-enospc-writeback.md)。

修复 LTP `mmap18`：匿名 `MAP_GROWSDOWN` VMA 现可在守卫页缺页时受限向下扩展，默认 `SIGSEGV` 改为终止线程组；execve 内部 sibling `SIGKILL` 保持线程级清理。RISC-V `log.ans` 中 musl/glibc 均 `passed 4 failed 0 broken 0` 并正常关机，详见 [problem/mmap18-growsdown-sigsegv-group-exit.md](./problem/mmap18-growsdown-sigsegv-group-exit.md)。

修复 LTP `mmap14` 的 `MAP_LOCKED` 标志保留和 `/proc/self/status` `VmLck` 统计；RISC-V musl/glibc 均 `TPASS`，详见 [problem/mmap14-map-locked-vmlck.md](./problem/mmap14-map-locked-vmlck.md)。

修复 LTP `mmapstress04` 文件扩展后的 mmap EOF 误判：缺页时改用 backing inode 当前长度，RISC-V musl/glibc 均 `TPASS`；详见 [problem/mmapstress04-dynamic-eof.md](./problem/mmapstress04-dynamic-eof.md)。

修复 LTP `mount03` 的普通文件 EOF 读取未更新 atime：EOF 快速路径现与普通读取路径一致记录访问时间。RISC-V musl/glibc 均为 `passed 55 failed 0 broken 0`；详见 [problem/mount03-atime-eof-read.md](./problem/mount03-atime-eof-read.md)。

## 7.25

优化 `execve` 动态解释器装载：ELF 前缀改为增量读取，解释器 PT_LOAD 文件段改为按需私有文件映射，降低全量物理页分配与复制。RISC-V `log.ans` 中 12 次 `execve` 累计 `208763 us`，CAgent `fs-search` 通过并正常关机；详见 [problem/execve-dynamic-interpreter-demand-paging.md](./problem/execve-dynamic-interpreter-demand-paging.md)。

进一步收缩 `execve` 解释器装载：仅读取 ELF 元数据，未页对齐段直接填充用户页，避免页对齐段在内核堆中的重复整镜像读取。RISC-V `fs-create` 单项中 `execve` 10 次累计由 `188572 us` 降至 `170980 us`，agent wall-clock 由 `522 ms` 降至 `472 ms`；详见 [problem/execve-dynamic-interpreter-demand-paging.md](./problem/execve-dynamic-interpreter-demand-paging.md)。

分析 RISC-V CAgent `fs-readwrite` 的 clone 统计：11 次 syscall 边界计时累计 `148592 us`，但 `clone_process` 实际创建累计仅 `22196 us`。差值来自 `CLONE_VFORK` 在 `sys_clone()` 中等待子进程 `execve/exit` 后才向父进程返回的既有 Linux 语义，不是子任务创建热路径；本轮未改动内核功能，后续性能对比应以 `clone_process` 分段计时与 vfork 等待分开判断。

根据 `debug.ans` 追踪 `buildstorm::minibuild::run()`：PID 64 有 8329 次 `lseek`，集中在 cargo/rustc 归档处理的 seek/read 路径。为验证 `OSFile::lseek()` 中 EXT4 类型检查、`SEEK_END` 大小查询和稀疏文件探测的实际占比，新增分阶段聚合计时；RISC-V、LoongArch64 构建通过，但 QEMU 仍受宿主 `/var/tmp` 只读限制，尚未取得 guest perf 快照。详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

新增 `scripts/plot_perf_durations.py`：解析最后一个 `shutdown!` 后的 perf 汇总，默认统计互斥的 `syscall_duration` 项，并生成带耗时和比例图例的标准库 SVG 饼图；支持切换指标族、列出可用指标和保留 `*_active` 子项。

修正 `wait4`/`waitid` 与 futex 的 perf 耗时口径：阻塞型 syscall 不再使用通用 syscall 边界时间，改为累计每次实际运行的 poll、等待前准备和唤醒后收尾区间；报告中的 `wait`/`futex` 因此不再包含任务被调度出去的时间。RISC-V、LoongArch64 perf 构建通过，QEMU 仍受宿主 `/var/tmp` 只读限制未能启动。

## 7.25

优化 BuildStorm MINIBUILD 的缓存路径命中：移除 `FsIndex` inode 双查和重复 alias 更新，复用 `open` 的 inode 类型，并让 EXT4 inode 类型查询走无锁只读字段。`log.ans` 中 MINIBUILD 正常 `ok`、`shutdown!`；RISC-V perf 构建通过，严格 A/B 运行耗时待补。详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

调整 `scripts/plot_perf_durations.py`：旧日志同时包含 `accept` 与 `accept_active` 时，默认图表使用只计实际运行区间的 `accept_active`，与 `wait` 的兼容逻辑一致；`--include-active` 仍保留原始嵌套项。

## 7.26

优化 CAgent/BuildStorm 的全量并发吞吐：`log.ans` 证实 EXT4 全局锁累计等待约 29.034 秒，现将 inode 路径和已知文件大小移出该锁，并让文件页缓存命中并发读取；同时移除 TCP 收发的无条件全栈轮询及 router RX 队列整包复制。双架构 perf 构建通过；维护者反馈评测性能提升明显，详见 [problem/cagent-ext4-tcp-throughput.md](./problem/cagent-ext4-tcp-throughput.md)。

修复 BuildStorm 中 `O_UNLINK`/`O_NOFOLLOW` 查找将 `/usr/bin/cc` 末级符号链接回填到普通 inode 缓存的问题，并补齐 Debian 中间符号链接解析。RISC-V 构建通过；定向日志已越过旧 LTO 报错位置至 Cargo `37/446`，完整回归待续，详见 [problem/buildstorm-cc-symlink-cache-poisoning.md](./problem/buildstorm-cc-symlink-cache-poisoning.md)。

修复 BuildStorm `ppoll` 未就绪时忙让出的调度热循环：改用 fd waker 与 timeout future 阻塞等待。RISC-V 新样本在约 342 s 时调度选择降至 15.3 万次、Cargo 推进至 `6/446`，无 panic/TFAIL/TBROK；完整编译仍因外层 QEMU 终止待续，详见 [problem/buildstorm-ppoll-busy-yield.md](./problem/buildstorm-ppoll-busy-yield.md)。

优化 BuildStorm 的 lwext4 全局锁竞争：竞争任务改由 `PollSet` 阻塞并在解锁时唤醒，启动期保留自旋回退；同一只读描述符重用不再在锁内分配比较 `CString`。相近读取量的新 RISC-V 样本中锁等待和持锁累计均下降，且无 panic/TFAIL/TBROK；完整 446 crate 仍待完成，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

进一步收敛 BuildStorm lwext4 锁交接：`PollSet` 对 waiter 去重并按 FIFO 单唤醒，EXT4 解锁只移交一个任务，避免并发 Cargo waiter 同时重试同一把锁。RISC-V、LoongArch64 `make perf` 通过；90 秒 RISC-V 定向运行已进入 `buildstorm-compile` 预构建后由外层 timeout 停止，完整 A/B 数据待后续 `log.ans`。

为 BuildStorm 普通小文件跨页 `read()` 接入共享文件页缓存，冷读只回填完整页，写入、截断和 rename 继续失效缓存。RISC-V/LoongArch64 release 构建通过；180 秒 RISC-V 定向运行无异常并推进到 Cargo `1/446`，完整 A/B 与全量编译仍待后续验证，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

修复 BuildStorm 的 `MemorySet`/EXT4 可睡眠锁死锁：文件 mmap 缺页、fork 预取及共享页写回均改为锁外 I/O、锁内短暂页表更新，并保持 `TaskControlBlockInner -> ResourceSlot` 的既定顺序。双架构 release 构建通过；详见 [problem/memoryset-ext4-sleeping-lock-deadlock.md](./problem/memoryset-ext4-sleeping-lock-deadlock.md)。

优化 BuildStorm 并行 Rust 编译的 VFS 热路径：`CLONE_VM|CLONE_VFORK` 的 exec 子进程可分布到各 hart，全局 inode/dentry 缓存跨短命 worker 保留并有 32K 上限，普通单页读复用页缓存，普通文件 `fstat` 复用可失效元数据。RISC-V/LoongArch64 构建通过；新 RISC-V 日志在约 303 s 前到达 `11/446`、无错误，完整编译和 `fstat` 的运行期 A/B 仍待验证，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

## 7.27

优化 BuildStorm lwext4 元数据热路径：首次路径查找复用同一 `ext4_stat_get()` 的类型、identity 和 size，`FsIndex` 不再为新 inode 重复登记初始 alias。3 分钟 RISC-V 样本在相近文件系统工作量下将 `ext4_fstat_lock` 样本从 `8929` 降至 `6028`，持锁从 `7.39 s` 降至 `4.11 s`；双架构构建通过。样本未完成 446 crate，尚不报告端到端加速，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

根据 10 分钟 `3.ans` 的分类统计优化 BuildStorm 写回缓存：`write_at` 已成为最大 EXT4 临界区，现将 whole-file cache 从 10 项 FIFO 改为 32 项有界 LRU，写命中提升活跃产物以避免并行 Rustc 临时文件反复被驱逐、重建和整文件回写。RISC-V、LoongArch64 `make perf` 均通过；新内核的同配置运行样本待取得，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

复核含 LRU 的十分钟 `4.ans`：Cargo 仍为 `9/446`、没有 panic/TFAIL/TBROK，但 `write_at` 的平均 EXT4 锁持有由 `3.ans` 的约 `33.1 ms` 降到约 `30.3 ms`；样本非严格 A/B，故仅作为局部方向证据。继续优化已缓存父目录下的末级 miss：实际目录 inode 已证明所有父级完成解析，child 不存在时不再逐前缀扫描中间符号链接；最终链接、普通路径、`O_NOFOLLOW`/`O_UNLINK` 保持旧语义。RISC-V、LoongArch64 `make perf` 均通过；新 fast path 尚无 guest 样本，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

分析含 cached-parent fast path 的 30 分钟 `5.ans`：Cargo 已推进到 `33/446`，但末段一次 Rustc 临时产物 rename 在 EXT4 锁内持有约 `74.878 s`。根因是 rename 为清理 whole-file byte cache 调用 `ext4_cache_flush()`，而 lwext4 会清空整块挂载点的 dirty block list，随后 descriptor close 又重复 flush。现拆分 byte-cache 写回与 mount-wide flush：rename 仍写回并丢弃旧 pathname cache、再执行 `ext4_frename()`，但不再隐式 sync 整个 mount；显式 sync/fsync 语义保留。RISC-V、LoongArch64 `make perf` 均通过；`5.ans` 早于修复，待下一样本验证 rename lock 尖峰是否消失，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

复核修复后的 210 秒 `6.ans`：Cargo 到达 `8/446`，13 次 rename 的累计 EXT4 持锁仅 `0.571 s`，未再出现 `5.ans` 的单次 `74.878 s` 尖峰。继续移除 `ext4_fread()` 后无谓失效 regular-file `stat_cache` 的路径，避免后续 `fstat()` 重入全局 EXT4 锁；双架构 perf 构建通过，运行期 A/B 待下一样本，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

复核含 regular-file `stat_cache` 的 240 秒 `7.ans`：末尾显示 `7/446`，表面低于 `6.ans`，但其 `5/446` 标记在 `166.289 s`（早于 `6.ans` 的 `185.991 s`）；慢段集中在后续 `serde_core`/artifact 写入密集阶段。该段新增 3,398 次 EXT4 write、平均持锁约 `5.99 ms`，而 fstat 锁仅新增 147 次，未见 stat cache 导致的 fstat 样本膨胀；rename 仍无分钟级尖峰。样本未完成且不是严格 A/B，保留缓存优化，不报告整体回归或加速比例，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

收敛 BuildStorm EXT4 稀疏写、路径 metadata 与连续页读取：稀疏 inode 以 `(mount, inode)` 有界 range 缓冲并支持读时覆盖，首次 lookup 复用 identity，页缓存仅预读下一页。最新 `log.ans` 在 `Building 9/446` 的两页方案中读锁累计 wait/hold 为 `424.939/53.579 s`，低于三页试验的 `525.574/68.484 s`；新增 write `open/quota/data` 分段 perf 统计待下一次 guest 样本验证，详见 [problem/buildstorm-ext4-sparse-write-readahead.md](./problem/buildstorm-ext4-sparse-write-readahead.md)。

## 7.28

优化 BuildStorm 读写交错的 lwext4 descriptor 路径：同路径已打开的 `O_RDWR` descriptor 可直接服务只读访问，避免 `O_RDWR -> O_RDONLY -> O_RDWR` 重开。新 RISC-V 样本在 `t=257.349s` 到 `Building 15/446`，write open 阶段为 `5.527s / 5711` 次（历史样本约 `38.080s / 3912` 次）；未见 panic/TFAIL/TBROK，详见 [problem/buildstorm-ext4-sparse-write-readahead.md](./problem/buildstorm-ext4-sparse-write-readahead.md)。

继续收束 BuildStorm sparse 写入：当时观测到 689 个已提交 sparse range、平均约 `23.5 KiB`，故将可并存的交错 range 槽位从 16 增至 32，数据上限与所有可见性 flush 边界保持不变。该计数后来确认按每个 `ext4_fwrite()` range 而非每个 buffer flush batch 递增，故只能作为候选证据；RISC-V、LoongArch64 `make perf`、普通 release 构建和格式/补丁检查均通过，详见 [problem/buildstorm-ext4-sparse-write-readahead.md](./problem/buildstorm-ext4-sparse-write-readahead.md)。

复核 5 分钟 RISC-V BuildStorm `log.ans`：末尾 guest 快照为 `t=216.291s`、Cargo `8/446`，无 panic/TFAIL/TBROK 或结束标记。32-run 后 sparse 提交 range 从 689 降至 628、平均 range 从 `23.50 KiB` 升至 `25.75 KiB`，但该计数按每个 `ext4_fwrite()` range 而非每个 buffer flush batch 递增，不能据此宣称槽位优化已生效；后续先补齐 batch/reason 统计，再以页缓存旁路来源定位 read/write/find/fstat 的 EXT4 锁排队，详见 [problem/buildstorm-ext4-sparse-write-readahead.md](./problem/buildstorm-ext4-sparse-write-readahead.md)。

分析维护者提供的三分钟样本 `tmp_01.ans`、`tmp_02.ans` 和页缓存等待实验回归 `tmp_03.ans`：原始样本中 `lseek` 的 `type_check` 累计 `15.522 s / 32920` 次，根因是每次调用重复执行 `inode.path()`、特殊节点表查询和 inode 类型回退；`OSFile` 现于 open 时缓存 `seek_type`，FIFO/socket 的 `ESPIPE` 语义保持不变。`tmp_02.ans` 中同量级调用的 `type_check` 为 `0`，`lseek` 实现累计降至 `10.833 ms`，证明热路径缺陷已消除；但 Cargo 阶段不同，不能报告整体 BuildStorm wall-clock 或超过 Linux。尝试对照 Linux locked-folio 的页缓存 loading 等待模型在 `tmp_03.ans` 中停在 `t=33511ms, Building 0/446`，已撤销，不作为交付修复。RISC-V/LoongArch64 `make perf`、格式和补丁检查通过；完整 BuildStorm、QEMU 独立 A/B 与 Linux 端到端对标仍待同配置运行。详见 [problem/buildstorm-lseek-open-file-type-cache.md](./problem/buildstorm-lseek-open-file-type-cache.md)。

复核维护者更新后的 `tmp_03.ans`：该同名文件实际是另一份 RISC-V 运行，`t=175421ms` 到达 `Building 5/446`，最后快照的文件页缓存命中/缺页为 `453844/24266`，但 `ext4_read_lock` 累计 wait/hold 仍为 `311.033/29.048 s`。发现大于一页的 `read()` 只要包含一个冷页，旧路径就会整段回源，重复读取其余已命中页；现改为按连续冷页段读取、命中页直接拼装，并沿用完整页插入与既有失效语义。RISC-V/LoongArch64 `make perf` 通过，尚无包含该修复的新 guest A/B，不能报告端到端加速比例；详见 [problem/buildstorm-lseek-open-file-type-cache.md](./problem/buildstorm-lseek-open-file-type-cache.md)。

复核修复后的 `tmp_04.ans`：最后可见 Cargo 为 `Building 5/446`，最后 perf 快照为 `t=145163ms, Building 4/446`，无 panic/TFAIL/TBROK/ERROR/shutdown。与旧 `tmp_03.ans` 约 145 秒快照相比，EXT4 read lock wait/hold 从 `226.250/24.141 s` 降为 `122.490/16.571 s`，read total/active 从 `199.853/118.286 s` 降为 `150.207/49.550 s`；由于 Cargo 阶段和请求工作量不同，只记录为混合命中修复的方向性证据，不报告严格 A/B 或整体 wall-clock 加速。详见 [problem/buildstorm-lseek-open-file-type-cache.md](./problem/buildstorm-lseek-open-file-type-cache.md)。

继续收缩 BuildStorm 文件页缓存命中成本：将全局 `(pathname, page_index)` 平铺索引改为路径分组的两级索引，并让 EXT4 inode 复用 `Arc<str>` 路径，避免数十万次命中反复分配/复制 pathname；写入、truncate、rename 的路径失效语义不变。`tmp_05`/两分钟 `tmp_06` 均受历史上已有的偶发 Rustc `SIGSEGV` 干扰，未报告运行期加速；RISC-V、LoongArch64 `make perf` 与格式/补丁检查通过，详见 [problem/buildstorm-lseek-open-file-type-cache.md](./problem/buildstorm-lseek-open-file-type-cache.md)。

复核 `tmp_08`/`tmp_09` 的数十万文件页 fault：mmap 已加载页的第二次缓存查询现复用 inode 的 `Arc<str>` 路径，不再为每个 fault 分配 pathname；RISC-V perf 构建和格式检查通过，`tmp_09` 仅为中途样本，不报告端到端加速，详见 [problem/buildstorm-lseek-open-file-type-cache.md](./problem/buildstorm-lseek-open-file-type-cache.md)。

参照 Linux inode 级写入与页缓存模型重构 BuildStorm EXT4 写缓存命中路径：已有 dense cache 且位于 quota 预留范围内的写入以每 inode 可睡眠状态锁串行，不再占用全局 lwext4 gate；cache miss、稀疏/超限、延迟删除、淘汰写回和所有 lwext4 调用仍走慢路径。仅完成格式与静态补丁检查，未按维护者要求重复构建，详见 [problem/buildstorm-ext4-inode-cache-write-concurrency.md](./problem/buildstorm-ext4-inode-cache-write-concurrency.md)。

复核 `tmp_10.ans`/`tmp_11.ans` 后停止扩大文件页预读：两份日志均未完成，且既有三页预读样本的读锁 wait/hold 高于正式两页方案。本轮保留两页预读，新增页缓存按 mmap/read/splice 的 hit/miss 归因与普通 read 的准入旁路统计，为下一份同配置样本定位可安全合并的冷读来源；仅通过格式和补丁检查，未重新构建或运行。

根据 `tmp_13.ans` 的下一轮锁优化方向补齐 lwext4 全局锁分类：新增 close、目录读取、路径恢复、metadata、namespace、sync、seek 等聚合 wait/hold/max 统计，并在页缓存记录底层加载竞争。RISC-V 130 秒样本中普通 `read` 占该窗口 EXT4 锁等待约 88.8%，而同页重复加载仅 `434/18080`（约 2.4%）；因此保留单一 lwext4 串行边界，暂不引入高风险的 per-page single-flight/waiter。双架构 perf 构建通过，两个 BuildStorm 样本均在外层 timeout 前中止，未报告端到端加速，详见 [problem/buildstorm-ext4-sparse-write-readahead.md](./problem/buildstorm-ext4-sparse-write-readahead.md)。

复核 `tmp_01.ans`/`tmp_02.ans` 后，以每 inode 状态锁保护 lwext4 descriptor，并把读取拆为 descriptor-open 和 data-read 两个全局锁段；唯一 `EXT4_OP_LOCK` 保留，避免把非 SMP-safe 的 lwext4 C API 错误并发化。文件页缓存最终采用最多四页/16 KiB 的有界顺序预读，8 页试验因放大读锁 hold/wait 已撤回。RISC-V perf、LoongArch64 release 构建及两个 `/tmp` qcow2 overlay 的 120 秒 BuildStorm 冒烟通过，但最终只可见 `Building 6/446`，没有完整 `BUILDSTORM_COMPILE ... ok=true elapsed_s=...`，不能声称已满足评测机一小时目标；后续优先恢复 `dentry_positive_hit`、细分 metadata/namespace 并建立完整受控基线，详见 [problem/buildstorm-ext4-sparse-write-readahead.md](./problem/buildstorm-ext4-sparse-write-readahead.md)。

补齐 BuildStorm 长 `CLONE_VFORK` 等待的四段时序与 `exec/exit/signal` release-reason 聚合，保留原有端到端 `vfork_wait`，不改变 parent 唤醒语义。RISC-V 和 LoongArch64 的 `make perf` 与普通 release 构建均通过；完整 BuildStorm 的异常尾段仍待用新统计复现，详见 [problem/buildstorm-vfork-clone3-lifecycle.md](./problem/buildstorm-vfork-clone3-lifecycle.md)。

## 7.29

将原计划引入的 stress-ng vfork/exec workload 收敛为原生 Rust 微基准：新增无分配的 `vfork`/raw `execve` 封装，串行运行 256 次 vfork+exit、64 次 vfork+exec 与 64 次 fork+exec 对照。RISC-V perf guest 运行三组均成功，`vfork_release` 为 `exec=64 exit=256 signal=0`；RISC-V、LoongArch64 perf 构建通过，未将单次样本作为性能提升结论。

将主 ELF 页对齐 PT_LOAD 改为 `MAP_PRIVATE` 文件后端按需映射，非对齐段保留 eager、`.bss` 保持匿名 lazy。`log1.ans` 对比 `log.ans`：EXT4 读取从 177,497,216 B 降至 461,952 B，`execve/from_elf/map_elf` 累计耗时分别下降约 88.7%/93.9%/94.2%，vfork profile 全部 `failed=0`；详见 `problem/execve-main-elf-lazy-file-mapping.md`。

分析 lmbench `tmp_01.ans` 的 write 统计并新增 regular-file `write_active` 分类：最终 `write=44.321130 s` 中仅 `1.894315 s` 属于常规文件，EXT4 write lock wait/hold 仅 `0.612/1.798 s`，高值来自 pipe 等非文件 fd 的阻塞累计。RISC-V perf 构建通过，guest 正常 `shutdown!` 且未见 `TFAIL/TBROK/panic`；未将不稳定的 pipe 吞吐样本宣称为文件系统加速。

补齐 pipe 的 perf 聚合取证：按 read/write 分别记录调用、请求/完成字节、short 操作、实际阻塞等待、用户缓冲区与 `PipeBuf` 间的数据复制、阻塞任务/poll 唤醒及唤醒后重检；新增不改变全量 lmbench 的 glibc/musl `bw_pipe -P 1` 独立入口和明确日志边界。RISC-V/LoongArch64 perf 与 release 构建通过；RISC-V guest 已输出新统计，预制测试镜像未装入新 `initproc`，故独立入口仍待镜像同步后完成运行验证。

针对 `tmp_02.ans` 的 lmbench pipe-only 基线，修复 RISC-V 远端 idle hart 未被 runnable task 入队通知、只能等 10 ms timer 的调度延迟：scheduler 在二次 run-queue 检查保护下仅唤醒已 idle 的远端 hart。两次同配置 QEMU 均正常结束，pipe 吞吐由 `40.90 MB/sec` 升至 `251.96` 和 `223.03 MB/sec`，IPI 均零失败；详见 [problem/riscv-pipe-remote-idle-wakeup.md](./problem/riscv-pipe-remote-idle-wakeup.md)。

细分同一 pipe workload 后，消除大消息 write 的临时 `Vec -> PipeBuf` 重复制，以及 read 的 `PipeBuf` 数据聚合 `Vec`：两次 RISC-V QEMU 完整样本为 `264.51/258.19 MB/sec`，read/write copy 相对细分基线分别下降约 `51.5%~52.9%` 和 `20.7%`，无 short I/O 或 IPI 失败。RISC-V、LoongArch64 perf/release 构建均通过，详见 [problem/riscv-pipe-remote-idle-wakeup.md](./problem/riscv-pipe-remote-idle-wakeup.md)。

为 lwext4 长临界区补齐内部阶段聚合：rename 细分 write-back、close、底层 rename 与 VFS/cache 失效，namespace 细分 create/unlink/truncate/link-symlink，metadata 细分 size/timestamp/mode/owner/link-count/alias/recovery 等。所有统计仅在 `perf` feature 下生效，未移动 `EXT4_OP_LOCK` 或修改文件系统语义；RISC-V、LoongArch64 的 perf 与 release 构建均通过，尚待同配置 BuildStorm 样本取证。

重构 lwext4 性能计时归属：`ext4_lw/mod.rs` 仅保留实际 gate/互斥实现，锁分类、`lock_for_*()` 包装、tick 与 atomic 统计均迁入 `utils/perf.rs`；释放 mutex 后再记账的顺序不变。RISC-V、LoongArch64 perf 与 release 构建均通过。

按内核分区重构 `utils/perf`：根 `perf.rs` 保留兼容导出和限频报告，lwext4/VFS/pipe、syscall、task、scheduler、net 及通用原子/报告格式分别进入子模块。计数器名称、`Relaxed` 记账、tick 范围和报告标签不变；RISC-V、LoongArch64 release 构建通过，未运行新的 QEMU/BuildStorm workload。

复核 `tmp_05.ans` 的 lwext4 内部阶段统计：31 次 rename 的 gate 持锁为 `10.991225 s`，其中 `write_back_cache=8.024712 s`、`lwext4_rename=2.958042 s` 均为必须保留在 gate 内的 lwext4 调用；唯一纯内存收尾 `vfs_cache_invalidate=6.723 ms`（最大 `2.206 ms`，约 0.06%）不满足“20% 或单次 500 ms”阈值。namespace/metadata 的高值来自 create/unlink/mode/owner 等含 lwext4 调用的操作本体，已有 alias 纯内存统计仅 `333 us`；因此未凭宽泛桶移动路径缓存、alias、stat 或页缓存失效，保持现有锁边界。该日志只到 `Building 20/446`，仅有 `BUILDSTORM_TOOLCHAIN/MINIBUILD ok`，没有完整 summary，不能作为功能回归或端到端性能结论。

复核 `tmp_05.ans` 的 fstat 缓存失效假设：末尾真实 `ext4_fstat` 为 `5607`（非 `7573`），但无 gate 的 write-cache fast hit 仅 `65`、所有 write-cache hit 仅 `490`，同期 `sparse_buffer_ops=9069`。`Ext4File::fstat()` 必须先 flush sparse buffer 以准确报告 `st_blocks`，而 `statx` 直接暴露同一字段，故不能仅按 `known_size` 更新 Kstat 后保留缓存；本轮不以过期 block/timestamp 属性换取吞吐。后续若要优化，需先让 sparse buffer 维护可精确合并的 allocation metadata 并持久化写入 mtime/ctime，再评估减少对应 `ext4_stat_get`。

补齐 lwext4 regular-file `fstat` 因果统计：`stat_cache` 保存最终 miss 来源，wrapper 分别计时 sparse flush、`ext4_stat_get` 与 write-back overlay/fallback，并按 fstat/close/rename/truncate/sparse-buffer 容量淘汰归因 sparse 落盘。RISC-V、LoongArch64 perf/release 构建通过；60 秒 RISC-V `-snapshot` 样本已输出新字段，但由 timeout 终止，未作为功能完整回归或性能提升结论。

根据 `tmp_06.ans` 的 fstat 因果统计，保持 sparse/direct write 后的完整 `Kstat` 失效：sparse `fstat` 必须获得真实 `st_blocks`，不能只用 `known_size` 伪造元数据。改为优化 FsIndex 的 identity 校验：`Ext4Inode` 记录 unlink/rename epoch，epoch 未变化的同 `(st_dev, st_ino)` 冲突免除 live `fstat()`，epoch 变化仍回退原有探测以防 inode 号复用。RISC-V/LoongArch64 perf 与 release 构建通过；120 秒 RISC-V BuildStorm 冒烟在最后可见的 `t=98093ms` 快照已产生 `753` 次 epoch 快路径、`18` 次保守 live probe，未见 panic/TFAIL/TBROK，完整编译待续。详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

复核同入口的 `tmp_07.ans`：在 `t=583585ms` 已有 `819` 次 FsIndex identity epoch 快路径、`18` 次保守 live probe、`0` 次 stale replacement，未见 panic/ERROR/TFAIL/TBROK；同 guest 时间快照 Cargo 为 `29/446`，而 `tmp_06` 的 `t=587383ms` 为 `20/446`。这符合“安全跳过重复 probe、epoch 变化仍回退”的预期，但两个未完成样本不是严格 A/B，不报告端到端加速。剩余 fstat miss 仍以 `ColdInode` 为主，下一轮先按 inode 类型和构造来源归因，并补 inode-number reuse 压力回归；不放宽 sparse/direct metadata 失效。详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

为下一次 10 分钟 BuildStorm 取证补齐 `ColdInode` 二维统计：实际进入 lwext4 fstat 的 ColdInode 现按 regular/directory/special 与 lookup-stat/new 构造来源分为六类，六项总和可与原 ColdInode samples 交叉校验；不改变缓存、Kstat、锁或写路径。RISC-V、LoongArch64 `make perf` 通过，guest 样本待维护者运行，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

复核 `tmp_08.ans` 后，确认 4596 次实际 ColdInode fstat 全为目录，且 `2758` 次携带 pathname lookup stat；实现目录 `(Kstat, directory epoch)` 的一次性复用，epoch 变化或第二次 fstat 保持原 live lwext4 路径，不引入目录持久 stat cache，也不改变 sparse/direct metadata 失效。RISC-V、LoongArch64 perf 和 release 构建通过，perf 版 `kernel-rv` 已就绪，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

根据 `tmp_10.ans` 新增 sparse batch 归因，确认 `427` 个容量 flush 中 `422` 个（`25,856,958 / 26,011,386 B`）由 64 KiB payload 上限而非 32-run 上限触发。保持 32 runs，将单 inode payload 提至 256 KiB，并以 8 MiB 全局预算约束 pending ranges；预算压力只提交当前 inode 或回退既有 direct write，不写回其他 inode。RISC-V/LoongArch64 perf、双架构 release 构建通过，120 秒 RISC-V `-snapshot` 冒烟完成 toolchain/MINIBUILD 且无异常；完整 BuildStorm 和 sparse 语义定向回归待测，详见 [problem/buildstorm-ext4-sparse-write-readahead.md](./problem/buildstorm-ext4-sparse-write-readahead.md)。

复核同为十分钟的 `tmp_11.ans`、`tmp_12.ans`、`tmp_13.ans`：P1.1 dense write 诊断在每次 write 额外执行四次跨 hart 共享的 `Relaxed` atomic RMW，`tmp_12` 因而从 `27/446` 降至 `17/446`；诊断已移除。payload 从 256 KiB 提至 512 KiB 后，`tmp_13` 到达 `32/446`，sparse batch 从 139 降至 77，8 MiB 全局预算未触发；这是固定窗口 crate 吞吐证据，不是完整 BuildStorm 时间结论，详见 [优化方案](./优化方案.md)。

## 7.30

复核维护者提供的十分钟 `tmp_15.ans` 与二次运行 `log.ans`：两份 sparse 工作量完全一致，`41/446` 与 `38/446` 的差异来自并发锁/调度交错，且都未完成完整 BuildStorm。针对稳定的 create 后 mode/owner 元数据序列，新增 ext4 组合入口合并连续锁交接；双架构构建和 120 秒 RISC-V 启动烟测通过，详见 [problem/buildstorm-create-metadata-lock-merge.md](./problem/buildstorm-create-metadata-lock-merge.md)。

按《优化方案》P1 将写入配额预留/回滚移出 `EXT4_OP_LOCK`，拆分 write open/data 锁统计，并解除 unlink/延迟删除中的 `MNT_TABLE` 反向嵌套。维护者提供的十分钟 `tmp_16.ans` 最后推进到 `Building 30/446`，本轮另以 180 秒 RISC-V qcow2 烟测验证启动；两者均无 panic/TFAIL/TBROK/ERROR，完整 BuildStorm 仍待验收。`tmp_16.ans` 暴露的下一轮候选是恢复正向 dentry 命中，详见《优化方案》。

复核 `tmp_17/18` 后确认正 dentry 被 FsIndex path 命中绕过，保留负 dentry 而不恢复正向快路径；目录 stat 改为受目录 metadata epoch 保护的缓存。`tmp_18` 仍输出旧 `lookup_directory_stat`，未装载该缓存，故其 3653 次 ext4 fstat 仅作旧路径基线；无异常但仍未完成 BuildStorm，详见《优化方案》和 `problem/buildstorm-read-path-lock-contention.md`。

更正 `tmp_18.ans` 未装载目录 epoch cache 的取证结论后，以其 8 MiB 读旁路 `11,252` 次/`74.6 MiB` 为 P9 基线：将 regular-file 缓存准入增至 32 MiB，并把全局文件页缓存限制为 96K 页（约 384 MiB），容量满时正常直读但不发布。RISC-V、LoongArch64 perf/release 构建与格式/补丁检查通过，最终 `kernel-rv` 为 perf 版；新 BuildStorm 样本须包含 `file_cache_capacity`、`directory_epoch_cached` 和 `ext4_fstat_directory_stat`，详见《优化方案》和 `problem/buildstorm-read-path-lock-contention.md`。

复核装载新内核的 `tmp_20.ans`：同为 Cargo `26/446` 时，大文件 read 旁路从 `11,060/72.7 MiB` 降至 `45/37 KiB`，页缓存常驻约 `60K/96K` 页且未触顶；read-data/find 锁统计方向下降，但样本止于 `27/446`，不报告端到端加速。下一轮固定 P9 参数复跑，再按实际底层 read 来源归因，详见《优化方案》和 `problem/buildstorm-read-path-lock-contention.md`。

复核第二份 P9 样本 `tmp_01.ans`：末尾 read 旁路仅 `37/30,784 B`、页缓存 `53,470/98,304` 页且无容量旁路，固定 P9 参数但不以未完成的 `24/446` 样本比较吞吐。新增仅在真实 `Inode::read_at()` 返回后累计的 mmap fill/page-cached cold run/direct bypass/other ops+bytes 归因；双架构 perf/release 构建和格式/补丁检查通过，最终 `kernel-rv` 为 perf 版，未改变锁、预读或缓存语义，详见《优化方案》和 `problem/buildstorm-read-path-lock-contention.md`。

`tmp_02.ans` 验证 P10 后发现 4,096 次/14.3 MiB exec/ELF `read_at` 未纳入来源桶；将未对齐 ELF 段由逐页读改为最大 64 KiB 有界分块读，并补齐 `other` 来源计数。双架构 perf/release 构建与格式/补丁检查通过，尚待同入口新样本验证，详见《优化方案》和 `problem/buildstorm-read-path-lock-contention.md`。

`tmp_03.ans` 验证 P11：`inode_read_source` 四类来源的 ops/bytes 均与 `ext4 reads=29060/287043303 B` 精确守恒，说明 exec/ELF 漏记路径已补齐；样本止于 `Building 26/446`，不报告端到端加速。下一轮只细分 mmap demand fault 与共享映射预取来源，暂不调整缓存容量、预读或 EXT4 锁域，详见《优化方案》和 `problem/buildstorm-read-path-lock-contention.md`。

依据 `tmp_04.ans` 实施 P12 mmap backing-read 取证：将文件页缓存来源拆为 demand 缺页与共享映射 fork 预取，保留原 mmap 聚合和 P9 参数；样本仍止于 `Building 26/446`，无完整 BuildStorm 结束标记，待新 perf 内核样本决定下一处实现优化。

根据 `tmp_05.ans` 确认 BuildStorm mmap 回源全部来自 demand fault（`mmap_prefetch=0`），修复 EOF 检查先加载、随后缺页处理再次查找同一页缓存的重复路径；仅保留页偏移/size 判断，失败冷路径重新检查 EOF 以覆盖并发截断，部分尾页和 SIGBUS 语义不变。RISC-V/LoongArch64 perf、默认 release、LoongArch64 log 构建和格式检查通过，尾页定向回归待下一样本。

复核 `tmp_06.ans`：`mmap_demand=19059`、`mmap_prefetch=0`，页缓存 `55956/98304` 且容量旁路为零；四类来源 ops/bytes 与 `ext4 reads=29210/289132560 B` 精确守恒。样本仍止于 Cargo `Building 24/446`，无完整 BuildStorm 结束标记，不报告端到端加速；P13 的尾页/SIGBUS 回归与同检查点比较待后续运行，详见《优化方案》和读路径问题复盘。

修复龙芯 36GiB/12 核配置的两处启动参数遗漏：buddy allocator 补齐 order 34，入口启动栈数量同步为 12。最新 LoongArch 日志已完整通过 CAgent 十项并正常 `shutdown!`，详见 [problem/loongarch-36g-12hart-bootstrap.md](./problem/loongarch-36g-12hart-bootstrap.md)。

## 7.31

复核 `tmp_08.ans`：目录 stat 局部 epoch 已生效，`ext4_fstat_lock` 与实际 fstat 次数较 `tmp_07` 相邻窗口下降；日志止于 Cargo `Building 28/446`，无 panic/TFAIL/TBROK/ERROR/SIGSEGV，也无完整 BuildStorm 结束标记。下一轮先补 interval delta、namespace/create 子阶段和 pipe wait 归因，再决定是否移动 lwext4 gate，详见 [优化方案](./优化方案.md) 和 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

完成 P16 create 路径优化：增加 interval delta/create 子阶段统计，将 mode 与非默认 owner 合并为一次 lwext4 路径事务并省略 root 0/0 owner；普通文件和目录以底层原子 `O_EXCL` 创建，删除重复 exist check。`tmp_02.ans` 中 owner 与 exist_check 均为 0，约 598 秒到 Cargo `31/446`，无异常但仍未出现完整 BuildStorm 结束标记；下一轮先细分 rename write-back 的 21.922 秒长尾，详见 [优化方案](./优化方案.md) 和 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

分析约 30 分钟的 `tmp_06.ans`：Cargo 最后停在 `54/446`，末段 171 秒中 write-open gate 出现 `175.954s` 最大等待，而 find/fstat/namespace 仍持续获得锁，确认下一轮应先取证并修复 `EXT4_OP_LOCK` 的插队/饥饿风险；随后再合并 create metadata transaction、实施受限 rename cache re-key，并拆分后段 lwext4 长事务。详见 [优化方案](./优化方案.md) 和 [problem/buildstorm-ext4-sparse-write-readahead.md](./problem/buildstorm-ext4-sparse-write-readahead.md)。

复核 P18.1 的十分钟 `tmp_07.ans`：FIFO gate 统计满足 `queued=handoffs+cancelled+queue_depth`，`max_handoff_wait_us` 降至约 25.8 秒，`tmp_06` 的 175.9 秒级饥饿长尾已消失；样本止于 Cargo `Building 24/446`，仍不能报告完整 BuildStorm 加速。公平 gate 的取消/退出、64+ waiter 和跨 hart 回归待补，后续转向 create 元数据事务与 lwext4 长事务细分，详见 [优化方案](./优化方案.md) 和 [problem/buildstorm-ext4-sparse-write-readahead.md](./problem/buildstorm-ext4-sparse-write-readahead.md)。

根据约 30 分钟 `tmp_08.ans` 实施 P15.1：普通文件 namespace 失效优先更新已缓存父目录的 stat epoch，未缓存父目录回退全局 epoch；新增 local/global fallback 计数。`tmp_08` 止于 `t=1787649ms`、Cargo `65/446` 且未装载本轮字段，双架构 perf 构建和静态检查通过，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

复核 `tmp_09.ans`、`tmp_10.ans` 后实施 P18.2：lwext4 在 inode 分配与目录项链接的同一 transaction 内写入最终 mode/uid/gid，普通文件和目录保留原子 `O_EXCL` 创建，并移除创建后的重复 `file_type`/metadata pathname 查询。维护者通过 Docker 生成的 `tmp_11.ans` 显示 `metadata_apply=0`、mode `3.534s/783`、create `27.259s/1182`，但样本仍止于 `Building 28/446`，完整 BuildStorm 和定向文件系统语义回归待后续完成，详见 [problem/buildstorm-create-metadata-transaction.md](./problem/buildstorm-create-metadata-transaction.md)。

`tmp_12.ans` 验证创建后免二次目录扫描：create 平均由约 `23.06ms` 降至 `17.71ms`，约 570 秒 Cargo 进度为 `37/446`。继续让创建 transaction 返回新 inode stat，供 VFS/FsIndex 直接复用以消除创建后的首次 pathname fstat；本轮静态检查通过，Docker 双架构构建和新运行样本待维护者完成，详见 [problem/buildstorm-create-metadata-transaction.md](./problem/buildstorm-create-metadata-transaction.md)。

`tmp_13.ans` 验证 P18.2.2：`regular_no_lookup_stat=0`，新建普通文件不再触发首次 pathname fstat；目录 epoch 仍有 `1238` 次 global miss。实施 P15.2，将目录 rename/rmdir 的源目录、父目录和已缓存替换目标改为局部 epoch，待下一份 Docker 样本验收，详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

## 8.1

分析 `tmp_03/tmp_04`：`tmp_04` 从 `t=822.028s` 的 `52/446` 到 `t=1692.421s` 的 `64/446` 呈现明显后段低吞吐；末尾
lwext4 mount-wide gate 仍有约 22.379 秒持锁和约 194.961 秒并发排队等待。更新《优化方案》：先补子阶段 interval
delta，再在固定环境完成完整 BuildStorm wall-clock 归因；不将不同 Cargo DAG 阶段的 crates/min 直接归因于内核。

修复 RISC-V BuildStorm 文件页缓存达到 96K 页后丢弃 mmap 已加载页的问题：容量旁路不再使新动态程序
SIGSEGV。新增 2,048 页定向回归，修复前等价路径为 `status=139`，修复后输出 case `ok` 与 `shutdown!`；
RISC-V、LoongArch64 release 构建均通过，详见 [problem/file-page-cache-capacity-bypass-mmap-fault.md](./problem/file-page-cache-capacity-bypass-mmap-fault.md)。

将文件页缓存生产上限固定为 192K 页，并在 clean-page CLOCK 满载路径中把 dirty/`in_use` 候选移入延迟队列：active
耗尽后每 256 个容量 miss 才批量复检 16 项。2K RISC-V 压力样本在 `bypass=35318` 时仅扫描 17,232 项、冷却旁路
32,414 次；双架构 perf 构建通过。该 60 秒样本未覆盖旧日志 rustc SIGSEGV 阶段，未宣称该错误已修复，详见
[problem/file-page-cache-eviction-inuse-backoff.md](./problem/file-page-cache-eviction-inuse-backoff.md)。

为 RISC-V BuildStorm 偶发 rustc SIGSEGV 新增默认关闭的缺页/信号诊断：首个 240 秒样本确认 fault 位于已驻留的
`R|X|U` file-backed mmap，而非简单的未映射容量旁路；后续日志还将输出叶子 PTE flags 与页表 token。双架构
diagnostic/perf 构建通过；新字段尚待完整 Cargo 阶段复现，详见
[problem/riscv-rustc-sigsegv-fault-diagnostics.md](./problem/riscv-rustc-sigsegv-fault-diagnostics.md)。

复核新诊断日志后，两个 rustc `FetchInstructionPageFault` 均有 `satp == MemorySet token` 和
`PTE=0x5b (V|R|X|U|A)`，排除未映射、执行权限和错误页表根；定位为 demand PTE 安装/更新后未执行本地
TLB 刷新，重试可能沿用 non-present translation，第二次 fault 才被误投递 SIGSEGV。RISC-V file mmap
read/write 与通用 anonymous lazy-fault 成功路径补 `tlb_invalidate()`，LoongArch file mmap read 路径同步。
双架构 release、RISC-V diagnostics feature 构建和 120 秒 RISC-V 冒烟通过；样本只到
`pre-build tg-xtask`，未进入 Cargo，完整 BuildStorm 稳定性待续，详见
[problem/riscv-rustc-sigsegv-fault-diagnostics.md](./problem/riscv-rustc-sigsegv-fault-diagnostics.md)。

## 8.2

分析 `tmp_03.ans` 发现 BuildStorm 在 gate 队列守恒仍成立时永久停滞，修复发散退出路径可能绕过
`Ext4OpGuard::Drop` 而遗留 `held=true` 的 owner 回收，并增加 owner 诊断字段。详见
[problem/buildstorm-ext4-gate-owner-exit.md](./problem/buildstorm-ext4-gate-owner-exit.md) 和《优化方案》。

针对 `tmp_05.ans` 暴露的写路径重复 gate 交接，在同一 inode 已持有 O_RDWR 描述符且 `known_size` 有效时跳过
`ensure_open()` no-op 的 `EXT4_OP_LOCK` 准入；同时补齐 write/fstat/rename/namespace 阶段 interval delta。双架构
perf/release 构建通过，120 秒 RISC-V 冒烟输出新阶段统计并通过 TOOLCHAIN/MINIBUILD，未完成完整 BuildStorm，详见《优化方案》。

完成 lwext4 SMP P21.1 前置：块设备接口改为位置无关的 `read_at/write_at`，删除共享 cursor，并以设备提交 mutex
保护完整 request 与非对齐 RMW；`EXT4_OP_LOCK` 保持不变。RISC-V/LoongArch64 release 构建通过，bcache 并发和
8 HART LBA 压力 oracle 仍待 P21.2，详见 [problem/lwext4-smp-position-independent-block-device.md](./problem/lwext4-smp-position-independent-block-device.md)。

完成 lwext4 SMP P21.2 并发 bcache 基础设施：增加 index 短锁、同 LBA load single-flight、writeback pin、任务等待
回调和每请求 byte-I/O scratch；仍保留 `EXT4_OP_LOCK`。新日志显示最后窗口约 91.1% gate 准入排队，确认下一轮先补
bcache telemetry、dirty-only 容量退让和并发 oracle，再进入 P21.3 shared read，详见 [problem/lwext4-smp-concurrent-bcache-foundation.md](./problem/lwext4-smp-concurrent-bcache-foundation.md)。

完成 lwext4 SMP P21.2a telemetry：在不改变 `EXT4_OP_LOCK` 语义的前提下，增加 bcache hit/miss、loader/wait、
shake/evict、writeback/驻留、C block callback 和 Rust `Disk::dev` 请求排队/服务统计；C/Rust 在 mount 后统一清零。
新 `log.ans` 的首末完整快照均满足 hit/miss、loader/writeback、驻留和 request 守恒，同时暴露 16-block 目标下
`shake_full_dirty=401664`、`capacity_overflows=181972`、驻留峰值 1243。下一轮只做 256/128 blocks 高低水位的
dirty-only `NEED_EXCLUSIVE` 退让，不同时拆设备锁或 waiter 分桶；双架构 release/perf 构建通过，日志止于 Cargo
`21/446`，完整 BuildStorm、并发 oracle、错误注入和 fsck 仍待执行。

复核 `tmp_11.ans` 发现首个 P21.2b dirty reclaim 在 `ext4_buf_lru_RB_REMOVE_COLOR` 触发 RISC-V
`LoadPageFault`；实验代码已回退到 P21.2a clean-only shake。下一轮先建立 bcache tree/list/refcount 生命周期 oracle
和单一 ownership API，再重新评估默认关闭的 dirty slack，详见
[P21.2 问题记录](./problem/lwext4-smp-concurrent-bcache-foundation.md)。

完成 P21.2b.0/P21.2b.1：新增 bcache 生命周期 oracle 与统一 dirty claim/release，host ASan/UBSan 的顺序、并发、
错误注入和 10 万步随机 trace 全部通过。维护者编译运行的 RISC-V 8 HART `tmp_12.ans` 在截断区间内无 panic/error
且累计守恒，但没有 BuildStorm 完整结束标记；下一步为默认关闭的 256/128 blocks 容量实验，详见 P21.2 问题记录。

完成 P21.2b.2 默认关闭的 dirty bcache 256/128 blocks 容量实验：回收路径复用 claim/pin API，在锁外写回并仅丢弃
最终 clean 的 buffer，失败保留 dirty。默认与实验配置的 host ASan/UBSan lifecycle oracle 均通过；维护者 Docker
编译后的 RISC-V 两分钟样本显示 peak resident 为 256、完成一次 128-block reclaim 且无 I/O error，但未完成
BuildStorm 或 fsck，feature 继续默认关闭，详见 P21.2 问题记录。

## 8.3

修复 EXT4 运行期 Rust 分配器 `free()` 在损坏或不匹配块头上构造非法 `Layout` 导致的 panic，并将内核版 lwext4 切换到显式 `ext4_user_*` 分配接口；Docker 编译已由维护者确认通过，详见 [problem/lwext4-rust-allocator-layout-error.md](./problem/lwext4-rust-allocator-layout-error.md)。

补齐 EXT4 xattr 的 VFS、lwext4 与 12 个 Linux syscall 入口，覆盖 `CREATE/REPLACE`、64 KiB 边界、长度查询、
fd 与末级符号链接语义，并修复 remove 分发和 lwext4 错误分支解锁。RISC-V/LoongArch64 构建及静态检查通过；
当前 final-2026 镜像未携带 xattr LTP 可执行文件，完整动态回归待使用完整测试镜像完成，详见
[problem/ext4-xattr-linux-semantics.md](./problem/ext4-xattr-linux-semantics.md)。

根据 `tmp_13.ans` 将 `EXT4_OP_LOCK` 收缩为 FIFO reader/writer admission：纯读的 data/find/dir/xattr/statfs 路径可
批量 shared，可能写回的 fstat、descriptor rebind 与 metadata fallback 保持 exclusive。RISC-V perf 构建通过；样本只到
Cargo `11/446`，尚未完成完整 BuildStorm、fsck 或 LoongArch64 验证，详见
[problem/ext4-linux-locking-and-lwext4-admission.md](./problem/ext4-linux-locking-and-lwext4-admission.md)。

新的 `server.ans/client.ans` 证明 P21.3 shared admission 会让被抢占的 C rwlock owner 被其它 hart 的裸自旋饿死：恢复
task-aware FIFO 排他 `EXT4_OP_LOCK`、退出 ticket 清理和 truncate 的 journal 嵌套事务判断。RISC-V、LoongArch64 release
构建通过；新 RISC-V 默认 8 HART 运行在 360 秒 timeout 前推进至 Cargo `15/446`、跨过旧约 `2/446` 卡点，但未完成
BuildStorm，详见
[problem/ext4-linux-locking-and-lwext4-admission.md](./problem/ext4-linux-locking-and-lwext4-admission.md)。

当前工作区继续将 lwext4 的 namespace、inode/group stripe、super、journal 与 cache 锁接入 Rust task-aware 资源锁，移除
挂载级 `EXT4_OP_LOCK`，并将 `VFileCache` 的数据访问、flush、快照写回和递归写锁状态分离；最新 `server.ans` 停在
Cargo `8/446`，GDB 现场仅确认 8 个 hart 均在 `wfi`，完整 BuildStorm、死锁根因和写回修复仍待维护者验证，详见
[problem/ext4-linux-locking-and-lwext4-admission.md](./problem/ext4-linux-locking-and-lwext4-admission.md)。

补齐 lwext4 资源锁分类 telemetry，缩小已解析 fd 的 namespace 锁域，并将 cache state 与长写回 flush 分离。`tmp_09.ans`
显示 namespace 累计 wait 为 `1,141,136,319us`；当前 staged 改动尚待完整 BuildStorm、fsck、LTP 与 LoongArch64 运行验收，详见
[problem/buildstorm-ext4-resource-lock-scope-telemetry.md](./problem/buildstorm-ext4-resource-lock-scope-telemetry.md)。

根据 `server.ans/client.ans` 修复调度器定时扫描在 `TID_TO_TASK` 锁内 `Vec::collect()` 触发 `HEAP` 等待的死锁；
BuildStorm 实际停在 Cargo `46/446`。本机 RISC-V perf 构建受缺少 `riscv64-linux-musl-cc` 限制，运行时回归待 Docker 环境完成，详见
[problem/buildstorm-task-timer-snapshot-deadlock.md](./problem/buildstorm-task-timer-snapshot-deadlock.md)。

## 8.4

修复 lwext4 journal 事务清理把共享 depth 误当作当前 task 所有、以及 RISC-V 返回用户态前 `SIE` 可使 S-mode 中断误入 user trap entry 的窗口；详见 problem/ 中对应复盘。

针对 BuildStorm 停在 Cargo `53/446` 的 CMA 现场，用 task-owner 原子锁替换可遗留 ticket 的 `LockedHeap`，并在发散 task exit 时显式回收 owner。`rustfmt` 与 `git diff --check` 通过；RISC-V 构建因宿主缺少 `riscv64-linux-musl-cc` 止于 lwext4 CMake，BuildStorm、LTP、fsck 与跨架构运行尚待验证。

修复 lwext4 journal 环形空间耗尽时 `jbd_journal_alloc_block()` 经 `ext4_assert()` 永久自旋：保留最后一个 journal slot，先强制 checkpoint 并在仍无空间时返回事务错误；同时向上层传播 transaction commit 失败并收紧 dense write-back cache 上限。新的 RISC-V `server.ans` 已推进至 Cargo `117/446`，跨过旧的无输出卡死区间；完整 BuildStorm、最新 bcache/checkpoint 现场和动态链接路径警告仍留待下轮处理，详见 [problem/lwext4-journal-full-buildstorm.md](./problem/lwext4-journal-full-buildstorm.md)。

根据 `server.ans/client.ans` 修复 lwext4 checkpoint 裸 refcount 增量导致的 bcache index-lock 断言自旋，并修复 journal clean-buffer 遍历和 writeback callback 的 `jbd_buf` UAF；同时将 Binutils `/usr/lib/bfd-plugins/` 绝对路径排除出动态库 basename fallback。RISC-V BuildStorm 从 `0/446` 稳定推进到 `131/446`，超过旧 `117/446` 卡点且无 warning/panic，详见 [problem/buildstorm-bcache-checkpoint-refcount.md](./problem/buildstorm-bcache-checkpoint-refcount.md)。

继续修复 journal checkpoint callback 与 close-time bcache flush 并发修改 transaction 链表的问题：增加 live queue 重扫、callback 生命周期保护，并统一 `journal -> cache_flush -> cache` 锁序。当前 RISC-V 日志已从 `431/446` 推进到 `444/446`，尚无对应 panic，但完整 BuildStorm 仍待验证，详见 [problem/buildstorm-journal-cache-flush-serialization.md](./problem/buildstorm-journal-cache-flush-serialization.md)。

## 8.5

修复 LoongArch64 BuildStorm 中 `MAP_FIXED` 提交范围与旧 `PROT_NONE` VMA 重叠导致的 rustc `StorePageFault/SIGSEGV`；修复后短时 QEMU 已越过原 `2/446` 卡点，详见 [problem 复盘](./problem/buildstorm-map-fixed-overlap-sigsegv.md)。

根据当前暂存区补齐 BuildStorm rustc 稳定性记录：双架构 brk 上限提升至 2 GiB、RISC-V 启用 FPU 上下文保存，并将已存在用户页的 load/fetch fault 收敛为一次 TLB 刷新后重试；详见 [problem/riscv-rustc-sigsegv-fault-diagnostics.md](./problem/riscv-rustc-sigsegv-fault-diagnostics.md) 和 [problem/buildstorm-rustc-fpu-and-brk-budget.md](./problem/buildstorm-rustc-fpu-and-brk-budget.md)。同时合并调度选择 perf 计数的原子操作，详见 [problem/scheduler-selection-counter-atomic-overhead.md](./problem/scheduler-selection-counter-atomic-overhead.md)。本轮只补文档和做差异检查，未重新构建或运行 QEMU/BuildStorm。

修复 `server.ans` 暴露的跨进程 epoll fd 注册表冲突：全局索引改用 `EpollFile` 对象身份，并从当前进程 fd 表解析实例，避免 fd 号复用导致 Tokio runtime 初始化返回 `EBADF`。RISC-V 构建、debug 构建通过；修复后的短时 BuildStorm 已进入 `pre-build tg-xtask`，但尚未再次执行正式 runtime，详见 [problem/buildstorm-epoll-registry-fd-reuse.md](./problem/buildstorm-epoll-registry-fd-reuse.md)。

修复持久化镜像中残留 `/proc/<pid>` 目录导致 initproc 以 `EISDIR` panic：先用只读 `O_DIRECTORY` 探测并复用目录，仅在 `ENOENT` 时创建，同时截断并覆盖 `stat/status/maps/pagemap`。双架构构建通过，旧镜像启动已越过 initproc 和 BuildStorm 预构建阶段，详见 [problem/procfs-persistent-task-directory-reuse.md](./problem/procfs-persistent-task-directory-reuse.md)。

修复 BuildStorm 多 reader/writer 并发使用过期 pipe 可用长度导致的 panic，并为 lwext4 `rename()` 补齐普通文件目标覆盖、同 inode no-op 和类型不匹配错误。双架构 release 构建、host rename 覆盖回归及 `e2fsck -fn` 通过；最新 RISC-V 日志已越过旧 `EEXIST`，从 Cargo `440/446` 推进到 `444/446`，遗留的 0 字节 dep-graph warning 正在由 Cargo 自动重建，详见 [pipe 复盘](./problem/pipe-concurrent-io-stale-availability.md) 和 [rename 复盘](./problem/buildstorm-ext4-rename-replace.md)。

修复 BuildStorm 原生动态库搜索的内核 WARN 误报：没有 legacy `/glibc`/`/musl` 映射是正常探测结果，现保留原路径和真实 `open(2)` 语义，仅降为 debug 记录。双架构 release 构建通过，详见 [problem/buildstorm-dynamic-library-probe-warning.md](./problem/buildstorm-dynamic-library-probe-warning.md)。

修复 BuildStorm linker wrapper 被 `.sh` 后缀特判强制交给 BusyBox `sh` 的问题，恢复按 shebang 选择 Bash 或 POSIX shell 的语义。详见 [problem/buildstorm-linker-script-shebang.md](./problem/buildstorm-linker-script-shebang.md)。

将 BuildStorm 并发 exclusive 创建中按 Linux 语义返回的 `EEXIST` 从 lwext4 error 日志降为 debug，保留 errno 和重试语义，详见 [problem/buildstorm-exclusive-create-eexist-log.md](./problem/buildstorm-exclusive-create-eexist-log.md)。

修复 LoongArch trap 汇编只保存 `fcc0`、丢失 `fcc1..fcc7` 导致 BuildStorm rustc 长时间抢占后状态污染的问题，详见 [problem/loongarch-buildstorm-fcc-context-loss.md](./problem/loongarch-buildstorm-fcc-context-loss.md)。

## 8.6

修正 brk 与固定 mmap 的边界：`MAP_FIXED` 不再覆盖 brk VMA，brk 扩展前检查其目标范围是否与其他 VMA 重叠；同时修复 `sys_brk` 地址减法的 `usize` 下溢风险。RISC-V/LoongArch64 release 构建通过。

新增 System V 消息队列 `msgget`、`msgsnd`、`msgrcv`、`msgctl`，覆盖权限、阻塞唤醒、`IPC_RMID`、消息类型选择及主要 Linux flags；RISC-V QEMU 回归通过，RISC-V/LoongArch64 release 构建通过，详见 [problem/sysv-msg-queue.md](./problem/sysv-msg-queue.md)。

修复 LoongArch BuildStorm 中软件页表已具备 U/X 权限却再次发生取指 fault 的路径：补齐 present-PTE 的本地 TLB 失效和一次性重试。双架构构建及 240 秒 LoongArch 前置冒烟通过，详见 [problem/loongarch-present-pte-fetch-retry.md](./problem/loongarch-present-pte-fetch-retry.md)。

实现 RISC-V 共享地址空间的远程 TLB shootdown、线程级 CPU placement 和实际 affinity 语义；随后复核 LoongArch IOCSR IPI 和可返回 kernel trap，接入同一 shootdown 协议并解除 home-hart 限制。双架构 release/perf 构建通过，LoongArch 12 hart 副本镜像启动无 IPI/TLB panic；完整 BuildStorm 仍待验收，详见 [problem/buildstorm-shared-address-space-smp.md](./problem/buildstorm-shared-address-space-smp.md)。

根据最新 BuildStorm GDB 现场补齐 shared-address-space shootdown 的锁等待协议：`UPDATE_LOCK` 与 `MemorySet` read lock 自旋时均轮询 TLB mailbox，ACK 等待改读 live active-hart mask，并让纯 lazy `mmap` 跳过无效广播。RISC-V perf、LoongArch64 release 构建通过；完整运行期 BuildStorm 尚待最新镜像复测，详见 [problem/buildstorm-shared-address-space-smp.md](./problem/buildstorm-shared-address-space-smp.md)。

最新 RISC-V `server.ans` 已在 188 秒推进到 Cargo `443/446`，所有 2654 个 remote TLB target 均已 ACK，8 个 hart 均参与调度；尚无 BuildStorm END，不能作为 axbuild 性能或完整通过结论，详见 [problem/buildstorm-shared-address-space-smp.md](./problem/buildstorm-shared-address-space-smp.md)。

将 CFS 从 per-hart ready queue 改为所有 Hart 共享的最小堆，按全局 `vruntime` 竞争并按线程 affinity 过滤；入队时按就绪任务数唤醒允许运行的 idle Hart。双架构、RR 分支和 RISC-V perf 构建通过，短时 QEMU 已确认 8 个 Hart 在线并进入 BuildStorm，完整吞吐对比仍待完成，详见 [problem/all-hart-cfs-scheduler.md](./problem/all-hart-cfs-scheduler.md)。

修复新 GDB 现场暴露的 futex 队列锁与 remote TLB ACK 等待环：futex 锁竞争主动轮询 mailbox，等待路径用版本化 compare-and-block 检查避免持 futex 锁访问用户内存。RISC-V/LoongArch64 构建和 RISC-V perf 通过，完整 BuildStorm 尚待复测，详见 [problem/buildstorm-futex-remote-tlb-deadlock.md](./problem/buildstorm-futex-remote-tlb-deadlock.md)。

根据新的 `client.ans` 修复 `TID_TO_TASK` 裸自旋不处理 remote TLB ACK 的等待环，并将任务表快照的 `Vec` 分配移到锁外。双架构、RR 和 RISC-V perf 构建通过，完整 BuildStorm 尚待维护者复测，详见 [problem/buildstorm-task-table-remote-tlb-deadlock.md](./problem/buildstorm-task-table-remote-tlb-deadlock.md)。

将任务表、futex 队列和 `UPDATE_LOCK` 的重复 mailbox-aware 自旋统一为 `sync::RemoteTlbMutex<T>`，并覆盖任务退出路径的 futex 访问。RISC-V/LoongArch64、RR 和 perf 构建通过，详见 [problem/buildstorm-task-table-remote-tlb-deadlock.md](./problem/buildstorm-task-table-remote-tlb-deadlock.md)。

根据 `server.ans/client.ans` 修复 all-hart CFS 取任务与入队之间的状态竞争，避免同一 TCB 被多个 Hart 重复调度并触发 page fault panic；隔离 RISC-V 运行已进入 BuildStorm toolchain 且无 panic，详见 [problem/buildstorm-cfs-dispatch-panic.md](./problem/buildstorm-cfs-dispatch-panic.md)。

修复 BuildStorm vfork 父线程跨调度保留子 TCB 强引用的问题：`sys_clone()` 入队后立即释放 `new_task`，短时 QEMU 已确认 `strong_count` 告警消失；完整 BuildStorm 仍待复测，详见 [problem/buildstorm-vfork-tcb-arc-lifetime.md](./problem/buildstorm-vfork-tcb-arc-lifetime.md)。

## 8.7

按《优化方案》补齐 P0 的 mmap downward hint 和 P1 的 rseq pending 事件驱动/批量用户访问，并同步调整 rseq regression 对无调度 syscall 的语义断言。RISC-V/LoongArch64 release 构建通过；P1 运行验证按要求暂未执行，详见 [problem/buildstorm-riscv-vma-hole-search-sigsegv.md](./problem/buildstorm-riscv-vma-hole-search-sigsegv.md)。

按 Linux `task_struct::on_cpu` 协议补齐阻塞唤醒与上下文切出的 SMP 交接，不增加额外任务状态；新的 RISC-V 日志已越过原 `440/446` panic 点并推进到 `444/446: axbuild`，8 个 Hart 均有调度记录。`axbuild` 仅单 Hart 活跃是非计时预构建到达依赖图尾部，不是 CFS affinity 回退；完整 446/446 仍待继续运行，详见 [problem/buildstorm-cfs-dispatch-panic.md](./problem/buildstorm-cfs-dispatch-panic.md)。

修复损坏 EXT4 目录项的 `rec_len == 0` 使 lwext4 目录插入永久循环，以及线性目录/HTree cleanup 覆盖插入错误的问题。GDB 与只读 `e2fsck` 确认首个新 `EIO` 来自原镜像中整块为零的 `/proc/67` 目录和损坏 journal；离线修复的镜像副本已推进到 Cargo `443/446` 且不再出现该错误，完整 BuildStorm 尚未完成，详见 [problem/buildstorm-ext4-zero-dir-entry.md](./problem/buildstorm-ext4-zero-dir-entry.md)。

根据 `server.ans/client.ans` 修复 fork 固定栈复制持有父 `MemorySet` read guard 再等待
`UPDATE_LOCK` 的 AB-BA 死锁；跨地址空间复制改为先快照源 frame `Arc`，再获取目标 MM 锁，
并在 task/MM 模块顶部补齐锁序。双架构 release 构建通过，RISC-V 从旧 `440/446` 卡点推进到
`444/446` 并进入正式 BuildStorm，后续受独立 ext4 `EIO` 阻断，详见
[problem/buildstorm-shared-address-space-smp.md](./problem/buildstorm-shared-address-space-smp.md)。

闭环 `server.ans` 中 `axbuild` artifact 写入 `EIO`：只读 fsck 和 `debugfs` 确认实际 RISC-V 持久化镜像的损坏 inode 正是报错 `.rmeta`，备份后离线修复原镜像。修复后回归通过 toolchain/minibuild 并从 `444/446: axbuild` 推进到 `445/446: tg-xtask(bin)`，30 分钟内无 `EIO`/panic/Cargo error，但未完成测试组；强制终止后已再次 fsck 并确认镜像五阶段清洁。详见 [problem/buildstorm-ext4-zero-dir-entry.md](./problem/buildstorm-ext4-zero-dir-entry.md)。

根据 `server.ans/client.ans` 将 remote TLB 更新前的旧帧保留从全驻留集收缩到实际可能
解除映射或替换的 VPN 范围，消除 rustc 频繁 `brk/munmap` 的全量扫描。双架构 release
构建通过；RISC-V 单次运行中 `axbuild` 从旧基线约 12 分钟缩短到约 8 分钟，并推进至
`445/446: tg-xtask(bin)`，但 15 分钟内未完成完整 BuildStorm，详见
[problem/buildstorm-memoryset-full-resident-retention.md](./problem/buildstorm-memoryset-full-resident-retention.md)。

修复 `ppoll` 临时信号掩码 guard 在不展开内核栈的致命退出路径中泄露 TCB 强引用：guard
改持 `Weak<TaskControlBlock>`，正常返回仍恢复旧掩码。双架构 release 构建通过；180 秒
RISC-V BuildStorm snapshot 推进到 `444/446: axbuild`，原三条 `extra TCB refs` 未再出现，
详见 [problem/buildstorm-ppoll-tcb-reference-leak.md](./problem/buildstorm-ppoll-tcb-reference-leak.md)。

修复多 Hart 线程组并发退出时兄弟 TCB `Arc` 集合造成的临时强引用放大：退出路径改为
`Weak` 快照加逐项短生命周期 `upgrade()`，详见
[problem/buildstorm-tcb-sibling-exit-arc-amplification.md](./problem/buildstorm-tcb-sibling-exit-arc-amplification.md)。

实现 P1 rseq 事件驱动和批量用户访问：普通用户返回跳过无事件的 rseq 用户内存访问，调度/迁核/信号事件按需更新；该优化随后在真实 BuildStorm 场景中暂不保留，回归分析详见 [problem/buildstorm-riscv-vma-hole-search-sigsegv.md](./problem/buildstorm-riscv-vma-hole-search-sigsegv.md)。

修复 `server.ans` 暴露的 RISC-V rustc SIGSEGV：保留 VMA 排序/合并与线性空洞搜索，但恢复碰撞后向下保留一页的边界，180 秒运行越过原 `440/446` 崩溃点至 `444/446`，详见 [problem/buildstorm-riscv-vma-hole-search-sigsegv.md](./problem/buildstorm-riscv-vma-hole-search-sigsegv.md)。

补充修复 `f9dc0703` 引入的 mremap VMA 拆分索引失效：中间拆分阶段暂不合并相邻匿名私有 VMA，并同步调整 `old_idx`；RISC-V、LoongArch64 release 构建通过，详见同一问题复盘。

修复 `server.ans` 中 rseq 回归的假失败：以必然切出父任务的 `vfork`/`exit` 替换可能同步完成的 `sleep(1)`，RISC-V QEMU 输出 `rseq regression: PASS` 并进入 BuildStorm toolchain/minibuild，详见 [problem/rseq-regression-schedule-trigger.md](./problem/rseq-regression-schedule-trigger.md)。

按《优化方案》实现 P2 的受限当前地址空间 uaccess fast path：RISC-V 通过 SUM RAII 做短小直访，LoongArch64 通过分页模式与 PTE 条件做同类直访，未命中仍回退原软件翻译路径。`make TARGET_ARCH=riscv64` 与 `make TARGET_ARCH=loongarch64` 通过；`server.ans` 新日志已推进到 `445/446: tg-xtask(bin)`，`sigaltstack` 与 `rseq` 仍为 PASS。

## 8.8

修复 P3 fault-safe uaccess 在缺页处理后递归获取 `TaskControlBlockInner` 锁导致的自锁：
重试 VPN 改为每 Hart 的原子状态。双架构 release 构建通过；修复后的 `server.ans` 已进入
BuildStorm 正式阶段且无死锁，详见 [problem/buildstorm-uaccess-fault-task-lock-deadlock.md](./problem/buildstorm-uaccess-fault-task-lock-deadlock.md)。

修复 RISC-V uaccess 同步 fault 中执行可阻塞缺页处理导致 trap CSR 随任务迁移失配并触发取指 panic：缺页/COW/文件页改由 copy fixup 回退软件路径，保留 present-PTE 的本地 TLB 重试。RISC-V overlay 已通过 CAgent 并推进 BuildStorm 至 `443/446`，详见 [problem/buildstorm-uaccess-trap-frame-migration-panic.md](./problem/buildstorm-uaccess-trap-frame-migration-panic.md)。

修复 libctest 动态 ELF 全部以 255 退出的问题：恢复主程序和动态解释器 `PT_LOAD` 段的 eager framed 映射。RISC-V 动态测试全部通过，详见 [problem/libctest-dynamic-elf-eager-load.md](./problem/libctest-dynamic-elf-eager-load.md)。

修复 `ed27f13...` 将 eager 策略扩大到 BuildStorm 主 ELF 的回归：动态解释器保留 eager 映射，主程序恢复页对齐 file-backed lazy 映射。双架构 release 构建通过；新的 BuildStorm/LTP 运行时回归待维护者执行，详见 [problem/buildstorm-main-elf-eager-mapping-regression.md](./problem/buildstorm-main-elf-eager-mapping-regression.md)。

修复 `server.ans` 中 BuildStorm 跨进程清理增量缓存后 `fstat` 返回 `ENOENT`：按 VFS inode 对象统计全局 `OSFile` 打开引用，仍有其他进程持有时改为延迟删除；新增跨进程 unlink/fstat 回归，未修改 ext4/lwext4 核心。短时 RISC-V QEMU 已通过该回归、sigaltstack、rseq 和全部 CAgent，完整 BuildStorm 待后续确认，详见 [problem/buildstorm-cross-process-unlink-fstat.md](./problem/buildstorm-cross-process-unlink-fstat.md)。

兼容评测机固定的 RISC-V QEMU CPU 参数：在用户态非法指令路径精确模拟嵌套 QEMU 所需的 Zicond `czero.eqz`/`czero.nez`，其他非法指令仍按原语义终止；嵌套 RISC-V QEMU 入口同时限制为 RISC-V，避免 LoongArch64 final 镜像执行错误 ISA 的用户程序。双架构 release 构建和 LoongArch64 final 启动验证通过，详见 [problem/nested-qemu-zicond-illegal-instruction.md](./problem/nested-qemu-zicond-illegal-instruction.md)。

根据 final-2026 `sdcard-la.img` 中的 BuildStorm 启动脚本，为 LoongArch64 增加 EFI 形式的嵌套 `arceos-helloworld` QEMU 启动流程：复制 `.bin` 为 `BOOTLOONGARCH64.EFI`，配置 EDK2 pflash 和 FAT ESP。详见 [problem/nested-loongarch-qemu-boot.md](./problem/nested-loongarch-qemu-boot.md)。

实现 RISC-V `riscv_flush_icache(259)`：接入 syscall 分发和用户态封装，校验 flags 后复用架构层 `fence.i`；RISC-V 与 LoongArch64 release 构建通过。详见 [problem/riscv-flush-icache-259.md](./problem/riscv-flush-icache-259.md)。

实现 `prctl(PR_MCE_KILL, 33)`：补齐线程级 MCE kill policy 的 SET/CLEAR/GET 语义，并在 fork/clone 时继承；RISC-V 与 LoongArch64 release 构建通过。

实现 `prctl(PR_SET_TIMERSLACK, 29)` 与 `PR_GET_TIMERSLACK`：增加线程级 timer slack 状态，默认 50us，支持设置和通过 0 恢复默认值；双架构 release 构建通过。

实现 `memfd_create(279)`：校验名称和 flags，创建可读写的匿名内存文件 fd，并支持 `MFD_CLOEXEC`；复用现有 `TmpFile` 文件对象，双架构 release 构建通过。

实现 `io_uring_setup(425)` 的基础 ABI 路径：校验 entries、用户参数地址和 setup flags，回写确定的 ring 参数并返回独立的 io_uring fd；完整 SQ/CQ 提交执行和 mmap ring 尚未接入。

修复嵌套 RISC-V QEMU 的 OpenSBI 固件查找：在公共 VFS `open()` 中为该 QEMU 进程的只读固件探测增加精确路径回退，覆盖 `access/stat/open` 解析而不修改 QEMU 启动参数。RISC-V/LoongArch64 release 构建通过，详见 [problem/nested-qemu-opensbi-firmware-lookup.md](./problem/nested-qemu-opensbi-firmware-lookup.md)。

按《优化方案》实现 P0-A：新增动态只读 `/proc/uptime`，使用架构单调 tick 和所有 Hart idle 累计时间生成 `uptime idle\n`，支持小读、EOF、`lseek` 和回归读取。RISC-V/LoongArch64 release 构建通过，RISC-V QEMU 内置 uptime 回归 PASS，详见 [problem/buildstorm-proc-uptime.md](./problem/buildstorm-proc-uptime.md)。

修复嵌套 RISC-V QEMU 的 Zicond 模拟条件反转：`czero.eqz/nez` 现在按规范在条件成立时写零；固定参数下 RISC-V final 测例已连续输出 `Hello, world!`，详见 [problem/nested-qemu-zicond-emulation-semantics.md](./problem/nested-qemu-zicond-emulation-semantics.md)。

## 8.9

纠正最小化复验中的旧产物误判：除 Zicond 条件修复外，stdin 必须按实际输入报告 `POLLIN`，eventfd 必须过滤未请求事件；移除 MM、madvise 和进程级 icache 尝试后，强制重编译的 RISC-V final 测例仍正常输出 `Hello, world!`。详见 [problem/nested-qemu-zicond-emulation-semantics.md](./problem/nested-qemu-zicond-emulation-semantics.md)。

实现《优化方案》P1：将批量 VMA/PTE 更新的重复内层 TLB 刷新收敛到一次统一 shootdown，并新增仅在 `perf` 特性下编译的调用来源、mailbox wait 与 ACK latency 统计。RISC-V/LoongArch64 release/perf 构建和 RISC-V 8 Hart 启动回归通过，详见 [problem/buildstorm-remote-tlb-batching.md](./problem/buildstorm-remote-tlb-batching.md)。

优化 cagent kernel 用例的 exec 路径：动态解释器/未对齐主程序段读取复用内核文件页缓存，重复 exec 不再重走 lwext4；loongarch64 批量页表映射收敛为一次 `invtlb`。双架构 release/debug 构建和两次 loongarch64 QEMU 回归 PASS，详见 [problem/loongarch-exec-segment-read-cache.md](./problem/loongarch-exec-segment-read-cache.md)。

定位 LoongArch CAgent 十项任务耗时过长的主因是 idle hart 未可靠响应 scheduler IPI，12-hart CFS 实际退化为单 hart。保留 8 个有界 polling hart 后，两次 CAgent wall-clock 稳定为 5.20/5.19 秒，相比原始约 11.37 秒降低约 54%，十项均 PASS；详见 [problem/loongarch-cagent-idle-polling.md](./problem/loongarch-cagent-idle-polling.md)。

修复 BuildStorm 并发创建 Rust 子进程时 fd 表空槽重复分配导致的 `CLOEXEC pipe`/`ENOTSOCK`：为 `alloc_fd` 增加 fd-table 级 reservation。双架构 release 构建、RISC-V 四项启动回归和 CAgent 均通过，详见 [problem/buildstorm-fd-allocation-reservation-race.md](./problem/buildstorm-fd-allocation-reservation-race.md)。

修复 LoongArch CAgent 中主 ELF 部分文件末页错误映射到 `.bss`，导致 `/usr/bin/find` 将 `".rodata"` 解引用为指针的 ADEM/SIGSEGV；同时恢复用户 TrapContext 的 `a0`/`PRMD` 保存并关闭返回用户态前的中断窗口。12-hart CAgent 十项、BuildStorm toolchain/minibuild 冒烟和双架构 release 构建通过，详见 [problem/elf-partial-page-bss-leak.md](./problem/elf-partial-page-bss-leak.md)。

根据 `server.ans/client.ans` 将 remote TLB shootdown 由逐目标“唤醒后立即等 ACK”改为先完整广播 IPI、再收集 ACK，消除同次多 Hart 失效的发送侧串行等待；保持 `UPDATE_LOCK`、旧帧 ACK 后释放与内存序不变。RISC-V/LoongArch64 release/perf 构建通过，完整 BuildStorm A/B 尚未完成，详见 [problem/buildstorm-remote-tlb-broadcast.md](./problem/buildstorm-remote-tlb-broadcast.md)。

将 future timer wheel、futex timeout heap 和 blocked-task timer 扫描收敛为每个 10 ms bucket 的单 Hart 维护，消除多 Hart 同时自旋 `TIMER_RUNTIME` 与重复任务表扫描；两次 LoongArch64 CAgent 均通过，详见 [problem/buildstorm-global-timer-maintenance.md](./problem/buildstorm-global-timer-maintenance.md)。

修复 BuildStorm COW 路径在引用计数判断前保活旧 frame，导致独占页被强制复制并发起 remote-TLB ACK；仅共享 COW 保留完整同步协议。双架构 perf 构建和 LoongArch64 CAgent/BuildStorm toolchain 冒烟通过，详见 [problem/buildstorm-cow-exclusive-frame-promotion.md](./problem/buildstorm-cow-exclusive-frame-promotion.md)。

最新 BuildStorm 日志确认 LoongArch64 虽启动 12 个 Hart，旧 8-Hart polling cohort 却让 Hart 8--11 在 QEMU `idle` 中无法可靠被 scheduler IPI 唤醒；扩展到全 Hart 轮询后 CAgent 的调度统计已覆盖全部 12 核。详见 [problem/loongarch-buildstorm-polling-cohort.md](./problem/loongarch-buildstorm-polling-cohort.md)。

勘误 LoongArch QEMU TCG 的 `idle 0` 语义：已使能的 pending IPI 会唤醒 halted vCPU 并进入中断入口，删除此前基于相反判断的全 Hart polling。LoongArch64 release 构建通过；详见 [problem/loongarch-qemu-tcg-idle-ipi.md](./problem/loongarch-qemu-tcg-idle-ipi.md)。

根据 `server.ans` 中 `96.4%` sparse buffer batch 命中 run 上限的统计，将 EXT4 稀疏写缓冲改为合并连续/重叠的连通 run，并按实际驻留字节更新预算；保持 hole 与 last-write-wins 语义。新增纯内存回归覆盖但未运行构建、QEMU 或测试，避免干扰正在进行的全量测试；详见 [problem/buildstorm-sparse-write-run-coalescing.md](./problem/buildstorm-sparse-write-run-coalescing.md)。

按《优化方案》实现 P0 分解计数：将 block request 计时 helper 移入 `utils/perf`，补充设备请求形态、bcache completion wait 和 BuildStorm 阶段 marker；不改变 EXT4 锁、写回或请求合并语义。本轮未运行验证，详见 [problem/buildstorm-p0-perf-baseline-telemetry.md](./problem/buildstorm-p0-perf-baseline-telemetry.md)。

完善 P0：以任务级 resource-lock 上下文关联 `lock_class × read/write/flush` 的提交、队列等待与设备服务，将 completion wait 分为持锁/解锁，连续性改按真实串行设备顺序计数。为避免 C 侧 telemetry，未增加 journal commit 计数；现有 C bcache telemetry 仅在 perf feature 编译。双架构 release/perf 构建均通过；QEMU 未运行以保留维护者的 `disk.img` 链接，详见 [problem/buildstorm-p0-perf-baseline-telemetry.md](./problem/buildstorm-p0-perf-baseline-telemetry.md)。

修复 LoongArch64 信号处理函数返回至 `0xfffffffffffe4c44` 时的未映射取指页故障：按
signal-vDSO `rt_sigreturn` 页内偏移映射现有私有 trampoline。双架构构建和 180 秒 LoongArch
QEMU 冒烟通过；完整 BuildStorm 长程窗口尚待回归，详见 [problem/loongarch-signal-vdso-sigreturn.md](./problem/loongarch-signal-vdso-sigreturn.md)。
后续勘误：该入口语义判断先被 signal provenance 日志推翻；更晚的 libc 合法 `bl` 运行时
异常进一步证明根因是跨 Hart 指令流同步缺失。本条仅保留第一阶段诊断时间线，最终结论见
8.10 的后续勘误和对应 problem 文档。

按 `server.ans` 与 10 分钟 `log.ans` 实现 BuildStorm P2 的连续 bcache 写回合并：仅合并无
journal callback 的相邻 dirty 块，失败保留 dirty 状态。生命周期回归、RISC-V64/LoongArch64
release 和 LoongArch64 perf 构建通过；为保留维护者的 `disk.img`，未执行 QEMU，详见
[problem/buildstorm-bcache-contiguous-writeback.md](./problem/buildstorm-bcache-contiguous-writeback.md)。

## 8.10

纠正此前将 `0xfffffffffffe4c44` 误判为 Linux 固定 `rt_sigreturn` 的实现：新日志和镜像内
glibc 2.36 反汇编证明它是需要返回的普通 helper 调用。恢复独立 signal restorer，并保留该
地址为返回 `-ENOSYS` 的兼容入口；双架构和 LoongArch fault-diagnostics 构建通过，运行回归
由维护者执行。详见 [problem/loongarch-signal-vdso-sigreturn.md](./problem/loongarch-signal-vdso-sigreturn.md)。

后续勘误：新的 BuildStorm fault 显示 `0x2a173021c4` 与 `0x2a17304418` 在实际 libc ELF
中均为合法 `bl`，却在运行时被报告为非法指令或产生非 canonical 访问。最终根因是地址空间
重新进入某 Hart 时只激活页表、未执行本地 `ibar`；此前“普通 helper”只能证明间接调用的
返回点，不能证明目标地址合法。现已在 `MemorySet::activate_for_user()` 的 Hart 首次发布
边界同步指令流，并删除 `0xfffffffffffe4c44` 的 `-ENOSYS` 假兼容映射。双架构 release 与
LoongArch fault-diagnostics 构建通过；行为回归仍由维护者执行。

修复嵌套 RISC-V QEMU 文件私有映射经 `mprotect` 切分后后半 VMA 未平移文件偏移的问题：
错误页把 `0xc00000ff` 当 CSR 回调，`jalr` 跳至 `0xc00000fe`。保持手工动态加载器配置，
新的 `log.ans` 已输出嵌套 guest 的 `Hello, world!` 和 `shutdown!`；详见
[problem/nested-qemu-file-mmap-vma-split-offset.md](./problem/nested-qemu-file-mmap-vma-split-offset.md)。

## 8.12

重写动态链接兼容层，删除竞赛镜像路径、工具链版本、basename fallback 和 libc 固定偏移机器码补丁；`open/openat` 与 `PT_INTERP` 装载恢复 Linux 精确路径及 `ENOENT` 语义。详见 [problem/dynamic-link-linux-vfs-boundary.md](./problem/dynamic-link-linux-vfs-boundary.md)。

依据近月提交同步 Ya2yOS 既有设计文档，补充共享地址空间、文件系统并发、动态 ELF 与性能证据边界；生成 12 页答辩 PDF/PPTX，完整 BuildStorm 成绩仍待官方 `ok=true elapsed_s` 对照。

## 8.11

针对 `log.ans` 中三个并发 BuildStorm 子进程在 `0xfffffffffffe4c44` 的取指 fault，补强 LoongArch 每次返回用户态前的 `ibar 0`，覆盖同一地址空间已在其他 Hart 活跃时的指令流失同步；同时统一 signal restorer 地址 API，修复 RISC-V 构建中的遗留符号引用。双架构 release 与 LoongArch `fault-diagnostics` 构建通过；QEMU 因沙箱 `/var/tmp` 只读未能启动，详见 [problem/loongarch-signal-vdso-sigreturn.md](./problem/loongarch-signal-vdso-sigreturn.md)。

纠正 LoongArch clang 取指异常的根因：native GLIBC 2.41 loader 被动态库 basename 兼容层与 `/glibc` GLIBC 2.38 libraries 混用，错误 `_rtld_global_ro` 槽最终跳到 `0xfffffffffffe4c44`。按当前 executable provenance 限制 legacy fallback；1850 次 clang 压力通过，详见 [problem/loongarch-signal-vdso-sigreturn.md](./problem/loongarch-signal-vdso-sigreturn.md)。

汇总近一个月 BuildStorm 的工具链、SMP/CFS、MemorySet/remote-TLB、页缓存/EXT4 与 bcache 写回修改，新增人工评审用 Typst 优化实现文档；明确 `axbuild` 定向观测约 1.50x，完整 446 crate A/B 尚待以官方 `elapsed_s` 标记复测。详见 [buildstorm-优化实现文档.typ](./buildstorm-优化实现文档.typ)。

## 8.12

新增启动期硬件配置：RISC-V 通过 SBI 传入的 FDT 自动读取 RAM、CPU 数量和 timebase-frequency，内核内存映射、CMA、调度核掩码与时钟改用探测值；LoongArch 保留本地定时器/平台默认值。详见 AI 记录。

新增 SD 卡运行时配置：启动期在根 ext4 就绪后可读取 `/etc/ya2yos.conf`，调整 pipe 最大容量和小页缓存水位/批量；非法配置回退默认值且不影响硬件探测。RISC-V/LoongArch64 release 构建通过，详见 AI 记录。

移除两架构 `memory_layout.rs` 的 RAM 起址、容量和分段表硬编码：RISC-V 使用 SBI FDT，LoongArch 从启动器 EFI system table 的 Device Tree table 解析全部 RAM 段，CMA 随之改用探测段。双架构 release 构建通过；QEMU 因当前 `/var/tmp` 只读未能启动，详见 [problem/bootloader-discovered-ram-layout.md](./problem/bootloader-discovered-ram-layout.md)。

删除两架构 QEMU `config.rs` 的固定 `HART_NUM`；启动器 FDT 的 CPU 数现在驱动启动、调度、IPI、affinity 与 `/proc/cpuinfo`，静态每 Hart 状态和启动栈仅保留 `MAX_SUPPORTED_HARTS=16` 容量。RISC-V/LoongArch64 release 构建通过。

修复 LoongArch QEMU `idle 0` 对外部 `ECFG.LIE` 状态的依赖：关闭 `CRMD.IE` 后立即重建 timer/IPI 本地中断掩码，返回后撤销 IPI 使能，结构与 RISC-V `wfi` 一致；LoongArch64 release 构建通过，详见 [problem/loongarch-qemu-tcg-idle-ipi.md](./problem/loongarch-qemu-tcg-idle-ipi.md)。

## 8.13

修复 LoongArch 用户页表 DMW 内核分支泄漏及空目录项被 refill 当作物理页 0 继续 walk 的问题；空分支现在落到零叶 PTE，避免伪造 `boot_stack` PLV0 映射和 PPI 异常循环。RISC-V/LoongArch64 release 构建通过，详见 [problem/loongarch-user-pagetable-directmap-alias.md](./problem/loongarch-user-pagetable-directmap-alias.md)。

根据单核 BuildStorm 的 `srfill` GDB 现场，收敛 LoongArch trap 进入、返回和同根页表激活的无条件全 TLB 失效；保留所有实际 PTE 更新与地址空间切换的显式失效。RISC-V/LoongArch64 release 构建通过，当前 GDB/QEMU 现场未重启，详见 [problem/loongarch-buildstorm-trap-tlb-thrash.md](./problem/loongarch-buildstorm-trap-tlb-thrash.md)。

新增 VisionFive 2 板级启动基础：RISC-V 内核改用 `0x40000000` 恒等映射与
`0xffffffc040200000` 高地址链接，接入 JH7110 NS16550 UART 和板级 MMIO 映射，并新增
`TARGET_ARCH=visionfive2` 构建入口。该历史入口现已迁移为
`TARGET_ARCH=riscv64 PLATFORM=visionfive2`。板级 release 构建通过；GMAC/SDIO 完整驱动待补齐
JH7110 时钟、PHY、DMA 与 `dw_sd` 依赖，详见 [visionfive2-board-bringup.md](./problem/visionfive2-board-bringup.md)。

## 8.14

修复嵌套 LoongArch64 QEMU 启动：ELF auxv 按 CPUCFG1.UAL 声明 `HWCAP_LOONGARCH_UAL`，并将其 2 GiB guest-RAM 加运行库映射纳入 4 GiB 懒 `mmap` 配额。双架构 release 构建通过，LoongArch64 运行已进入内层 OVMF；详见 [problem/nested-loongarch-qemu-boot.md](./problem/nested-loongarch-qemu-boot.md)。

修复 12 hart 嵌套 LoongArch64 QEMU 的 futex wake/requeue waiter 丢失竞态；保持默认 all-hart affinity，内层 guest 已输出 `Hello, world!` 和 `shutdown!`。详见 [problem/nested-loongarch-qemu-smp-futex-requeue.md](./problem/nested-loongarch-qemu-smp-futex-requeue.md)。

## 8.15

修复 `FUTEX_REQUEUE` 返回值把重排 waiter 错计为已唤醒，导致 BuildStorm 条件变量计数失配并死锁的问题；详见 [problem/buildstorm-futex-requeue-return-count.md](./problem/buildstorm-futex-requeue-return-count.md)。

修复 Docker QEMU 10 在 16 GiB guest RAM 下将 SBI FDT 放到高地址、而 RISC-V 启动页表未映射该页导致的启动异常循环；Docker QEMU 10.0.2 已完成内核启动、全部 cagent 并进入 BuildStorm，详见 [problem/riscv-qemu10-high-fdt-bootstrap-map.md](./problem/riscv-qemu10-high-fdt-bootstrap-map.md)。

完成 BuildStorm P1-A：当前任务调度记账重入队改用不发送远程通知的专用路径，保留外部 wake-up 和 affinity 失败路径通知，并加入两项聚合计数；RISC-V/LoongArch64 CFS、RISC-V RR 构建及 120 秒 perf 观测通过，详见 [problem/buildstorm-p1a-current-requeue-ipi.md](./problem/buildstorm-p1a-current-requeue-ipi.md)。

重构设备模块目录：通用设备接口、磁盘适配、网络缓冲区和 VirtIO 实现留在 `drivers/`，RISC-V MMIO 与 LoongArch PCI VirtIO transport 移入 `arch/<arch>/drivers/`；RISC-V64/LoongArch64 release 构建通过。

新增 VisionFive 2 JH7110 GMAC1 轮询驱动：以 Linux `dwmac-starfive`/`stmmac` 的寄存器和描述符语义为准，使用 CMA DMA 环接入现有网络栈；板级及双架构 release 构建通过，实板网络验证待执行。详见 [visionfive2-dwmac-driver.md](./problem/visionfive2-dwmac-driver.md)。

补充 Loongson 2K1000 板级支持：新增 UART/PMC 关机、高端 DRAM 链接、双核 EFI-FDT 启动和轮询 AHCI 根盘，构建输出 U-Boot 原始镜像；2K1000、普通 LoongArch64 与 RISC-V64 release 构建通过。详见 [loongson-2k1000-board-ahci.md](./problem/loongson-2k1000-board-ahci.md)。

补充 2K1000 GMAC0 的 DWMAC 3.70a 单队列轮询驱动，依据 Linux `dwmac-loongson` 与实板固件 FDT
使用 `0x40040000`、PHY 0 和正常 DMA 描述符；2K1000 release 构建通过，实体板链路与收发待验。详见 [loongson-2k1000-board-ahci.md](./problem/loongson-2k1000-board-ahci.md)。

统一 RISC-V 板级构建入口：VisionFive 2 不再伪装为 `TARGET_ARCH`，改由
`make build-arch TARGET_ARCH=riscv64 PLATFORM=visionfive2` 选择；QEMU 保持默认 `PLATFORM=qemu`。
详见 [riscv-platform-build-unification.md](./problem/riscv-platform-build-unification.md)。

补充 `initproc` 的镜像内容探测：决赛镜像优先识别 `/work/tgoskits` 或
`/glibc/cagent_testcode.sh`，初赛镜像识别 musl/glibc 基础脚本，自动选择对应测例入口；未知镜像拒绝运行，避免手动切换源码。
RISC-V release 构建通过，QEMU 冒烟因宿主 `/var/tmp` 只读未启动。
