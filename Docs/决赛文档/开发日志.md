## 7.2

重构 PCB 与地址空间锁边界：将自带同步的 `MemorySet`、`SigTable`、`FdTable`、`FSInfo` 移出旧 `ProcessInner`，`MemorySet` 内部持有 `RwLock<MemorySetInner>`，`personality` 并入 `ProcessMeta`。

继续重构 PCB 访问接口：移除 `Process::inner_lock()` 和 `ProcessResources` 兼容层，调用点直接通过 `Process` 访问地址空间、信号表、fd 表和 fs 上下文；`make` 通过，QEMU 运行验证本轮未完成。

新增 `ResourceSlot<T>` 资源槽抽象，将 `Process.memory_set` 与 `Process.sig_table` 改为私有可替换槽位，明确区分资源内部同步和进程资源指针替换；`make` 通过，LoongArch64 `make run` 基础测试跑到 `shutdown!`。

继续收敛 PCB 地址空间接口：删除 `get_locked_memory_set_read/write()` 兼容方法，调用点统一使用 `memory_set_arc()`；`make` 通过，LoongArch64 `make run` 基础测试跑到 `shutdown!`。

收敛 pipe 文件描述符分类：`pipe2()` 分配的匿名 pipe 统一存入 `FileClass::Pipe`，`vmsplice` 改为通过 pipe 类型入口校验和写入；默认 LoongArch64 `make` 通过。

实现 `splice(2)` 兼容搬运路径：支持 regular file 与 pipe、pipe 与 regular file、pipe 与 pipe 之间通过内核缓冲区传输，补齐 offset 写回、pipe offset `ESPIPE`、`O_APPEND` 输出 `EINVAL`、`SPLICE_F_NONBLOCK` 基础语义；LoongArch64/RISC-V 构建通过，LoongArch64 `splice01` musl/glibc 均 `TPASS`。详见 problem/。

优化 pipe 内部缓冲结构：将 `PipeRingBuffer` 从字节环改为 `PipeBuf` 片段队列，`splice(pipe, pipe)` 走片段引用移动，`tee()` 走片段引用复制；默认 LoongArch64 `make` 通过。当前 `initproc` 中的 `splice05` 仍暴露 socket splice `ENOTCONN` 语义缺口，需后续单独处理。

接入文件页面缓存到 `MAP_SHARED` mmap 与 `splice(file, pipe)`：共享文件缺页改由 `FILE_PAGE_CACHE` 提供 `FrameTracker`，`PipeBuf` 支持挂载 `FilePage`，普通文件到 pipe 的 `splice` 走缓存页引用 fast path；默认 LoongArch64 `make` 通过，当前 `splice09` 入口因内核版本要求 `TCONF/skipped` 并跑到 `shutdown!`。

修复 pipe LTP 语义：无读端写入 pipe 时先投递 `SIGPIPE` 再返回 `EPIPE`，并实现 `ioctl(FIONREAD)` 返回 pipe 当前可读字节数；默认 LoongArch64 `make` 通过，当前 `pipe02`/`pipe08`/`pipe12` 核心断言均 `TPASS`。详见 problem/。

重构 pipe 源码布局：将原 `os/src/fs/files/pipe.rs` 按功能拆分到 `os/src/fs/files/pipe/`，分别承载片段缓冲、共享 ring buffer、FIFO 表、splice/tee、阻塞等待和 `File` trait 实现；对外 `Pipe`、`make_pipe()`、`open_fifo()` 路径保持不变。默认 LoongArch64 `make` 通过。

## 7.3

将 `FsIndex` 从路径强引用表改为 VFS inode cache：使用 `(st_dev, st_ino)` 归并 inode 身份，路径索引只保存 cache key，inode cache 使用 `Weak` 避免缓存永久持有对象；`Ext4Inode` 增加路径别名以兼容 hard link / rename 后的 path-based lwext4 操作。RISC-V 与 LoongArch64 构建通过，RISC-V QEMU 交互验证 hard link、unlink 原路径、rename 后读取均正常。

修复 RISC-V Alpine 镜像启动期仍按竞赛测试镜像布局写 `/glibc/lib/libgcc_s.so.1` 和覆盖 `/bin/sh -> /musl/busybox` 的问题；动态解释器加载增加精确路径 `open_direct()` 回退，并将解释器加载失败作为 exec 错误传播。`make TARGET_ARCH=riscv64` 通过，25 秒 QEMU 日志未再出现原 ENOENT panic、`exec /bin/sh failed` 或 `FetchInstructionPageFault 0xfffffffffffffffe`。详见 [problem/riscv-alpine-initfiles-dynamic-link.md](./problem/riscv-alpine-initfiles-dynamic-link.md)。

将 `initproc` 调整为默认启动 `/bin/sh -i`，失败时回退 `/musl/busybox sh -i`，并补充 `TERM/HOME/SHELL/USER` 默认环境变量。`make TARGET_ARCH=riscv64` 通过，25 秒 QEMU 日志出现 `/ #` 提示符，可直接交互输入命令。详见 [problem/riscv-alpine-initfiles-dynamic-link.md](./problem/riscv-alpine-initfiles-dynamic-link.md)。

补充控制台 stdin 基础回显，shell 中输入的命令字符、回车和退格现在会显示在 QEMU 串口输出中。`make TARGET_ARCH=riscv64` 通过，延迟输入 `echo SHELL_ECHO_OK` 后日志显示命令本身和执行结果。详见 [problem/riscv-alpine-initfiles-dynamic-link.md](./problem/riscv-alpine-initfiles-dynamic-link.md)。

修复 `vi` 的 `:` 模式输入双回显：stdio 现在支持最小 `TCGETS/TCSETS*` termios，stdin 回显受 `ECHO` 位控制，`ICANON` 关闭时按原始字符返回。`make TARGET_ARCH=riscv64` 通过，QEMU 中 `stty -echo` 后输入命令不再被内核回显，恢复 `stty echo` 后回显正常。详见 [problem/riscv-alpine-initfiles-dynamic-link.md](./problem/riscv-alpine-initfiles-dynamic-link.md)。

修复 `iperf-musl` 后连续运行 `iperf-glibc` 的 5001 端口冲突与 glibc 栈保护失败：socket fd close/exit 主动 shutdown 释放 listener，`TCGETS` 改回 Linux old termios 36 字节布局，并在标准 `run_testsuit()` 收尾时清理被 init 收养的 daemon 子进程。`make` 通过，`/tmp/iperf-generic-cleanup.log` 中 musl/glibc 两轮 iperf 六项均 success，未再出现 `socket already listening on port 5001`。详见 [problem/iperf-port-reuse-termios-stack-smash.md](./problem/iperf-port-reuse-termios-stack-smash.md)。

修复内核资源锁边界重构后 `netperf` 回归：`FdTable::close()` 不再因父进程关闭 accepted fd 而提前 shutdown 子进程继承的控制连接，`FdTable::clear()` 仍在进程退出时主动释放 listening socket，避免 `netperf-glibc` 复用 12865 端口失败。`make` 通过，`timeout 300s make run > log.ans 2>&1` 中 musl/glibc 两轮 netperf 十项均 success。详见 [problem/netperf-glibc-port-reuse.md](./problem/netperf-glibc-port-reuse.md)。

修复 `cyclictest` STRESS 模式下 `hackbench` 大量 `CLIENT: ready write (error: Broken pipe)`：`sys_socketpair()` 不再连续两次 `alloc_fd()` 复用同一空闲 fd，AF_UNIX socket 补齐阻塞 `recv/accept` wake 语义。`make` 通过，`timeout 300s make run > log.ans 2>&1` 中 musl/glibc cyclictest 四项均 success，未再出现 ready 阶段 Broken pipe。详见 [problem/cyclictest-socketpair-fd-allocation.md](./problem/cyclictest-socketpair-fd-allocation.md)。

修复 libctest `clocale_mbfuncs` 全量入口报 `Interrupted system call`：`sigtimedwait(SIGCHLD)` 成功消费信号后现在会清理内部 `interrupt_waker/interrupted` 状态，避免随后 `wait4()` 误返回 `EINTR`。`make` 通过，`make run` 中 clocale 输出 `Pass!`。详见 [problem/libctest-sigtimedwait-eintr.md](./problem/libctest-sigtimedwait-eintr.md)。

修复 LTP `abort01` 中 `tst_test.c:1654` 的 `waitpid(...)=EINTR` `TBROK`：`waitpid/waitid` 不再由通用 `interruptible()` 抢先返回 `EINTR`，而是按 pending signal 与 `SA_RESTART` 语义返回内部 `ERESTART` 并交由 signal frame 重启。`make` 通过，最新 `log.ans` 中 `abort01` 为 `passed 2 failed 0 broken 0`。详见 [problem/waitpid-sa-restart-eintr.md](./problem/waitpid-sa-restart-eintr.md)。

修复 LTP `access02`：`clone_process()` 按 `CLONE_VM` 处理共享地址空间子任务，避免 vfork/clone 路径在 `MemorySet::get_mut()` panic；`OpenFlags::read_write()` 改为按 `O_ACCMODE` 解码，避免 `O_RDONLY|O_CLOEXEC/O_LARGEFILE` 被误判成读写打开。`make` 通过，最新 `log.ans` 中 `access02` 16 项核心断言均 `TPASS`。详见 [problem/access02-ltp-execve.md](./problem/access02-ltp-execve.md)。

修复 LTP `access04`：`faccessat()` 补齐单个路径分量 `NAME_MAX=255` 检查，并按绝对路径所属只读挂载点返回 `EROFS`。`make` 通过，最新 `log.ans` 中 `access04` 12 项核心断言均 `TPASS`。详见 [problem/access04-faccessat-name-rofs.md](./problem/access04-faccessat-name-rofs.md)。

修复 LTP `acct01`：`acct(2)` 改用 effective uid 做特权判断，补齐 `NAME_MAX`、尾随 `/`、只读挂载和错误优先级处理。`make` 通过，最新 `log.ans` 中 `acct01` 9 项核心断言均 `TPASS`。详见 [problem/acct01-sys-acct-errno.md](./problem/acct01-sys-acct-errno.md)。

修复 LTP `acct02`：process accounting 记录中的普通 `exit(128)` 不再被特殊归零，而是和 `wait4` 一致编码为 `exit_code << 8`。`make` 通过，`make run` 单跑 `acct02` 显示 `passed 1 failed 0 broken 0`。详见 [problem/acct02-exitcode-128.md](./problem/acct02-exitcode-128.md)。

修复 LTP `alarm05`：`getitimer/setitimer(old_value)` 现在返回按 `last_time` 折算后的剩余 `it_value`，周期 timer 到期后推进到下一轮 interval。`make` 通过，最新 `log.ans` 中 `alarm05` 3 项核心断言均 `TPASS`。详见 [problem/alarm05-itimer-remaining.md](./problem/alarm05-itimer-remaining.md)。

修复 LTP `bind01`：TCP/UDP `bind()` 增加本机地址归属检查，非本地地址返回 `EADDRNOTAVAIL`；AF_UNIX pathname bind 先校验父目录，路径前缀非目录返回 `ENOTDIR`。`make` 通过，最新 `log.ans` 中 `bind01` 7 项核心断言均 `TPASS`。详见 [problem/bind01-bind-address-path.md](./problem/bind01-bind-address-path.md)。

修复 LTP `bind04`：AF_UNIX pathname bind 创建 VFS socket 节点以支持 cleanup `unlink()`，补齐 AF_UNIX `SOCK_SEQPACKET`，允许 IPv4/IPv6 sockaddr 使用更大的 `sockaddr_storage` 长度，并为 SCTP one-to-one stream 与 IPv6 loopback 通信提供最小兼容。`make` 和单跑 `bind04` 的 `make run` 通过，最新 `log.ans` 中 16 项通信场景均 `TPASS`。详见 [problem/bind04-unix-seqpacket-sockaddr.md](./problem/bind04-unix-seqpacket-sockaddr.md)。

修复 LTP `splice07`：`splice` 对非 pipe 端增加文件类型准入检查，避免目录输入到 pipe 错误成功，以及空 pipe 读端 splice 到 `/dev/zero` 输出时阻塞；`DummyFd` 补默认 `fstat()` 以避免未实现 fd 错误路径 panic。`make` 通过，单跑 `splice07` summary 为 `passed 566 failed 0 broken 0 skipped 25 warnings 0`。详见 [problem/splice07-file-type-validation.md](./problem/splice07-file-type-validation.md)。

修复 LTP `open14` 长时间无输出：补齐 `O_CREAT|O_EXCL` 已存在返回 `EEXIST` 语义，减少 `mkdirat`/`linkat` 的重复路径查找，并收窄 `rmdir`/`unlink` 慢路径。`make` 通过，`timeout 600s make run` 单跑 `open14` summary 为 `passed 3 failed 0 broken 0`。详见 [problem/open14-otmpfile-path-slow.md](./problem/open14-otmpfile-path-slow.md)。

继续优化 `open14` 深层路径性能：`open()` 和 `create_file()` 现在优先复用 `FsIndex` 中已缓存的父目录 inode，并避免 `Ext4Inode::types()` 为类型判断再次走路径查询；`make` 与单跑 `open14` 通过。详见 [problem/open14-otmpfile-path-slow.md](./problem/open14-otmpfile-path-slow.md)。

实现 VFS dentry cache：在 `os/src/fs/dcache.rs` 增加父 inode + child name 的 positive/negative 目录项缓存，`open()` 复用缓存父目录时可直接命中 child inode，并在 create/link/unlink/symlink/rename 成功后失效或回填目录项；`make` 与单跑 `open14` 通过。详见 [problem/open14-otmpfile-path-slow.md](./problem/open14-otmpfile-path-slow.md)。

继续降低 `open14` 深层目录性能开销：`FsIndex` 与 dentry positive cache 改为可显式失效的 strong inode cache，`Ext4Inode::live_path()` 只在元数据操作失败后走 alias 恢复，并合并 `create_file()` 父目录元数据读取；`open14` 单跑从约 223.35s 降到 65.94s，summary 仍为 `passed 3 failed 0 broken 0`。详见 [problem/open14-otmpfile-path-slow.md](./problem/open14-otmpfile-path-slow.md)。

修复 LTP `tst_virt` 启动探测阶段 `/proc/cpuinfo` 缺失导致的 `TBROK`：启动期补齐 RISC-V/LoongArch64 最小 `/proc/cpuinfo` 伪文件，避免 `fopen("/proc/cpuinfo")` 返回 `ENOENT`。`make` 通过，最新 `log.ans` summary 为 `passed 7 failed 0 broken 0`。详见 [problem/proc-cpuinfo-tst-virt.md](./problem/proc-cpuinfo-tst-virt.md)。

修复 RISC-V musl LTP `epoll_create02`：对预赛镜像 `/musl/lib/libc.so` 的 `epoll_create(size)` 包装补只读兼容 patch，保留 `epoll_create1(0)` 成功语义，同时让 `size <= 0` 返回 `EINVAL`。`make TARGET_ARCH=riscv64` 通过，临时单跑 `epoll_create02` 核心断言 2 项 `TPASS`。详见 [problem/epoll-create02-riscv-musl-libc.md](./problem/epoll-create02-riscv-musl-libc.md)。

修复 iperf server 启动阶段 `setsockopt(IPPROTO_IPV6, IPV6_V6ONLY)` 返回 `ENOPROTOOPT` 导致 5001 端口未监听的问题；新增 IPv6 socket option 最小兼容。`make` 通过，使用 `/tmp` qcow2 overlay 运行 RISC-V QEMU 后 musl/glibc 两组 iperf 六项均 `success`。详见 [problem/iperf-ipv6-v6only.md](./problem/iperf-ipv6-v6only.md)。

## 7.4

修复 LoongArch64 glibc LTP `mkdir09`：`PagePrivilegeIllegal` 先进入统一缺页处理以支持 `MAP_STACK` 懒分配，同时为 stack/brk 懒分配补 VMA 权限检查；`tmpfs` 挂载前清空挂载点内容，避免简化 mount 模型下 ext2 轮次目录泄漏。`make TARGET_ARCH=loongarch64` 通过，单跑 `mkdir09` summary 为 `passed 12 failed 0 broken 0`。详见 [problem/mkdir09-loongarch-map-stack-tmpfs.md](./problem/mkdir09-loongarch-map-stack-tmpfs.md)。

## 7.6

实现 `faccessat2(439)`：复用并整理 `faccessat` 权限检查，补齐 `AT_EACCESS`、`AT_SYMLINK_NOFOLLOW`、`AT_EMPTY_PATH` flags 校验与分发，同时修正绝对路径忽略坏 `dirfd` 的语义。`make` 通过，RISC-V 单跑 musl/glibc `faccessat201`、`faccessat202` 均为 `TPASS`。

实现 `fanotify_init(262)` 的基础 fd 创建与参数校验：新增 `FanotifyFd` 文件对象，支持 `FAN_CLOEXEC/FAN_NONBLOCK` 转 fd flags，并校验 init flags 与 event fd flags。LoongArch64 `make` 通过；当前 `fanotify01` 继续停在未实现的 `fanotify_mark(263)`。

实现 `fanotify_mark(263)` 并补齐 `fanotify01` 所需基础事件队列：支持 mark add/remove/flush、ignore mask、open/access/modify/close 事件投递和 legacy metadata 读取。LoongArch64 `make` 通过，单跑 musl/glibc `fanotify01` summary 均为 `passed 156 failed 0 broken 0`。详见 [problem/fanotify01-mark-events.md](./problem/fanotify01-mark-events.md)。

收敛 syscall 模块边界：将 inotify syscall 从 `fs/mod.rs` 拆出，将 process accounting 写记录与 `CloneFlags` 下沉到 task 层，去除 task 对 syscall 根模块的反向依赖；默认 LoongArch64 `make` 通过。

新增 `os/src/syscall/fs/path.rs` 聚合路径上下文相关 syscall：迁入 `getcwd/chdir/chroot/readlinkat/faccessat/faccessat2`，并移除 `sys` 下的 chroot 子模块；默认 LoongArch64 `make` 通过。

修复 LTP `fchmod02` 启动环境前置条件：启动期 `/etc/group` 补齐 `users` 与 `daemon` 组，避免 `SAFE_GETGRNAM_FALLBACK("users", "daemon")` 找不到组而 `TBROK`。`make` 通过，最新 `log.ans` 中 musl/glibc `fchmod02` 均 `TPASS`，Summary 为 `passed 1 failed 0 broken 0`。详见 [problem/fchmod02-group-database.md](./problem/fchmod02-group-database.md)。

修复 LTP `fchmod05`：`fchmod/fchmodat` 现在统一执行 chmod 权限检查、只读挂载 `EROFS` 检查，并在非 root 且有效 gid 不匹配目标 inode gid 时静默清除 `S_ISGID`。`make` 通过，最新 `log.ans` 中 musl/glibc `fchmod05` 均 `TPASS`，Summary 为 `passed 1 failed 0 broken 0`。详见 [problem/fchmod05-chmod-setgid.md](./problem/fchmod05-chmod-setgid.md)。

修复 LTP `fanotify02` 卡死：目录 mark 现在支持 `FAN_EVENT_ON_CHILD` 直接子项匹配，并允许 `FAN_MARK_REMOVE` 单独移除 `FAN_EVENT_ON_CHILD/FAN_ONDIR` 修饰位。`make` 通过，LoongArch64 单跑 musl/glibc `fanotify02` 均 8 项 `TPASS`。详见 [problem/fanotify02-event-on-child.md](./problem/fanotify02-event-on-child.md)。

修复 LTP `chown04`：`chown/fchownat` 补齐路径长度、父目录搜索权限和只读挂载 errno 优先级处理，避免统一落到非 root `EPERM`。`make` 通过，LoongArch64 单跑 musl/glibc `chown04` 均 8 项 `TPASS`。详见 [problem/chown04-chown-errno.md](./problem/chown04-chown-errno.md)。

修复 LTP `fcntl01`：`F_GETFL` 现在返回 fd 保存的真实访问模式和文件状态标志，`F_SETFL` 会更新 `O_APPEND/O_NONBLOCK` 等可修改状态位。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl01` 均 `passed 1 failed 0 broken 0`。详见 [problem/fcntl01-f-getfl-setfl.md](./problem/fcntl01-f-getfl-setfl.md)。

继续完善 `fcntl(2)` 基础兼容性：修正无效 fd 错误码、`F_DUPFD/F_DUPFD_CLOEXEC` 的 `FD_CLOEXEC` 语义和 fd limit 错误码，并为 pipe 增加 per-pipe capacity、`F_GETPIPE_SZ/F_SETPIPE_SZ` 与 `/proc/sys/fs/pipe-max-size`。默认 LoongArch64 `make` 通过；`make run` 在根文件系统 ext4 mount 阶段失败，未进入 LTP。详见 [problem/fcntl-dupfd-pipe-size.md](./problem/fcntl-dupfd-pipe-size.md)。

修复 LTP `fcntl11`：POSIX record lock 现在使用当前进程 pid 作为 owner，同进程重叠锁会按区间覆盖拆分/合并，`F_GETLK` 返回最靠前的其它进程冲突锁。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl11` 均 `passed 1 failed 0 broken 0`。详见 [problem/fcntl11-record-lock.md](./problem/fcntl11-record-lock.md)。

修复 LTP `fcntl13`：record lock 分支现在先从用户空间复制 `struct flock`，再解析普通文件对象，避免 stdout 等非普通 fd 的 `EINVAL` 遮蔽坏用户指针应返回的 `EFAULT`。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl13` 均 `passed 4 failed 0 broken 0`。详见 [problem/fcntl13-lock-efault-priority.md](./problem/fcntl13-lock-efault-priority.md)。

修复 LTP `fcntl14`：POSIX record lock 支持 `SEEK_CUR` 当前偏移、负 `l_len` 反向区间、非法 `l_whence` 返回 `EINVAL`，并补齐 `F_SETLKW` 阻塞等待、死锁检测和 close/exit 释放锁。RISC-V 与默认 LoongArch64 构建通过，维护者确认 `fcntl14` 已通过。详见 [problem/fcntl14-record-lock-seekcur-len.md](./problem/fcntl14-record-lock-seekcur-len.md)。

修复 LTP `fcntl23`：`F_SETLEASE/F_GETLEASE` 不再固定返回 `EAGAIN/F_UNLCK`，新增按路径和 pid 管理的最小文件租约表，并在 close/exit 时清理租约。`make` 通过，LoongArch64 单跑 musl/glibc `fcntl23` 均 `TPASS`。详见 [problem/fcntl23-file-lease.md](./problem/fcntl23-file-lease.md)。

修复 LTP `fcntl31`：补齐 `F_GETOWN/F_SETOWN/F_GETOWN_EX/F_SETOWN_EX/F_GETSIG/F_SETSIG` 的最小 async I/O owner 状态，并让 pipe 写入时向 TID/PID/PGRP owner 投递配置的信号。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl31` 均 5 项 `TPASS`。详见 [problem/fcntl31-fasync-owner-signal.md](./problem/fcntl31-fasync-owner-signal.md)。

重构 `file_lock` 模块：将 `struct flock` ABI、POSIX record lock、file lease 和 BSD `flock(2)` 拆分为高内聚子模块，对外保持 `file_lock::...` 接口不变；LoongArch64 与 RISC-V 构建均通过。

完善 session ID 语义：`ProcessMeta` 新增独立 `sid` 字段，`fork/clone` 继承调用者的 `pgid/sid`，`getsid/setsid/setpgid` 不再混用 `pgid` 表示会话；LoongArch64 与 RISC-V 构建均通过。

收敛 `fcntl(2)` syscall 代码位置：将 `sys_fcntl`、record lock 阻塞 helper 和 `struct f_owner_ex` 编解码从 `fd_ops.rs` 迁入 `fcntl.rs`，让 `fd_ops.rs` 回到通用 fd/open/close 操作；默认 LoongArch64 `make` 通过。

修复 LTP `fcntl33`：补齐 `/proc/sys/fs/lease-break-time`，为冲突 `open/truncate` 增加 file lease break `SIGIO` 通知，并修正写 lease 降级与 `truncate()` 相对路径语义。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl33` 均 7 项 `TPASS`，Summary 为 `passed 7 failed 0 broken 0`。详见 [problem/fcntl33-lease-break-sigio.md](./problem/fcntl33-lease-break-sigio.md)。

修复 LTP `fcntl34`：OFD lock 改用 open file description 级 owner，`F_OFD_SETLKW` 接入阻塞等待，并在最后一个 fd 关闭时释放对应 OFD 锁。`make` 通过，最新 `log.ans` 中 musl/glibc `fcntl34` 均 `TPASS`，Summary 为 `passed 1 failed 0 broken 0`。详见 [problem/fcntl34-ofd-lock-owner.md](./problem/fcntl34-ofd-lock-owner.md)。

修复 LTP `fcntl35`：`/proc/sys/fs/pipe-max-size` 写入现在会同步到 pipe 子系统，非 `CAP_SYS_RESOURCE` 任务新建 pipe 时初始容量受该上限约束，特权任务保持默认 `65536`。LoongArch64 `make` 通过，单跑 musl/glibc `fcntl35` 均为 `passed 2 failed 0 broken 0`。详见 [problem/fcntl35-pipe-max-size-init.md](./problem/fcntl35-pipe-max-size-init.md)。

## 7.7

修复 sys_setresuid 语义的bug，setresuid_allowed函数体内判断各个uid各自管各自的逻辑。

修复 LTP `kill05`：`kill(2)` 对用户发信号增加 real/effective/saved uid 权限检查，并修正 `pid == 0`、`pid < -1` 的进程组投递语义。`make` 通过，最新 `log.ans` 中 musl/glibc `kill05` 均 `TPASS`，Summary 为 `passed 1 failed 0 broken 0`。详见 [problem/kill05-kill-permission.md](./problem/kill05-kill-permission.md)。

重构 `os/src/signal` 模块：将 signal frame、pending/default action、信号投递/`kill(2)` 权限和 itimer/pselect 信号拆成独立子模块，`mod.rs` 收敛为门面导出；默认 LoongArch64 `make` 通过。

实现 `shmdt(197)` 基础语义：接入 syscall 分发，按页对齐和当前进程 shm attach 起始地址校验，成功时解除当前地址空间中的 SysV shm 映射并刷新 TLB。默认 LoongArch64 `make` 通过。

重构 `os/src/mm/memory_set`：将 `MemorySetInner` 类型定义、锁封装、基础 area 操作、页表/统计/回收 helper 从 `mod.rs` 拆入独立子模块，`mod.rs` 收敛为门面导出并补充模块职责文档；默认 LoongArch64 `make` 通过。

修复 LTP `kill10`：pending signal 现在保存用户态 `kill/tkill/tgkill` 的发送者 `siginfo_t`，`SA_SIGINFO` handler 可读取正确 `si_pid`，避免 signal flooding 测试误判来源 pid 后循环刷屏。LoongArch64 单跑 musl/glibc `kill10` 均 `TPASS`，LoongArch64 与 RISC-V 构建通过。详见 [problem/kill10-siginfo-sender.md](./problem/kill10-siginfo-sender.md)。

修复 LTP `kill12`：显式 `SIG_IGN` 的 pending signal 不再按默认终止动作处理，并移除投递阶段过早写入 `termination_signal` 的逻辑，避免被忽略信号污染 `waitpid()` status。LoongArch64 单跑 musl/glibc `kill12` 均 `TPASS`，LoongArch64 与 RISC-V 构建通过。详见 [problem/kill12-sigign-wait-status.md](./problem/kill12-sigign-wait-status.md)。

修复 LTP `link04`：`linkat` 补齐空路径、超长路径、路径分量长度和父目录权限错误码处理。`make` 通过，LoongArch64 单跑 musl/glibc `link04` 均为 `passed 14 failed 0 broken 0`。详见 [problem/link04-linkat-errno-permission.md](./problem/link04-linkat-errno-permission.md)。

修复 LTP `link08`：`linkat` 补齐跨挂载点 `EXDEV`、只读挂载 `EROFS`，并避免符号链接环被路径长度预检错误遮蔽为 `ENAMETOOLONG`。`make` 通过，LoongArch64 单跑 musl/glibc `link08` 均为 `passed 4 failed 0 broken 0`。详见 [problem/link08-linkat-mount-rofs-eloop.md](./problem/link08-linkat-mount-rofs-eloop.md)。

修复 LTP `linkat02` 卡死：为 hard link 增加有限 `EMLINK` 上限，并让 `unlinkat` 删除最终 symlink 本身，避免 cleanup 阶段跟随 symlink 环产生 warning。`make` 通过，LoongArch64 单跑 musl/glibc `linkat02` 均为 `passed 7 failed 0 broken 0`。详见 [problem/linkat02-hardlink-emlink-unlink-symlink.md](./problem/linkat02-hardlink-emlink-unlink-symlink.md)。

修复 LTP `mkdir02`：目录创建时若父目录带 `S_ISGID`，新目录现在继承该 mode 位并继续继承父目录 gid。`make` 通过，LoongArch64 `make run` 中 musl/glibc `mkdir02` 均 `TPASS`。详见 [problem/mkdir02-setgid-inherit.md](./problem/mkdir02-setgid-inherit.md)。

## 7.12

修复 LTP `mmap04`：`/proc/<pid>/maps` 在打开时按当前 VMA 刷新，使用 Linux 兼容的无前导零地址格式并正确显示共享映射 `s`；`MAP_FIXED` 拆分 VMA 时同步更新共享/私有标志。LoongArch64 单跑 musl/glibc 均为 `passed 14 failed 0 broken 0`，LoongArch64 与 RISC-V 构建通过。详见 [problem/proc-self-maps-mmap04.md](./problem/proc-self-maps-mmap04.md)。

补齐 `/proc/<pid>/pagemap` 与 `/proc/self/pagemap`：按当前页表刷新 Linux pagemap 二进制条目，并以稀疏文件保存未映射页的零值；initproc 创建和进程退出清理同步覆盖该文件。默认 LoongArch64 `make` 通过，最新 `log.ans` 中 musl/glibc `mmap12` 均为 `passed 1 failed 0 broken 0`。详见 [problem/proc-self-pagemap-mmap12.md](./problem/proc-self-pagemap-mmap12.md)。

修复 LTP `mmap13`：文件映射在完整 EOF 外页缺页时现在投递 `SIGBUS`，VMA 保留 mmap 时的文件长度；同时修正 ext4 截断扩展后的长度一致性，避免 LTP 框架 unlink 的共享页被误判。LoongArch64 musl/glibc 均为 `passed 1 failed 0 broken 0`。详见 [problem/mmap13-sigbus-eof.md](./problem/mmap13-sigbus-eof.md)。

## 7.13

修复 LoongArch PCI VirtIO-net 启动期内存破坏：早期每 hart 4 KiB 栈无法容纳网卡初始化的 Rust 调用链，覆盖 `.data` 中的 UART/ext4 cache；同时修正 PCI ECAM 非对齐非 volatile 访问及 VirtQueue DMA 未清零。LoongArch64 完整启动至用户态 `shutdown!`，RISC-V 构建和启动回归均未见 panic。详见 [problem/loongarch-pci-virtio-net-bootstrap-corruption.md](./problem/loongarch-pci-virtio-net-bootstrap-corruption.md)。

为 VirtIO-net 增加用户态分层回归：UDP loopback 作为协议栈基线，SLIRP DNS 单次往返和 160 次连续往返验证真实 `eth0` TX/RX 与描述符回收，非阻塞超时用例验证无响应路径不会卡死。LoongArch64 与 RISC-V QEMU 均为 `passed 4 failed 0`。详见 [problem/loongarch-pci-virtio-net-bootstrap-corruption.md](./problem/loongarch-pci-virtio-net-bootstrap-corruption.md)。

修复 LoongArch64 启动期 fork 停滞：`/proc/<pid>/pagemap` 不再在进程创建时对 ext4 截断 384 MiB，改为按需生成的动态只读文件。`glibc/basic_testcode.sh` 完整结束，glibc `mmap12` 仍为 `passed 1 failed 0 broken 0`；LoongArch64 与 RISC-V 构建通过。详见 [problem/proc-pagemap-fork-allocation.md](./problem/proc-pagemap-fork-allocation.md)。

修复 LoongArch64 `cyclictest` 的内核堆 OOM：AF_UNIX 接收队列按 64KiB 上限实施背压，龙芯 QEMU RAM 升至 2GiB 并按真实两段地址纳入同一 CMA。LoongArch64 musl/glibc cyclictest 八个阶段和两个 hackbench 清理均成功，无 panic。详见 [problem/loongarch-unix-queue-oom.md](./problem/loongarch-unix-queue-oom.md)。

RISC-V QEMU RAM 同步为连续 2GiB：CMA 在 bootstrap 1GiB 映射内初始化，完整 Sv39 直接映射启用后再纳入第二个 GiB；RISC-V 2GiB 启动完成 `remap_test`、initproc、busybox/Lua 两组和 iperf-musl 六项，无内存分配 panic。详见 [problem/loongarch-unix-queue-oom.md](./problem/loongarch-unix-queue-oom.md)。

修复 LTP `signal03` 卡死：pending 信号分发对显式 `SIG_IGN` 不再把默认 stop 信号例外处理，`SIGTSTP`/`SIGTTIN`/`SIGTTOU` 现会被正确忽略。LoongArch64 单跑 musl/glibc 均完成，Summary 分别为 `passed 31`、`passed 30`，均无 failed/broken。详见 [problem/signal03-sigign-stop.md](./problem/signal03-sigign-stop.md)。

## 7.14

将内核设计文档从 Markdown 迁移为以 `Docs/ya2yos/main.typ` 为入口的 Typst 工程，补齐十章内容、参考文献和参赛人员信息；同步调整章节编号、目录结构，并以 Typst 原生图替换原有 UML 源文件和图片。

新增 `make doc`，仅为内核 crate 生成 Rustdoc（不生成依赖文档）；增加 `scripts/extract_failed_tests.py`，可从测试日志中识别 libctest 与 LTP 的实际失败项，并忽略固定打印的 `FAIL LTP CASE ... : 0` 包装行。

修正 `sys_read` 对不可读 fd 的 Linux 错误码语义：由 `EACCES` 改为 `EBADF`。

修复 LTP `splice07` 的匿名挂载 fd 卡死：`fsopen`/`fspick` 与 `open_tree` 返回的上下文 fd 不再沿用 `File` 的默认可读写能力，`splice` 现在在空 pipe 读之前返回 `EBADF`。RISC-V `make` 与 LoongArch64 `make` 均通过；最新 `log.ans` 中 musl/glibc 均为 `passed 566 failed 0 broken 0`，并正常 `shutdown!`。详见 [problem/splice07-mount-context-fd-block.md](./problem/splice07-mount-context-fd-block.md)。

修复 LTP `pipe2_01`：syscall 分发不再丢弃 `pipe2(flags)` 参数，`O_CLOEXEC`、`O_NONBLOCK` 和 `O_DIRECT` 现在按 fd/端点语义保存。RISC-V musl/glibc 均为 `passed 7 failed 0 broken 0` 并正常关机。详见 [problem/pipe2-flags-propagation.md](./problem/pipe2-flags-propagation.md)。

修复 LTP `open02`：打开既有 inode 时，`O_NOATIME` 现在要求 caller 是文件 owner 或持有 `CAP_FOWNER`，否则返回 `EPERM`。LoongArch64 musl/glibc 均为 `passed 2 failed 0 broken 0` 并正常关机。详见 [problem/open02-noatime-permission.md](./problem/open02-noatime-permission.md)。

修复 LTP `open11` 目录 flags 语义：既有目录的写 access mode、非 `O_PATH` 的 `O_TRUNC` 或 `O_CREAT` 现在返回 `EISDIR`，并保持 `O_PATH` 不触发创建、截断或 `O_NOATIME` 检查。RISC-V 与 LoongArch64 构建通过；QEMU 运行分别受 `IllegalInstruction` 和沙箱 `/var/tmp` 只读限制，尚未取得有效 LTP 回归结果。详见 [problem/open11-directory-open-flags.md](./problem/open11-directory-open-flags.md)。

## 7.15

修复 LTP `utimes01`：补齐 NULL pathname 的 `EFAULT`、非 owner 时间戳更新的 `EACCES`/`EPERM` 以及只读挂载 `EROFS` 语义。RISC-V/LoongArch64 单测中 musl/glibc 均通过；详见 [problem/utimes01-permission-rofs.md](./problem/utimes01-permission-rofs.md)。

修复 LoongArch64 全量 glibc LTP `kill10` 的 signal frame EFAULT panic：完整预检用户信号帧范围，保证 handler 栈 16 字节对齐，失败时仅终止当前任务为 `SIGSEGV`，并移除 `TaskControlBlockInner` 锁跨用户内存访问。RISC-V/LoongArch64 构建通过，LoongArch64 单跑 musl/glibc `kill10` 均为 `passed 1 failed 0 broken 0`；全量 LTP 尚待恢复全量入口后复跑。详见 [problem/kill10-signal-frame-efault-panic.md](./problem/kill10-signal-frame-efault-panic.md)。

修复 LTP `openat201`：`openat2` 现在接受基础 resolve 标志，并补齐 `open_how` 扩展尾部、flags/mode、pathname 和 dirfd 的 ABI 校验；RISC-V musl/glibc 均为 `passed 16 failed 0 broken 0`。详见 [problem/openat2-open-how-resolve.md](./problem/openat2-open-how-resolve.md)。

修复 LTP `socket01` 的 socket type errno：非法 type 现在返回 `EINVAL`，未实现的 IPv4/IPv6 `SOCK_RAW` 返回 `EPROTONOSUPPORT`；最新 `log.ans` 中 musl/glibc 均为 `passed 9 failed 0 broken 0` 并正常关机。详见 [problem/socket01-socket-type-errno.md](./problem/socket01-socket-type-errno.md)。

修复 LTP `socketpair01`：AF_INET/AF_INET6 的 socketpair errno 现在区分 `EINVAL`、`EOPNOTSUPP` 与 `EPROTONOSUPPORT`；RISC-V user-copy 也会在缺页前校验用户 VMA，坏指针返回 `EFAULT`。RISC-V musl/glibc 均为 `passed 10 failed 0 broken 0`。详见 [problem/socketpair01-protocol-errno.md](./problem/socketpair01-protocol-errno.md)。

修复 signal 默认 disposition ABI：默认 action 不再复用 `SIG_IGN` 或内核函数地址，显式 `SIG_IGN`、默认动作和用户 handler 现由独立 disposition 表示。LoongArch64 与 RISC-V 的 musl/glibc `waitid07` 均为 `passed 5 failed 0 broken 0`。详见 [problem/signal-disposition-default-abi.md](./problem/signal-disposition-default-abi.md)。

修复 LTP `clone02`：为 `CLONE_FILES`/`CLONE_FS` 共享的 fd table 和 FS context 建立独立活跃进程所有者计数，避免退出子进程清空父进程资源，也避免 zombie `Arc` 阻止最后一个 pipe 端关闭。RISC-V 与 LoongArch64 的 musl/glibc 均为 `passed 2 failed 0 broken 0` 并正常关机。详见 [problem/clone02-shared-resource-exit.md](./problem/clone02-shared-resource-exit.md)。

修复 legacy `clone(2)` 的 `CLONE_THREAD` 低位退出信号兼容：RISC-V musl/glibc 和 LoongArch64 glibc `clone08` 均为 `passed 5 failed 0 broken 0`；LoongArch64 musl 剩余失败确认由测试镜像 libc 在 syscall 前拒绝线程 flags，未修改镜像。详见 [problem/clone08-legacy-clone-thread-signal.md](./problem/clone08-legacy-clone-thread-signal.md)。

修复 LTP `getcwd03`：`chdir` 保存解析后的目录路径，`readlinkat` 与内部 unlink 查找保留末级符号链接并绕过已跟随链接的缓存。LoongArch64 与 RISC-V 的 musl/glibc 均为 `passed 1 failed 0 broken 0`。详见 [problem/getcwd03-symlink-cwd-readlink-cache.md](./problem/getcwd03-symlink-cwd-readlink-cache.md)。

## 7.16

修复 LTP `vmsplice02`：对有效但非 pipe 的 fd 调用 `vmsplice` 现在返回 `EBADF`，不再错误返回 `EINVAL`。RISC-V 单测中 musl/glibc 均通过；详见 [problem/vmsplice02-non-pipe-fd.md](./problem/vmsplice02-non-pipe-fd.md)。

修复 LTP `waitpid04`：`waitpid/wait4` 现在严格拒绝未知 options 位，非法 `0xffffffff` 返回 `EINVAL` 而不是在无 child 时误返 `ECHILD`。RISC-V 单测中 musl/glibc 均通过；详见 [problem/waitpid04-invalid-options.md](./problem/waitpid04-invalid-options.md)。

修复 LTP `writev01` 的 `writev(2)` 错误码和空 iovec 语义：无效 fd 返回 `EBADF`，空 iovec 成功返回 0，零长度 NULL iovec 不再触发用户地址拷贝，关闭 pipe 读端可返回 `EPIPE`。双架构构建通过；详见 [problem/writev01-writev-errno.md](./problem/writev01-writev-errno.md)。

修复 LTP `unlink07`：`unlinkat` 在 pathname 归一化前拒绝空路径并检查总长度及分量长度，分别返回 `ENOENT` 与 `ENAMETOOLONG`。双架构构建通过，LoongArch64 musl/glibc 均为 `passed 6 failed 0 broken 0`；详见 [problem/unlink07-path-errno.md](./problem/unlink07-path-errno.md)。

修复 LTP `readv01`：合法 fd 上的零 iovec 现在成功返回 0；`readv` 同步收敛 fd、长度、目录和用户输出缓冲区校验。双架构构建通过，LoongArch64 musl/glibc 均为 `passed 10 failed 0 broken 0`；详见 [problem/readv01-iovec-semantics.md](./problem/readv01-iovec-semantics.md)。

修复 LTP `open14`：在普通源路径校验前识别 `/proc/self/fd/<fd>` magic-link，并让 lwext4 小文件缓存物化时保存内容和 mode、避免 `fstat` panic。RISC-V musl/glibc 均为 `passed 3 failed 0 broken 0`，无内核 error；详见 [problem/open14-procfd-linkat-cache.md](./problem/open14-procfd-linkat-cache.md)。

## 7.17

修复 LTP `fs_bind17` 至 `fs_bind21` 的 shared peer 与多级 slave 挂载传播，并消除 `fs_bind22` 同树 bind 的递归镜像内核 panic。RISC-V 回归中 17 至 21 均为 `failed 0`；同树 bind 的真实 mount-root 视图仍待 VFS 层实现。详见 [problem/fs-bind-peer-slave-propagation.md](./problem/fs-bind-peer-slave-propagation.md)。

修复 LTP `kill02`：pipe 阻塞读写与 readiness wait 现在会消费默认或显式忽略的 pending signal，避免默认忽略的 `SIGCHLD` 被错误返回为 `EINTR` 并提前关闭 LTP 输出读端。RISC-V 与 LoongArch64 的 musl/glibc `kill02` 均为 `passed 2 failed 0 broken 0`。详见 [problem/kill02-ignored-sigchld-pipe-eintr.md](./problem/kill02-ignored-sigchld-pipe-eintr.md)。

修复 LTP `linkat01`：相对路径的 `dirfd` 现在强制为目录并正确返回 `ENOTDIR`，`/proc` compatibility namespace 与普通路径间返回 `EXDEV`，未知 flags 返回 `EINVAL`。RISC-V 与 LoongArch64 的 musl/glibc 均为 `passed 22 failed 0 broken 0`。详见 [problem/linkat01-dirfd-procfs-flags.md](./problem/linkat01-dirfd-procfs-flags.md)。

重构 `os/src/syscall/fs/ctl/mod.rs`：拆为目录项、链接、命名空间、元数据、时间、ioctl 与共享 helper 八个子模块，门面保留原有 `fs::ctl::*` 接口。双架构构建通过，RISC-V `linkat01` musl/glibc 均为 `passed 22 failed 0 broken 0`。

修复 LTP `mmap08`：非匿名文件映射的无效 fd 现在在零长度参数校验前返回 `EBADF`，与 Linux 错误优先级一致。RISC-V musl/glibc 单跑均 `TPASS`；详见 [problem/mmap08-fd-errno-priority.md](./problem/mmap08-fd-errno-priority.md)。

修复 LTP `chdir01`：`chdir(2)` 现在按 effective uid/gid 验证已解析路径中各目录的 search 权限，非 root 无法进入 `0644` 目录。RISC-V musl/glibc 在 ext2、tmpfs 均为 `passed 32 failed 0 broken 0`；详见 [problem/chdir01-search-permission.md](./problem/chdir01-search-permission.md)。

修复 LTP `chdir04`：长度恰为 `MAX_PATH_LEN` 且未含 NUL 的 pathname 现在返回 `ENAMETOOLONG`，不再错误落入文件查找并返回 `ENOENT`。RISC-V musl/glibc 单跑均 `TPASS`；详见 [problem/chdir04-path-length-boundary.md](./problem/chdir04-path-length-boundary.md)。

修复 LTP `tcp4-multi-diffnic01`：启动期补齐 BusyBox `wc` applet，并为当前单节点、无独立网卡对的环境添加明确的空 alias-pair 兼容 wrapper。RISC-V musl/glibc 单跑均为 `passed 1 failed 0 broken 0`；详见 [problem/tcp4-multi-diffnic01-single-node-env.md](./problem/tcp4-multi-diffnic01-single-node-env.md)。

修复 LTP `fs_bind_rbind01`：启动期补齐 BusyBox `seq`、`diff` applet，并为路径化挂载表实现 bind 层、递归 shared propagation 和同事件卸载；RISC-V musl/glibc 均为 `passed 28 failed 0 broken 0`。详见 [problem/fs-bind-rbind-propagation.md](./problem/fs-bind-rbind-propagation.md)。

修复 LTP `fs_bind13`：挂载层现保存 unbindable 状态，bind clone unbindable source 返回 `EINVAL` 且不产生 peer 副本；RISC-V musl/glibc 均为 `passed 24 failed 0 broken 0`。详见 [problem/fs-bind13-unbindable-source.md](./problem/fs-bind13-unbindable-source.md)。

修复 LTP `fs_bind23`：`MS_MOVE` 现在重定位 source mount subtree、向目标 shared peer 复制同 event group，并同步路径化 VFS 目录视图。RISC-V musl/glibc 均为 `passed 20 failed 0 broken 0`。详见 [problem/fs-bind23-move-propagation.md](./problem/fs-bind23-move-propagation.md)。

修复 LTP `fs_bind24`：子目录 bind 现从覆盖 source 的顶层 mount 继承状态，shared-slave 转换保留上游 master，并在传播时保留 source 子目录偏移。RISC-V musl/glibc 均为 `passed 15 failed 0 broken 0`。详见 [problem/fs-bind24-subtree-shared-slave-propagation.md](./problem/fs-bind24-subtree-shared-slave-propagation.md)。

修复 LTP `fs_bind_move05`：private subtree 移入 shared parent 时，移动根现继承接收端 propagation state，后续事件通过同一 move event 的 peer root 保留子路径偏移。RISC-V musl/glibc 均为 `passed 27 failed 0 broken 0`。详见 [problem/fs-bind-move05-private-shared-propagation.md](./problem/fs-bind-move05-private-shared-propagation.md)。

## 7.18

完成 RISC-V QEMU 双 hart 首版 SMP bring-up：SBI 启动 AP、进程固定 hart 调度、共享任务/文件系统/异步定时状态加锁；RISC-V 双核和 LoongArch64 单核的 musl/glibc `iperf` 均完成并正常关机。详见 [problem/riscv-smp-bringup.md](./problem/riscv-smp-bringup.md)。

修复双 hart `netperf` 偶发卡死：统一 smoltcp 全局锁序，闭合 `block_on`/`AtomicWaker` 的跨核丢唤醒窗口，并按 `home_hart` 驱动 blocked timer。双架构构建通过，RISC-V musl/glibc netperf 连续两次共 10 项全成功并正常关机。详见 [problem/riscv-smp-netperf-wakeup-locking.md](./problem/riscv-smp-netperf-wakeup-locking.md)。

实现编译期可切换的 CFS/RR 调度器，默认 CFS 按 nice 加权 `vruntime` 从 per-Hart heap 选择任务，`make SCHEDULER=rr` 保留 FIFO RR。双策略、双架构构建通过；RISC-V 的 CFS/RR 及 LoongArch64 的 CFS 下，musl/glibc cyclictest 各 8 个阶段均成功，两轮 400-task hackbench 完成预期清理并正常关机。

## 7.19

修复 RISC-V 双 hart `basic_testcode.sh/test_yield` 卡死：父进程继续 fork 与首个子进程退出时不再以相反顺序获取 `ProcessMeta` 和 `TaskControlBlockInner`。双架构构建与运行回归通过，RISC-V release+CFS 多轮均完成 musl/glibc 各 15 条输出并正常 `shutdown!`。详见 [problem/basic-test-yield-fork-exit-deadlock.md](./problem/basic-test-yield-fork-exit-deadlock.md)。

修复 BusyBox musl/glibc 回归的 fork/exec/exit 并发卡死：futex 仅在 `Blocked -> Ready` 时入队，CFS/RR 丢弃陈旧就绪项，并在 exec/退出回收前切换有效页表。当前 RISC-V 日志两组均到达 `GROUP END` 并 `shutdown!`；其他 BusyBox 失败项仍待单独处理。详见 [problem/busybox-fork-exec-exit-smp-hang.md](./problem/busybox-fork-exec-exit-smp-hang.md)。

修复 RISC-V 双 hart CFS `UDP_RR` 首 burst 卡死：CFS 在陈旧 entry 出队时不再丢失并发网络唤醒，smoltcp 的微秒 deadline 也恢复为正确内核时间。双架构构建通过，独立 musl `UDP_RR` 多次完成并 `shutdown!`；详见 [problem/riscv-cfs-netperf-udp-rr-hang.md](./problem/riscv-cfs-netperf-udp-rr-hang.md)。

修复 RISC-V 双 hart libctest 批量运行的随机段错误：COW 写保护复制在解除旧 VMA 映射前固定源 `FrameTracker`，避免另一 hart 并发回收并复用源物理页。最终 `log.ans` 完整运行 217 项、输出 `GROUP END` 和 `shutdown!`，无段错误；`utime` 两个既有失败未纳入本次修复。详见 [problem/riscv-libctest-cow-source-frame-race.md](./problem/riscv-libctest-cow-source-frame-race.md)。

参考 Linux 7.0 `wp_page_copy()` 继续加固 RISC-V COW：新页现先分配和复制，成功后才替换 PTE/旧 frame；`copy_to_user` 也会正确触发 present COW 页分裂。同步修复 Brk shrink 遗留 PTE 与 lazy stack clone 的临时页表所有权；双架构构建通过，RISC-V libctest 217 项完整结束。详见 [problem/riscv-libctest-cow-source-frame-race.md](./problem/riscv-libctest-cow-source-frame-race.md)。

修复 libc-test `utime` 的 `futimens` ABI：`utimensat` 现仅将有效 fd 加 NULL pathname 识别为 fd 目标，保留 `utimes(NULL)` 的 `EFAULT`。RISC-V 与 LoongArch64 定向测例均输出 `Pass!` 并正常 `shutdown!`；详见 [problem/libctest-futimens-fd-null-pathname.md](./problem/libctest-futimens-fd-null-pathname.md)。

## 7.20

修复 iozone 连续 random-read/backward-read 卡死：FsIndex 不再遗留可被 inode 号复用命中的 canonical inode，lwext4 write-back FIFO 同步清理，并绕过短生命周期 task proc 文件缓存。RISC-V 与 LoongArch64 两项 iozone 测例均完整结束并 `shutdown!`；详见 [problem/iozone-inode-cache-reuse-hang.md](./problem/iozone-inode-cache-reuse-hang.md)。

修复 BusyBox `hwclock` 和目录 `mv/rmdir`：`renameat2` 不再以写方式打开目录源路径，`DevRtc` 实现 `RTC_RD_TIME`。RISC-V musl/glibc 最小回归三项均返回 0 并 `shutdown!`，LoongArch64 编译通过；详见 [problem/busybox-hwclock-rename.md](./problem/busybox-hwclock-rename.md)。

修复 lmbench musl 后 glibc 卡死：ext4 已存在目录创建失败时不再在持锁状态析构临时 inode 并重入同一自旋锁。RISC-V 24 项 musl 单项矩阵、真实 musl+glibc 组合均完整结束，LoongArch64 编译通过；详见 [problem/lmbench-ext4-eexist-drop-self-deadlock.md](./problem/lmbench-ext4-eexist-drop-self-deadlock.md)。

修复 RISC-V 双 hart LTP `kill10` 信号路径锁序风险：信号投递和 handler 现在均先快照 `SigTable` action，再进入 `TaskControlBlockInner` 更新 pending 状态，消除反向嵌套。RISC-V musl/glibc `kill10` 均为 `passed 1 failed 0 broken 0` 并正常 `shutdown!`；详见 [problem/kill10-signal-lock-order.md](./problem/kill10-signal-lock-order.md)。

修复 RISC-V release+CFS 的 LTP `kill10` `pause()` 卡死：无 fd `ppoll` 现在原子发布 Blocked 等待，ITIMER 能唤醒所有 blocked task，并补齐 raw ppoll signal-mask ABI。最新 `log.ans` 中 musl `kill08/kill10` 均通过并正常 `shutdown!`；详见 [problem/kill10-ppoll-itimer-blocked-wakeup.md](./problem/kill10-ppoll-itimer-blocked-wakeup.md)。

修复 RISC-V LTP `mmap001` 卡死：硬件 PTE 构造会将逻辑 `PROT_WRITE` 规范化为 `R|W`，避免生成 `R=0,W=1` 的保留编码；`mmap001` 现为 `passed 4 failed 0 broken 0` 并正常关机。详见 [problem/mmap001-riscv-write-only-pte.md](./problem/mmap001-riscv-write-only-pte.md)。

修复 BuildStorm Debian 根文件系统启动前置条件：目录初始化不再在已存在目录上误报 `EISDIR` 并漏建 DevFS，`/bin -> /usr/bin` 不再被 BusyBox wrapper 覆盖；补齐 `sigaltstack(2)`、`SA_ONSTACK` 信号帧和返回恢复。RISC-V、LoongArch64 release 构建通过，详见新增 problem/ 复盘。

修复 RISC-V 双 hart 启动栈越界和 BuildStorm 多线程 `execve`：每 hart bootstrap stack 提升至 128 KiB，exec 在替换共享地址空间前终止并等待 sibling 退出。完整 BuildStorm QEMU 回归待续跑，详见 [problem/riscv-buildstorm-bootstrap-stack-overflow.md](./problem/riscv-buildstorm-bootstrap-stack-overflow.md) 和 [problem/buildstorm-multithreaded-execve-dethread.md](./problem/buildstorm-multithreaded-execve-dethread.md)。

实现 Linux asm-generic `fadvise64(223)`：补齐 syscall 分发以及 fd、FIFO、长度和 advice 的 Linux errno 校验，合法 hint 当前作为无状态建议成功返回。RISC-V 与 LoongArch64 release 构建通过；按维护者要求未运行 QEMU/LTP，相关黑名单保持不变。

修复 BuildStorm 动态库打开路径的 `..` 规范化缺失：`get_abs_path()` 现对绝对路径与相对路径一致折叠 `.`/`..`，使 Rust 工具链 DSO 能匹配既有镜像路径兼容表。RISC-V 与 LoongArch64 release 构建通过，未运行 QEMU 回归。

## 7.21

扩展 LoongArch 评测配置至 8GiB/8 核：内核现按 QEMU 分段 RAM 纳管 CMA，并通过 mailbox/IPI 启动 7 个 AP；`getcpu`、affinity 和 `/proc/cpuinfo` 同步拓扑。LoongArch64 release 构建及真实 QEMU 基础回归均完成并正常关机；详见 [problem/loongarch-8g-8hart-bootstrap.md](./problem/loongarch-8g-8hart-bootstrap.md)。

修复 final-2026 Debian 动态库错误回退、ext4 `getdents64.d_type` ABI 和通用 `FIONBIO` 语义；RISC-V `log.ans` 已出现 `BUILDSTORM_TOOLCHAIN ok`，详见新增 problem/ 复盘。

RISC-V、LoongArch64 release 构建通过；本机仅 `2G / 2 CPU`，guest 内 minibuild 编译在 300 秒窗口仍持续满载，未将完整 BuildStorm 编译或性能项标记为通过。
实现 Linux asm-generic `rseq(293)`：补齐 syscall 分发、经典 32-byte ABI 的线程级注册状态和用户态入口，并加入基础回归探针。RISC-V `log.ans` 已输出 `rseq regression: PASS`；未运行 LTP/Linux rseq selftest 或完整 critical-section 语义验收，详见 [problem/rseq-syscall.md](./problem/rseq-syscall.md)。

接入 RISC-V `riscv_hwprobe(258)` 的最小桩：仅登记和分发，固定返回 `ENOSYS`，不会向用户态伪造硬件能力数据。RISC-V 与 LoongArch64 release 构建通过；未运行 QEMU 行为回归。

修复评测机 `8G / 8 CPU` RISC-V 启动：本地 allocator 现可表示 8GiB CMA 的高阶块，汇编 bootstrap stack 数同步为 8。双架构 release 构建通过，默认配置 QEMU 已启动全部 secondary hart 并进入 `BUILDSTORM_TOOLCHAIN ok`；详见 [problem/riscv-8g-8hart-bootstrap.md](./problem/riscv-8g-8hart-bootstrap.md)。

修复评测机 RISC-V QEMU 自动分配 VirtIO 网卡后启动日志报无设备：内核将网卡 MMIO 基址和映射改为 `virtio-mmio-bus.7` 的 `0x10008000`，无需改动评测机 QEMU 参数。RISC-V 启动已正常初始化网络；详见 [problem/riscv-virtio-net-mmio-autobus.md](./problem/riscv-virtio-net-mmio-autobus.md)。

记录 BuildStorm `BUILDSTORM_TOOLCHAIN ok` 后 minibuild 超时：尝试减少 lwext4 大文件的重复小文件缓存准入并复用只读 private DSO 页；RISC-V release 构建通过，但 10 分钟 QEMU 运行未到达 `BUILDSTORM_MINIBUILD ok`。复核还发现两项 mmap/cache 语义风险，源码暂不提交，详见 [problem/buildstorm-minibuild-post-toolchain-stall.md](./problem/buildstorm-minibuild-post-toolchain-stall.md)。

拆分 BuildStorm 诊断脚本为工具链、MINIBUILD prepare/build、`tg-xtask` 预构建和正式编译五项，并避免向评分日志输出正式 `BUILDSTORM_*` 标记。RISC-V、LoongArch64 release 构建通过；RISC-V 独立运行确认 prepare 完成后进入原始静默 `cargo build`，尚未得到 MINIBUILD 完成或根因结论；详见同一 problem 复盘。

修复 BuildStorm MINIBUILD Rustc 的 mmap 虚拟地址预算不足：两架构累计 mmap 上限从 512 MiB 提升到 2 GiB，已消除日志中的首个 `ENOMEM`。强制新建项目后进一步暴露 Cargo worker 普通 fork 的动态栈 `EFAULT`，完整 MINIBUILD 仍待修复；详见 [problem/buildstorm-minibuild-post-toolchain-stall.md](./problem/buildstorm-minibuild-post-toolchain-stall.md)。

修复 BuildStorm `clone3/vfork` 共享地址空间交接：vfork 父 task 现立即让出 CPU，exec 在新地址空间与 trap context 安装后才唤醒父 task，非线程 `CLONE_VM` 子进程继承父 hart，且两架构统一 clone3 stack ABI。RISC-V、LoongArch64 release 构建通过；完整 MINIBUILD 的动态栈 fork `EFAULT` 仍待单独修复。详见 [problem/buildstorm-vfork-clone3-lifecycle.md](./problem/buildstorm-vfork-clone3-lifecycle.md)。

修复 lwext4 大文件 mmap 触发的小文件缓存准入重复探测：超限文件只探测一次，`initialize cache!` 仅记录真实缓存成功；RISC-V MINIBUILD 完成并正常关机，详见 [problem/buildstorm-minibuild-post-toolchain-stall.md](./problem/buildstorm-minibuild-post-toolchain-stall.md)。

修复 BuildStorm MINIBUILD fresh `cargo build` 的普通 fork：动态 `MAP_STACK` VMA 现按 mmap 语义继承，固定初始 stack 仍由 child 重建，避免 `CLONE_CHILD_SETTID` 写入 `EFAULT`。RISC-V debug 日志已确认原失败 clone 成功创建并运行子进程，双架构 release 构建通过；fresh RISC-V 600 秒回归尚未到达 MINIBUILD 完成标记，详见 [problem/buildstorm-minibuild-post-toolchain-stall.md](./problem/buildstorm-minibuild-post-toolchain-stall.md)。

## 7.22

修复 BuildStorm MINIBUILD fresh fork 的 TrapContext 页错误覆盖，并删除已无调用的 eager `clone_area()` 接口；final Debian 原生 libc 的 linker script 现优先使用镜像 native `ld-linux`，仅在原路径缺失时使用 legacy fallback。RISC-V fresh `prepare -> build` 已输出 `BUILDSTORM_DEBUG_MINIBUILD ok` 与 `shutdown!`；RISC-V、LoongArch64 release 构建均通过，详见 [problem/buildstorm-minibuild-fresh-fork-loader.md](./problem/buildstorm-minibuild-fresh-fork-loader.md)。
修复 CAgent 决赛脚本启动：决赛运行器改用 `/bin/bash`，VFS 可正确解析 Debian `/bin -> /usr/bin` 中间符号链接。RISC-V 日志已到达 CAgent `GROUP END` 和 `shutdown!`，7 项 pass、3 项 reject；详见 [problem/cagent.md](./problem/cagent.md)。

实现 Linux `fchdir(50)`：从 fd 直接定位目录 inode，校验 `EBADF`、`ENOTDIR` 和目录 search 权限后更新共享 cwd，并补齐用户态封装。RISC-V `log.ans` 中 musl/glibc 的 `fchdir01` 至 `fchdir03` 均为 `TPASS`，已解除对应 LTP blacklist。

实现 Linux `prctl(PR_SET_CHILD_SUBREAPER)`：进程级 subreaper 标记现会在父进程退出时接管孤儿后代，并补齐 `PR_GET_CHILD_SUBREAPER`。RISC-V musl/glibc `prctl03` 均为 `passed 6 failed 0 broken 0` 且正常 `shutdown!`；未验证 LoongArch64，详见 [problem/prctl-child-subreaper.md](./problem/prctl-child-subreaper.md)。

修复 LTP `prctl04` seccomp 语义：`PR_SET/GET_SECCOMP` 现保存线程级 strict/filter 状态，在 syscall 入口执行严格模式和已验证的经典 BPF 子集，并由 fork/clone 继承。RISC-V、LoongArch64 的 musl/glibc 均为 `passed 9 failed 0 broken 0` 且正常 `shutdown!`；详见 [problem/prctl-seccomp-prctl04.md](./problem/prctl-seccomp-prctl04.md)。

修复 LoongArch 八核 basic 随机非法指令与段错误：COW 分裂现固定源 `FrameTracker`，先复制新页再替换 PTE，避免父子跨 hart 并发释放并复用仍在读取的源页。LoongArch64 release 构建通过，`basic-musl/basic-glibc` 连续五轮完整结束并正常关机；详见 [problem/loongarch-cow-source-frame-race.md](./problem/loongarch-cow-source-frame-race.md)。

修复 LTP read03 `TBROK: Mode does not indicate fifo file`：mknodat 创建的 FIFO 在 ext4 上 inode mode 类型位被误置为普通文件，fsidx 记录的正确 InodeType 未应用于 fstat 输出。在 `Ext4Inode::fstat()` 末尾用 `FsIndex::special_node_type` 修正 st_mode 类型高位。LoongArch64 musl/glibc read03 均 TPASS；详见 [problem/read03-fifo-mode-type-bits.md](./problem/read03-fifo-mode-type-bits.md)。

修复 LoongArch LTP `fs_bind01`：启动期兼容 BusyBox hush 的 LTP timeout 局部变量写法，并将 bind mount 的 `/proc/mounts` source 改为非路径表示，避免 BusyBox 单次误卸两层挂载。LoongArch64 musl 单跑为 `passed 29 failed 0 broken 0` 并正常关机；详见 [problem/fs-bind01-loongarch-hush-timeout-mount-stack.md](./problem/fs-bind01-loongarch-hush-timeout-mount-stack.md)。

## 7.22

修复 LTP `kill07`：用户态 `SIGKILL` 现会在进入 pending 集时记录进程终止原因，阻塞快速退出后 `waitpid()` 正确返回信号终止 status；内部 execve sibling 清理信号不会污染状态。LoongArch64 单跑为 `passed 1 failed 0 broken 0` 并正常 `shutdown!`，双架构 release 构建通过；详见 [problem/kill07-sigkill-fast-exit-wait-status.md](./problem/kill07-sigkill-fast-exit-wait-status.md)。

修复 LoongArch CAgent 的动态链接器 LSX panic：每个 hart 现启用 `EUEN.SXE`，用户 trap、信号和 clone 上下文完整保存 32 个 128-bit LSX 寄存器。LoongArch64 release 构建通过，`log.ans` 到达 CAgent `GROUP END` 和 `shutdown!`，无 panic；详见 [problem/loongarch-cagent-lsx-disabled-panic.md](./problem/loongarch-cagent-lsx-disabled-panic.md)。

修复 LoongArch 批量 LTP 在 `fsmount01` 前的 Rust heap OOM：全局 heap 耗尽后可安全接管 CMA 连续页，LTP 退出边界同步回收 idle VFS 缓存、shared mmap 和遗留 helper。双架构 release 构建通过；LoongArch `fsmount01` 单例无 heap panic、6 项 TPASS，但仍有 1 项既有 TBROK，完整批量回归受运行窗口限制待续。详见 [problem/loongarch-ltp-heap-cma-reclaim.md](./problem/loongarch-ltp-heap-cma-reclaim.md)。

## 7.22

修复 BuildStorm Rustc 编译 `unicode-ident` 的 `mremap(MREMAP_MAYMOVE)` 数据丢失：私有 VMA 现先复制已驻留页、全部成功后才卸载旧映射。RISC-V、LoongArch64 构建和 RISC-V debug 构建通过；RISC-V probe 的原文件/复制文件 Rustc 均返回 0，但 180 秒窗口未完成完整 Cargo 编译。详见 [problem/buildstorm-mremap-data-loss.md](./problem/buildstorm-mremap-data-loss.md)。

## 7.22

修复 BuildStorm Rustc artifact rename 后的 lwext4 write-back 时序：rename 前先完整回写并清除旧 pathname cache，成功后清理源/目标孤立状态，短写返回 `EIO` 阻止发布。RISC-V、LoongArch64 release 构建通过，RISC-V `rename_publish` 回归通过；完整 `cargo build -p tg-xtask` 仍未在本轮完成，详见 [problem/buildstorm-rustc-artifact-rename-writeback.md](./problem/buildstorm-rustc-artifact-rename-writeback.md)。

## 7.22

将 BuildStorm 分阶段诊断从 `initfiles.rs` 的启动期脚本注入迁至 `user/src/bin/buildstorm/`：各 case 在用户态写入固定 `/tmp/buildstorm-*.sh` 后由 Bash 单独执行，保留官方顺序与扩展诊断组合入口。RISC-V snapshot 已确认选定脚本从 `/tmp` 启动并进入 `cargo build -p tg-xtask`，但 120 秒窗口未完成；内核不再向根文件系统写入 BuildStorm 测试脚本，详见 [problem/buildstorm-minibuild-post-toolchain-stall.md](./problem/buildstorm-minibuild-post-toolchain-stall.md)。
修复 LoongArch CAgent loopback TCP 大请求：仅 IPv4 loopback 使用 4096 B MSS 并以 IPv4 分片传输，修正 smoltcp 首片构造对整个 fragmentation buffer 计算 TCP checksum 的错误，同时串行排空全局 fragmenter。新增 checksum 回归测试通过；最终 LoongArch64 CAgent 十项均 pass，RISC-V release 构建通过，详见 [problem/cagent-loopback-tcp-fragmentation.md](./problem/cagent-loopback-tcp-fragmentation.md)。

将定制 `smoltcp` 从 `os/vendor/smoltcp` 迁移到 `crates/smoltcp`，保持 `[patch.crates-io]` 本地补丁模式；`os/dotcargo/config` 的 `vendor` 目录仍只服务其余离线注册表依赖。smoltcp 定向离线单测与 RISC-V、LoongArch64 release 构建均通过。

修复 BuildStorm 全量入口误用分阶段 DEBUG 输出导致 judge 为 0 分：正式 runner 现构建期嵌入参考脚本并从 `/tmp` 一次执行。双架构构建通过；RISC-V 180 秒回归已恢复 `BUILDSTORM_TOOLCHAIN/MINIBUILD ok`，judge 为 20/180，预构建阶段超时，尚未验证 compile/time 项；详见 [problem/buildstorm-full-run-marker-contract.md](./problem/buildstorm-full-run-marker-contract.md)。

## 7.23

优化 BuildStorm 普通只读路径：跳过一次性读取不需要的 whole-file cache 初始化和 `fseek`，
跨页 `read()` 合并为一次 EXT4 操作，并移除每次读取前的冗余 `inode.size()` 探测；同时将
调度器 timer 维护扫描改为按 hart 快照、10 ms 节流。双架构 release 构建通过，独占 RISC-V
运行进入 `pre-build tg-xtask` 且无错误，但完整 446 crate 和严格 A/B 加速比例仍待完成，详见
[problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

## 7.23

修复 LoongArch BuildStorm 对稀疏 EXT4 文件的读取破坏：hole 现零填且不参与物理块聚合，C archive 会随源码变更重建；`uname -m` 同时返回标准 `loongarch64`。官方 Docker 重建后 Rust 工具链和 MINIBUILD 均恢复 `ok`，完整 compile 仍因外层 QEMU 终止待续跑；详见 [problem/loongarch-buildstorm-sparse-ext4-read-corruption.md](./problem/loongarch-buildstorm-sparse-ext4-read-corruption.md)。
## 7.22

修复 LoongArch64 LTP `fcntl14` 的 `rt_sigsuspend` 信号掩码恢复时机：临时解除屏蔽的 `SIGUSR1` 现可先建立 handler frame，并在 `rt_sigreturn` 后恢复调用前 mask。LoongArch64 musl/glibc `fcntl14` 均为 `passed 96 failed 0 broken 0`；详见 [problem/fcntl14-sigsuspend-mask-restore.md](./problem/fcntl14-sigsuspend-mask-restore.md)。

## 7.23

修复 LTP `lseek11` 的 ext4 `SEEK_DATA/SEEK_HOLE`、sparse write 和 `ftruncate` partial-tail 数据一致性，并收口 sparse cache 的首次打开、`O_TRUNC`、unlink 生命周期及 ftruncate 锁错误路径：LoongArch64 musl/glibc 均为 `passed 15 failed 0 broken 0` 并正常 `shutdown!`；临时 `ftruncate01` 两侧均通过后已移除入口。RISC-V、LoongArch64 release 构建通过，详见 [problem/lseek11-ext4-seek-data-hole-sparse-write.md](./problem/lseek11-ext4-seek-data-hole-sparse-write.md)。

## 7.23

修复 BuildStorm Rustc 长 `argv` 被 pathname 256 B 上限截断导致的 `E0765`：`execve` 现按显式上限保存原始参数字节，并在替换地址空间前完成可失败的新栈构造；RISC-V 无任务时改用 timer-backed `WFI`，LoongArch 保持保守轮询。`initproc` 的定向入口保留子测例返回码。双架构构建通过，定向 prebuild 已越过原错误位置但完整 446 单元尚未完成，详见 [problem/buildstorm-execve-argv-truncation.md](./problem/buildstorm-execve-argv-truncation.md)。

优化 BuildStorm 并行构建：恢复 `sched_getaffinity` 的八核可见拓扑，允许干净 `MAP_PRIVATE` 文件页跨进程复用并通过 COW 保持私有写语义，同时删除 mmap 缺页的重复清零/中间复制。RISC-V、LoongArch64 release 构建通过，RISC-V 180 秒启动已进入多 crate Cargo 预构建；完整 446 单元耗时对比尚未完成，详见 [problem/buildstorm-parallel-file-cache.md](./problem/buildstorm-parallel-file-cache.md)。
收束决赛 CAgent/BuildStorm 本地诊断包装：平台入口继续在 `/glibc` 下以 `run_final_testsuit` 直接运行两个正式 `testcode.sh`；CAgent 仅保留 10 个单项入口，BuildStorm 仅保留 toolchain、MINIBUILD、compile success、compile time 四项。两架构用户态构建通过；未运行 QEMU，因为单项依赖 final 镜像环境且完整编译最长可达 4 小时。

## 7.24

为 BuildStorm 长时间编译增加低开销 guest 内统计：系统调用按 read/write/open/close/stat/mm/process/futex 分类，EXT4 记录读取字节数及全局锁等待/持有 tick，文件页缓存记录命中/缺页，调度器记录 ready queue 选取和 idle 次数；汇总最多每 30 秒输出一次，不逐条污染评分日志。独占 RISC-V 90 秒样本无 panic/错误，在 63 秒时观察到约 55 万次 syscall、13.3 万次 futex 和 631 万次调度选取，EXT4 锁等待约 47 万 tick、持有约 2.63 亿 tick。该结果表明下一轮应优先检查调度/任务切换频率，不能继续把文件系统假定为唯一瓶颈；双架构 release 构建通过。详见 [problem/buildstorm-read-path-lock-contention.md](./problem/buildstorm-read-path-lock-contention.md)。

优化 timer 抢占路径：当前 hart 没有其他就绪任务时直接返回，避免无竞争场景经过 idle 上下文往返；CFS/RR 均提供按 hart 的就绪检查，阻塞和主动让出路径保持原语义。双架构 perf 构建通过，完整 BuildStorm A/B 尚未完成，详见 [problem/scheduler-uncontended-preemption.md](./problem/scheduler-uncontended-preemption.md)。

修复 `rt_sigsuspend` 忙让出：等待信号的任务现在进入 `Blocked`，由信号唤醒，并补齐阻塞前 pending 检查；同时收束无竞争 `sched_yield` 与 timer future `nanosleep` 路径。详见 [problem/sigsuspend-busy-yield-scheduler.md](./problem/sigsuspend-busy-yield-scheduler.md)。

## 7.24

优化 clone 的 procfs 路径：clone 时只登记 PID，首次访问 `/proc/<pid>` 或枚举 `/proc` 时再物化 EXT4 目录；RISC-V 样本中 procfs 阶段由 95703 us 降至 231 us，CAgent CPU 正常通过。详见 [problem/clone-procfs-lazy-materialization.md](./problem/clone-procfs-lazy-materialization.md)。
