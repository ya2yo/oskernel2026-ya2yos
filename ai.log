# AI 工作日志

## 2026-05-27: futex add_signal 与 futex_wake 竞态修复

### 问题发现

用户提供 `log.ans`（内核日志），要求分析报错原因。

日志末尾 panic：
```
[kernel] Panicked at src/task/manager.rs:43 add task fail: task already in queue!
```

### 根因分析

从日志中提取关键事件序列（TID 5 与 TID 6 的交互）：

1. TID 5 `clone` 创建 TID 6，设置 `SIGRT_1` 信号处理函数（SA_RESTART）
2. TID 6 调用 `futex(FUTEX_WAIT, pa)` → 创建 Waiter 插入 `FUTEX_QUEUE_BITMAP[pa]` → `block_current_and_run_next()` 进入 Blocked
3. TID 5 调用 `tkill(SIGRT_1 → TID 6)` → `add_signal()` 检测到 TID 6 为 Blocked → 改 Ready → `ready_queue::add_task(TID 6)` ← **第一次入队**
4. TID 5 继续运行（非抢占内核），调用 `futex(FUTEX_WAKE, pa)` → 遍历 `FUTEX_QUEUE_BITMAP[pa]` 找到残留 Waiter → `wakeup_futex_task(TID 6)` → `ready_queue::add_task(TID 6)` ← **第二次入队 → PANIC**

根因位于 `os/src/signal/mod.rs:240` 的 `add_signal` 函数：向阻塞态任务发送信号时直接将其加入就绪队列，但没有清理 `FUTEX_QUEUE_BITMAP` 中对应的 Waiter 条目。

此前已有的修复（`futex_wait_bitset` 中 EINTR 返回前清理 Waiter）只能覆盖**任务先被调度运行**再检测信号的路径。在非抢占内核中，发送信号的线程 TID 5 在 `tkill` 后继续执行 `futex_wake`，TID 6 无机会运行清理代码。

### 修改内容

| 文件 | 函数/位置 | 修改 |
|------|-----------|------|
| `os/src/task/manager.rs:71` | `wakeup_futex_task` | 新增 `task_status == Ready` 检查：若任务已被信号唤醒则在就绪队列中，跳过重复入队，仅清理 futex 字段 |
| `os/src/task/futex.rs:182` | `new_futex_key` | `fetch_add(1) + 1` 使 key 从 1 开始分配，0 稳定表示"无效/已清理" |
| `os/src/task/futex.rs:127` | `futex_wait_bitset` EINTR 路径 | 新增 `futex_key != 0` 守卫，防止 `wakeup_futex_task` 已清理后用 key=0 误操作 |

---

## 2026-05-27: 内核堆碎片化 — 全量测例 OOM

### 问题发现

用户提供 `log.ans`（全量测例日志）和 `no-log.ans`（单独 iozone 日志），要求分析为何全量运行 panic 而单独运行正常。

日志末尾 panic（与 futex panic 不同）：
```
[kernel] Panicked at src/mm/heap_allocator.rs:18 Heap allocation error,
         layout = Layout { size: 13770752, align: 1 (1 << 0) }
```

### 根因分析

全量运行测试序列（log.ans）：
```
 1. basic-musl          ✓
 2. busybox-musl        ✓
 3. libctest-musl       ✓  (最长，652 行日志)
 4. lua-musl            ✓
 5. iozone-musl         ✓
 6. libcbench-musl      ✓
 7. lmbench-musl        ✓
 8. basic-glibc         ✓
 9. busybox-glibc       ✓
10. lua-glibc           ✓
11. iozone-glibc        ✘ PANIC (堆分配失败)
```

单独运行仅 iozone-glibc：正常完成，shutdown。

内核堆 48MB（buddy system allocator，2^n 分配）。iozone 申请 13.77MB → 向上取整为 16MB 块（2^24 ≈ 16MB）。

单独运行时堆是干净的，16MB 连续块存在。全量运行 10 组测例后，**FsIndex 全局 inode 缓存** (`fs/kernel_fs_ops/fsidx.rs`) 持续增长导致碎片化。

追踪了完整的资源生命周期：
- `Process::drop` (process.rs:218): `PID_2_PROCESS_ARC.remove()` 被注释掉，但 `sys_wait4` → `remove_from_global_map` 补偿移除
- `FsIndex`: 每次 `open()` 插入条目，仅在 `sys_unlinkat` 时删除，`sys_close` 不清理
- `PAGE_CACHE`: 最多 128 页（512KB），有界，影响有限
- `GROUP_SHARE`: MapArea 共享帧管理，drop 路径正确

### 修改内容

| 文件 | 函数 | 修改 |
|------|------|------|
| `os/src/syscall/fs/fd_ops.rs:219` | `sys_close` | 关闭 fd 前获取 inode path，在 `fd_table.take()` 释放 OSFile 后检查 `Arc::strong_count <= 2`，若仅剩 FsIndex 持有则淘汰缓存条目 |

### 涉及文件汇总

- `os/src/task/manager.rs` — `wakeup_futex_task` 防重复入队
- `os/src/task/futex.rs` — `new_futex_key` 从 1 起始 + EINTR 路径守卫
- `os/src/task/mod.rs` — `exit_current_group_and_run_next` debug 日志
- `os/src/syscall/fs/fd_ops.rs` — `sys_close` FsIndex 缓存淘汰
- `user/src/bin/initproc.rs` — 启用多组测例
- `Docs/初赛文档/problem/` — futex + OOM 问题文档
- `ai.log` — 本文件（新建）

---

## 2026-05-28: wait4 信号处理与托孤修复

### 问题发现

libctest、lmbench 测例卡死。用户提供现象后，使用 Claude 对用户态二进制反汇编，定位到测试进程在 `wait4` 处反复进出内核。

另发现父进程先于子进程退出时，子进程未挂到 initproc。

### 交互与分析过程

1. AI 分析反汇编结果，指出 busy-wait 发生在 `wait4` 系统调用路径。
2. 阅读 `wait.rs`：阻塞前未调用 `check_if_any_sig_for_current_task`，与 Linux 可中断阻塞语义不符。
3. 阅读 `process.rs` / `mod.rs`：进程退出时缺少将孤儿进程 reparent 到 initproc 的逻辑。
4. 人工确认后实现：wait 路径增加信号分支 + `interruptible` 包装；新增 `exit_and_reparent`。

### 修改内容

| 文件 | 修改 |
|------|------|
| `os/src/syscall/task/wait.rs` | 阻塞前处理 pending 信号；`interruptible` + `block_on` |
| `os/src/task/process/process.rs` | `exit_and_reparent`、`link_child_to_parent` |
| `os/src/task/mod.rs` | 进程全线程 zombie 时调用托孤 |

### 涉及文件汇总

- 上述代码文件
- `Docs/初赛文档/problem/wait4-signal.md`、`exit-reparent.md`
- `Docs/初赛文档/开发日志.md` — 5.28 条目

---

## 2026-05-29: accept02 组播与 setsockopt 日志分析

### 问题发现

用户提供多份 `log.ans`，依次出现：

1. `router.rs` 断言 `left: 10.0.2.15, right: 127.0.0.1`
2. `virtio/net.rs` token 65472 越界 panic
3. `accept02.c:73 TFAIL: Multicast group was copied!`（后期 log 在修复 syscall 前）

### 交互与分析过程

1. 对照 LTP accept02 源码（CVE-2017-8890）：accepted fd 上 `MCAST_LEAVE_GROUP` **应失败** `EADDRNOTAVAIL`。
2. 追踪 IGMP 发送：`join_multicast_group` 后 `poll_interfaces` 触发组播包；源地址取决于 IP 注册顺序 → 调整 eth0/lo 顺序。
3. 组播目的 `224.0.0.0` 走 default 路由；`ethernet.send` 对组播误走 ARP → 添加组播 MAC 直发。
4. 阅读 `tcp.rs`：`new_connected` 的 `memberships` 为空，`LeaveGroup` 正确返回 `EADDRNOTAVAIL`。
5. 阅读 `opt.rs`：`match` 结果未 `?`，导致 syscall 始终返回 0 → 用户误判为组播被复制。
6. virtio：对非法 token 加边界检查，避免 panic（WARN 仍可能出现）。

### 修改内容

| 文件 | 修改 |
|------|------|
| `os/src/net/mod.rs` | eth0 先于 lo 注册 IP |
| `os/src/net/router.rs` | IPv4 组播路由；注释断言 |
| `os/src/net/device/ethernet.rs` | 组播 MAC 映射与直发 |
| `os/src/net/tcp.rs`, `udp.rs` | JoinGroup / LeaveGroup |
| `os/src/syscall/net/opt.rs` | MCAST 选项 + `?` 传播错误 |
| `os/src/drivers/virtio/net.rs` | `token_index` 边界检查 |

### 涉及文件汇总

- 上述代码文件
- `Docs/初赛文档/problem/accept02-mcast-setsockopt.md`
- `Docs/初赛文档/开发日志.md` — 5.29 条目
- `.claude/skills/` — 项目 Agent 技能（doc-writing 等）

---

## 2026-05-30: LTP access04 多轮 log 分析与修复

### 问题发现

用户多次提供 `log.ans` 排查 `access04`：

1. RISC-V：`mkdirat(mntpoint)` 后 `translate.rs:118` panic（`sys_mount` + `translated_str`）
2. LoongArch：`TBROK Failed to acquire device`，loop 路径 `ENOENT`
3. 修复 loop 后：`tmpfs mount EFAULT`（`special=NULL`）、`/dev/loop-control` 缺失

用户确认 LoongArch musl 测例已全部通过后，要求写入文档。

### 交互与分析过程

1. 对照 `iozone-glibc.md` / `copy_from_user` 规范，确认 mount 与 statx 同根因。
2. 分析 LTP 日志：`tst_test.c` 先尝试 tmpfs 只读挂载，失败后 fallback 到 loop 块设备。
3. 实现 `loopdev.rs`：`DevLoop` + `DevLoopControl`（`LOOP_CTL_GET_FREE` 等 ioctl）。
4. `read_user_cstr` 对 NULL 返回空串，满足 tmpfs `mount(NULL, ...)` 语义。
5. 保留 LA `handle_mprotect` 懒分配页跳过逻辑；移除调试期 `find_pte` 日志。
6. 用户曾要求撤销 premature 文档；测例通过后再按 doc-writing 补全四份文档。

### 修改内容

| 文件 | 修改 |
|------|------|
| `os/src/syscall/fs/mount.rs` | `copy_from_user`；NULL special |
| `os/src/fs/files/loopdev.rs` | 新建 loop / loop-control |
| `os/src/fs/files/devfs.rs` | 设备打开路径 |
| `os/src/fs/vfs.rs` | `File::ioctl` |
| `os/src/syscall/fs/ctl.rs` | `sys_ioctl` 分发 |
| `os/src/fs/kernel_fs_ops/initfiles.rs` | `/dev/block` 等目录 |
| `os/src/arch/loongarch64/qemu/page_table.rs` | `handle_mprotect` |

### 验证

LoongArch QEMU 单跑 access04：12 TPASS，退出码 0。

### 涉及文件汇总

- 上述代码文件
- `Docs/初赛文档/problem/access04-ltp-musl.md`
- `Docs/初赛文档/开发日志.md` — 5.30 条目

---

## 2026-05-30: LTP access02 execve shebang

### 问题发现

用户提供 LoongArch `log.ans`（`LTP_TEST_START=7`，单跑 `access02`）。12 TPASS / 4 TFAIL，退出码 256。失败信息：

```text
sh: can't execute './file_x': Exec format error
access02.c:129: TFAIL: execute file_x as root failed: SUCCESS (0)
```

`access(file_x, X_OK)` 已通过，符号链接用例也全部 TPASS。

### 根因分析

对照 LTP `access02.c`：X_OK 分支在 `access()` 通过后调用 `system("./file_x")`。setup 写入 `file_x` 为 mode `0555`、内容 `#!/bin/sh\n`。

`sys_execve` 仅识别 ELF，非 ELF 返回 `ENOEXEC`；busybox 报 Exec format error。已有 `.sh` 后缀特判不覆盖 `file_x`。`/bin/sh` symlink 已存在，缺的是内核 shebang 解析。

### 修改内容

| 文件 | 修改 |
|------|------|
| `os/src/syscall/task/execve.rs` | `parse_shebang`；非 ELF 时按 Linux binfmt_script 重建 argv 并加载解释器 |

### 验证

用户确认 LoongArch `access02` 全部通过。

### 涉及文件汇总

- `os/src/syscall/task/execve.rs`
- `Docs/初赛文档/problem/access02-ltp-execve.md`
- `Docs/初赛文档/开发日志.md` — 5.30 条目

---

## 2026-05-31 — setresgid(149) 系统调用

### 用户请求

根据 skill 实现 149 号系统调用（Linux `setresgid`）。

### 实现

1. `TaskControlBlockInner` 增加 `real_gid` / `effective_gid` / `saved_gid`，fork 继承
2. `os/src/syscall/sys.rs`：`sys_setresgid`、`sys_getresgid`；更新 `getgid`/`getegid`/`getgroups`
3. `syscall/mod.rs`：路由 `SetResgid`(149)、`GetResgid`(150)、`GetResuid`(148)；修正 GetResgid 编号
4. 顺带修复 `sys_chroot` 编译错误（`read_user_cstr` + 目录检查）

### 验证

RISC-V 单跑 `setresgid01`：5× TPASS。

### 涉及文件

- `os/src/task/task/task.rs`
- `os/src/syscall/sys.rs`
- `os/src/syscall/mod.rs`
- `Docs/初赛文档/problem/setresgid-syscall.md`

---

## 2026-05-31: clone04 缺页 SIGSEGV 与 _Fork 语义修复

### 问题发现

用户提供 `log.ans`，`clone04` 测试退出码 254。日志显示 `StorePageFault` at `0xfffffffffffffff0`，内核打印 "don't send SIGSEGV, just exit the process"。

### 根因分析

AI 分析 `clone04.c` 源码：测试调用 `ltp_clone(0, child_fn, NULL, CHILD_STACK_SIZE, NULL)`，预期返回 EINVAL。musl 的 `__clone` 汇编 wrapper 在 syscall 前向 `child_stack - 16 = -16 = 0xfffffffffffffff0` 写入函数指针，触发无法恢复的缺页。

内核 trap handler（`trap/mod.rs`）仅在进程有自定义 SIGSEGV handler 时才发送信号，否则直接 `exit(-2)`。这与 Linux 行为不符：应始终发送 SIGSEGV，由信号分发机制根据默认动作终止。

同时发现 fork 路径子进程 `sig_mask` 继承自父进程，不符合 `_Fork`（POSIX 严格 fork）的干净掩码要求。

### 修改内容

| 文件 | 位置 | 修改 |
|------|------|------|
| `os/src/trap/mod.rs` | `fn trap_handler` 缺页分支 | 删掉 `has_sigsegv_handler` 检查，无条件 `send_signal_to_thread(SIGSEGV)` |
| `os/src/task/task/task.rs` | `fn clone_process` fork 路径 | `sig_mask` 改为 `SigSet::empty()` |

### 验证

进程被 SIGSEGV 正确终止，退出码 128+11=139（替代原 254）。

### 涉及文件汇总

- `os/src/trap/mod.rs`
- `os/src/task/task/task.rs`
- `Docs/初赛文档/problem/clone04-fork-sigsegv.md`
